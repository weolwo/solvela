<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sa.mall.favorite.dao.MallFavoriteDao">

    <resultMap id="MallFavoriteVO" type="sa.mall.favorite.domain.vo.MallFavoriteVO">
            <result column="id" property="id" />
            <result column="member_id" property="memberId" />
            <result column="commodity_id" property="commodityId" />
            <result column="create_time" property="createTime" />
            <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="base_columns">
                id, 
                member_id, 
                commodity_id, 
                create_time, 
                update_time
    </sql>

    <sql id="query_conditions">
        <where>
        </where>
    </sql>

    <select id="queryPage" resultMap="MallFavoriteVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_mall_favorite
        <include refid="query_conditions"/>
        ORDER BY create_time DESC
    </select>

    <select id="queryList" resultMap="MallFavoriteVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_mall_favorite
        <include refid="query_conditions"/>
        ORDER BY create_time DESC
    </select>


</mapper>