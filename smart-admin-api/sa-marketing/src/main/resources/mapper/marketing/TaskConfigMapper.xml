<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sa.task.taskconfig.dao.TaskConfigDao">

    <resultMap id="TaskConfigVO" type="sa.task.taskconfig.domain.vo.TaskConfigVO">
            <result column="id" property="id" />
            <result column="activity_code" property="activityCode" />
            <result column="tenant_id" property="tenantId" />
            <result column="task_name" property="taskName" />
            <result column="template_code" property="templateCode" />
            <result column="trigger_event" property="triggerEvent" />
            <result column="task_group" property="taskGroup" />
            <result column="target_audience" property="targetAudience" />
            <result column="limit_type" property="limitType" />
            <result column="limit_count" property="limitCount" />
            <result column="rule_config" property="ruleConfig" />
            <result column="sort_weight" property="sortWeight" />
            <result column="action_url" property="actionUrl" />
            <result column="ui_config" property="uiConfig" />
            <result column="status" property="status" />
            <result column="start_time" property="startTime" />
            <result column="end_time" property="endTime" />
            <result column="create_by" property="createBy" />
            <result column="create_time" property="createTime" />
            <result column="update_by" property="updateBy" />
            <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="base_columns">
                id, 
                activity_code, 
                tenant_id, 
                task_name, 
                template_code, 
                trigger_event, 
                task_group, 
                target_audience, 
                limit_type, 
                limit_count, 
                rule_config, 
                sort_weight, 
                action_url, 
                ui_config, 
                status, 
                start_time, 
                end_time, 
                create_by, 
                create_time, 
                update_by, 
                update_time
    </sql>

    <sql id="query_conditions">
        <where>
                        <if test="queryForm.tenantId != null">
                            AND tenant_id = #{queryForm.tenantId}
                        </if>
                        <if test="queryForm.taskName != null">
                            AND task_name = #{queryForm.taskName}
                        </if>
                        <if test="queryForm.templateCode != null">
                            AND template_code = #{queryForm.templateCode}
                        </if>
                        <if test="queryForm.activityCode != null">
                            AND activity_code = #{queryForm.activityCode}
                        </if>
                        <if test="queryForm.startTime != null">
                            AND start_time = #{queryForm.startTime}
                        </if>
                        <if test="queryForm.triggerEvent != null">
                            AND trigger_event = #{queryForm.triggerEvent}
                        </if>
                        <if test="queryForm.status != null">
                            AND status = #{queryForm.status}
                        </if>
        </where>
    </sql>

    <select id="queryPage" resultMap="TaskConfigVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_task_config
        <include refid="query_conditions"/>
        ORDER BY create_time DESC
    </select>

    <select id="queryList" resultMap="TaskConfigVO">
        SELECT
        <include refid="base_columns"/>
        FROM t_task_config
        <include refid="query_conditions"/>
        ORDER BY create_time DESC
    </select>

                <delete id="batchDelete">
                    delete from t_task_config
                    where id in
                    <foreach collection="idList" open="(" close=")" separator="," item="item">
                        #{item}
                    </foreach>
                </delete>
                <delete id="deleteById">
                    delete from t_task_config
                        where id = #{id}
                </delete>

</mapper>