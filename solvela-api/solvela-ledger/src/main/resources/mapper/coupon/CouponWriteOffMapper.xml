<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="solvela.ledger.coupon.template.dao.CouponWriteOffDao">

    <sql id="base_columns">
        id, coupon_id, member_id, action,
        biz_type, biz_ref_id, scene_code,
        original_amount, discount_amount, deduct_target,
        remark, create_by, create_time
    </sql>

    <!-- 一张券的完整时间线。走 idx_coupon -->
    <select id="selectByCoupon" resultType="solvela.coupon.CouponWriteOff">
        SELECT
        <include refid="base_columns"/>
        FROM t_coupon_write_off
        WHERE coupon_id = #{couponId}
        ORDER BY id DESC
    </select>

    <!-- 🔴 对账入口：按单据号反查用了哪张券、减了多少。走 idx_biz -->
    <select id="selectByBiz" resultType="solvela.coupon.CouponWriteOff">
        SELECT
        <include refid="base_columns"/>
        FROM t_coupon_write_off
        WHERE biz_type = #{bizType}
          AND biz_ref_id = #{bizRefId}
        ORDER BY id
    </select>

    <!--
      本期核销金额。只算 CONFIRM ——
      LOCK 还没成交、RELEASE 是撤销，都不该计入。
    -->
    <select id="sumConfirmedAmount" resultType="java.math.BigDecimal">
        SELECT SUM(discount_amount)
        FROM t_coupon_write_off
        WHERE action = 'CONFIRM'
          AND create_time &gt;= #{from}
          AND create_time &lt; #{to}
    </select>


    <!--
        这一笔对这张券的锁定行。确认/释放时回头抄 original_amount / discount_amount ——
        让每个调用方自己带金额进来，漏传的表现是流水里的金额是个编出来的数，
        而那要等对账时才发现。

        取最后一行：同一张券同一笔单据【可以】锁定多次
        （锁了 → 释放 → 又锁），最后那次才是当前这次。
    -->
    <select id="selectLockRow" resultType="solvela.coupon.CouponWriteOff">
        SELECT
        <include refid="base_columns"/>
        FROM t_coupon_write_off
        WHERE coupon_id = #{couponId}
          AND biz_ref_id = #{bizRefId}
          AND action = 'LOCK'
        ORDER BY id DESC
        LIMIT 1
    </select>

</mapper>