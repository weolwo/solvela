<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.base.module.support.job.repository.SmartJobDao">

    <update id="updateDeletedFlag">
        update t_smart_job
        set deleted_flag = #{deletedFlag}
        where job_id = #{jobId}
    </update>

    <!-- 定时任务-分页查询 -->
    <select id="query" resultType="net.lab1024.sa.base.module.support.job.api.domain.SmartJobVO">
        SELECT *
        FROM t_smart_job
        <where>
            <if test="query.searchWord != null and query.searchWord != ''">
                AND ( INSTR(job_name,#{query.searchWord})
                OR INSTR(job_code,#{query.searchWord})
                OR INSTR(handler_name,#{query.searchWord})
                OR INSTR(trigger_value,#{query.searchWord})
                )
            </if>
            <if test="query.triggerType != null">
                AND trigger_type = #{query.triggerType}
            </if>
            <if test="query.jobGroup != null and query.jobGroup != ''">
                AND job_group = #{query.jobGroup}
            </if>
            <if test="query.enabledFlag != null">
                AND enabled_flag = #{query.enabledFlag}
            </if>
            <if test="query.deletedFlag != null">
                AND deleted_flag = #{query.deletedFlag}
            </if>
            <!--
              终态的一次性任务默认折叠：活动多了以后它们只增不减，
              不过滤的话列表会被历史任务淹没，运营找不到在跑的那些
            -->
            <if test="query.includeTerminal == null or query.includeTerminal == false">
                AND terminal_flag = 0
            </if>
        </where>
        <if test="query.sortItemList == null or query.sortItemList.size == 0">
            ORDER BY sort ASC,job_id DESC
        </if>
    </select>

    <select id="selectByHandlerName" resultType="net.lab1024.sa.base.module.support.job.repository.domain.SmartJobEntity">
        SELECT *
        FROM t_smart_job
        WHERE handler_name = #{handlerName}
          AND deleted_flag = 0
    </select>

    <select id="existsJobCode" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM t_smart_job WHERE job_code = #{jobCode})
    </select>

    <!-- ==================== 抢占式调度 ==================== -->

    <!--
      🔴 数据库时钟。整个调度链路的时间判断都以它为准（铁律 9/10）：
         next_trigger_time 的计算基准、misfire 判定、bizDate 推导，全都不许碰 JVM 时间
    -->
    <select id="selectDbNow" resultType="java.time.LocalDateTime">
        SELECT NOW()
    </select>

    <select id="selectDueJobList" resultType="net.lab1024.sa.base.module.support.job.repository.domain.SmartJobEntity">
        SELECT *
        FROM t_smart_job
        WHERE app_env = #{appEnv}
          AND enabled_flag = 1
          AND deleted_flag = 0
          AND terminal_flag = 0
          AND handler_missing_flag = 0
          AND next_trigger_time IS NOT NULL
          AND next_trigger_time &lt;= #{dueTime}
        ORDER BY next_trigger_time
        LIMIT #{limit}
    </select>

    <!--
      🔴 抢占。后两个条件是状态防御，缺了会出现「幽灵执行」：
         扫描到数据与执行 UPDATE 之间的几毫秒里任务被停用/删除，没有它们照样会被投递，
         而运营的认知是「我明明关了」—— 最难自证的一类故障
    -->
    <update id="preemptJob">
        UPDATE t_smart_job
        SET next_trigger_time = #{nextTriggerTime},
            prev_trigger_time = #{prevTriggerTime},
            trigger_version   = trigger_version + 1
        WHERE job_id = #{jobId}
          AND trigger_version = #{version}
          AND enabled_flag = 1
          AND deleted_flag = 0
    </update>

    <update id="markTerminal">
        UPDATE t_smart_job
        SET terminal_flag = 1,
            next_trigger_time = NULL
        WHERE job_id = #{jobId}
    </update>

    <!-- increment = 0 表示成功后清零，1 表示失败累加 -->
    <update id="updateContinuousFail">
        UPDATE t_smart_job
        SET continuous_fail_count =
            <choose>
                <when test="increment > 0">continuous_fail_count + 1</when>
                <otherwise>0</otherwise>
            </choose>
        WHERE job_id = #{jobId}
    </update>

    <update id="updateHandlerMissing">
        UPDATE t_smart_job
        SET handler_missing_flag = #{missing}
        WHERE job_id = #{jobId}
    </update>

    <!--
      🔴 调度器自监控：next_trigger_time 早于阈值却还没被抢走，说明扫描线程已经挂了。
         这是唯一能发现「任务根本没在跑」的手段 —— 其余告警都建立在「任务跑了」的前提上
    -->
    <select id="selectOverdueJobList" resultType="net.lab1024.sa.base.module.support.job.repository.domain.SmartJobEntity">
        SELECT *
        FROM t_smart_job
        WHERE app_env = #{appEnv}
          AND enabled_flag = 1
          AND deleted_flag = 0
          AND terminal_flag = 0
          AND handler_missing_flag = 0
          AND next_trigger_time IS NOT NULL
          AND next_trigger_time &lt; #{overdueTime}
    </select>

    <update id="deleteTerminalOneTimeJob">
        UPDATE t_smart_job
        SET deleted_flag = 1
        WHERE terminal_flag = 1
          AND deleted_flag = 0
          AND update_time &lt; #{beforeTime}
    </update>

</mapper>
