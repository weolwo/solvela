<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.base.module.support.file.dao.FileDao">

    <!--
      v3.53.0 起 t_file 的两列改了名：file_key -> storage_key、file_name -> original_name。

      FileVO 的属性名<b>刻意保持 fileKey / fileName 不变</b> —— 前端有 5 个文件在用这两个字段名，
      改 JSON 契约是档⑤（调用方迁移）的事，不该在数据模型这一档顺手做掉。
      所以这里显式列出字段并做别名，而不是继续 SELECT *：SELECT * 只会把 storage_key
      映射到 VO 里并不存在的 storageKey 上，fileKey 静默变成 null。

      顺带把 SELECT * 换成显式列表 —— t_file 现在有 20+ 列（含 content_hash / tags 等），
      列表查询没必要全捞。
    -->
    <sql id="voColumns">
        t_file.file_id,
        t_file.category_id,
        t_file.folder_type,
        t_file.original_name AS fileName,
        t_file.storage_key   AS fileKey,
        t_file.file_size,
        t_file.file_type,
        t_file.extension,
        t_file.content_type,
        t_file.storage_kind,
        t_file.visibility,
        t_file.status,
        t_file.tags,
        t_file.creator_id,
        t_file.creator_name,
        t_file.creator_user_type,
        t_file.create_by,
        t_file.create_time
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="net.lab1024.sa.base.module.support.file.domain.vo.FileVO">
        SELECT
        <include refid="voColumns"/>
        FROM t_file
        <where>
            t_file.deleted_flag = 0
            <!--文件夹类型-->
            <if test="queryForm.folderType != null">
                AND t_file.folder_type = #{queryForm.folderType}
            </if>
            <!--文件名词-->
            <if test="queryForm.fileName != null and queryForm.fileName != ''">
                AND INSTR(t_file.original_name,#{queryForm.fileName})
            </if>
            <!--文件Key-->
            <if test="queryForm.fileKey != null and queryForm.fileKey != ''">
                AND INSTR(t_file.storage_key,#{queryForm.fileKey})
            </if>
            <!--文件类型-->
            <if test="queryForm.fileType != null">
                AND t_file.file_type = #{queryForm.fileType}
            </if>
            <!--分类-->
            <if test="queryForm.categoryId != null">
                AND t_file.category_id = #{queryForm.categoryId}
            </if>
            <!--标签。tags 存成前后各带一个逗号的形式（,双十一,banner,），
                所以必须连着逗号一起匹配：直接 INSTR(tags,'618') 会命中「6180」，
                INSTR(tags,'11') 会命中「双11」和「1111」。这种错不报警，
                只是搜出一堆不相干的东西，运营会当成"搜索不准"忍下来-->
            <if test="queryForm.tag != null and queryForm.tag != ''">
                AND INSTR(t_file.tags, CONCAT(',', #{queryForm.tag}, ','))
            </if>
            <!--创建人-->
            <if test="queryForm.creatorName != null and queryForm.creatorName != ''">
                AND INSTR(t_file.creator_name,#{queryForm.creatorName})
            </if>
            <!--创建时间-->
            <if test="queryForm.createTimeBegin != null">
                AND DATE_FORMAT(t_file.create_time, '%Y-%m-%d') &gt;= #{queryForm.createTimeBegin}
            </if>
            <if test="queryForm.createTimeEnd != null">
                AND DATE_FORMAT(t_file.create_time, '%Y-%m-%d') &lt;= #{queryForm.createTimeEnd}
            </if>
        </where>
        <if test="queryForm.sortItemList == null or queryForm.sortItemList.size == 0">
            ORDER BY t_file.create_time DESC
        </if>
    </select>

    <select id="getByFileKey" resultType="net.lab1024.sa.base.module.support.file.domain.vo.FileVO">
        SELECT
        <include refid="voColumns"/>
        FROM t_file WHERE t_file.storage_key = #{fileKey} AND t_file.deleted_flag = 0
    </select>

    <select id="selectByFileKeyList" resultType="net.lab1024.sa.base.module.support.file.domain.vo.FileVO">
        SELECT
        <include refid="voColumns"/>
        FROM t_file WHERE t_file.deleted_flag = 0 AND t_file.storage_key IN
        <foreach collection="fileKeyList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </select>

</mapper>
