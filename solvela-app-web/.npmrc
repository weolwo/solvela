# 精确锁定版本：C 端跑在用户设备上，依赖悄悄升个小版本就可能引入体积或兼容问题。
# 升级走显式 PR，不靠 ^ 漂移。
save-exact=true

# 刻意不开 engine-strict：它会连**传递依赖**的 engines 一起校验，
# 于是某个深层依赖声明了 node ^24.15.0，整个 npm install 就装不动了 ——
# 而那个约束多半跟本项目能不能跑没关系。
# 项目自己的 node 要求写在 package.json 的 engines 和 .nvmrc 里，由 CI 保证。
