# 脚本引擎核心架构与扩展白皮书

> 面向接手人（含几个月后的自己） · 2026-08-23
> 覆盖范围：`common-api` 的 `sa.scriptengine` 包 + `sa.config.ScriptEngineAutoConfiguration` + `sa.aspect.EngineExecutionMonitorAspect`
>
> **本文每一条结论都对着当前代码复核过，并标了文件与行号。**
> 凡是「我记得当初是这样设计的」与代码不一致的地方，**以代码为准**。
>
> 状态：**内核 + 存储双双落地**（2026-08-23）。
> 脚本文件、`t_script` / `t_script_ref` 两表、加载器、挂载点、运行时入口全部就位，存量三处切入点已迁走。
> 后台脚本管理页已上线（系统配置 → 脚本管理）。

---

## 0. 三十秒建立上帝视角

这套东西的定位一句话：**让 Java 写"能力"，让脚本写"怎么组合这些能力"。**
Java 侧提供一堆被白名单暴露的原子函数（发积分、查等级、扣库存），运营/活动侧用 QLExpress
把它们串成具体玩法。脚本引擎是这两者之间的**受控通道**。

它有两条完全独立的链路，**分不清这两条就永远看不懂这个包**：

```
【启动期链路 A】——— 把 Java 能力挂上引擎 ———————————————————————————

  Spring 容器就绪
        │
        ▼
  EngineFunctionScanner.afterSingletonsInstantiated()      ★ 唯一的绑定入口
        │
        ├─① 捞出所有 ScriptFunctionHandler 类型的 Bean
        │      └─ 读 bean.domain() 拿业务域（抽象方法，忘写编译不过）
        │
        ├─② 扫每个 @ScriptFunction 方法 → buildMeta()
        │      ├─ 函数名 = domain.qualify(短名)，如 getLevel → member_getLevel
        │      ├─ 首参若为 EngineContext → injectContext = true，该参数不算脚本入参
        │      └─ 非 public / 自己写了域前缀 → 直接抛异常，应用起不来
        │
        ├─③ registry.register(meta)   同名 → 抛异常（不静默覆盖）
        │
        └─④ 全部扫完后，才逐个 evaluator.registerFunction(meta)
                └─ 包成 QLExpressFunctionAdapter 挂进 Express4Runner


【启动期链路 B】——— 把脚本文件加载进来（与 A 无先后依赖，见 §1.12）——————

  ScriptFileLoader.afterSingletonsInstantiated()
        │
        ├─① 扫 classpath:scripts/**/*.ql，解析文件头 @name/@scene/@desc
        │      └─ 格式不对 / 场景不存在 / 目录与域不符 → 抛，应用起不来
        │
        ├─② 逐个 ScriptEngine.check() 语法校验
        │      └─ ★ 一个脚本语法错 = 整个应用拒绝启动（坏脚本进不了生产）
        │
        ├─③ 同步进 t_script：按 content_hash 判断变没变，变了才写、version +1
        │      └─ params_schema 由 ScriptScene.getParams() 生成，不人工维护
        │
        └─④ 常驻内存 Map<script_code, ExecutableScript>，运行期不查库


【运行期链路】——— 每次执行都跑 ——————————————————————————————————————

  业务代码
        │  ScriptRuntime.evaluate(ScriptRefPoint, refId, ctx, 类型)   ← 业务侧唯一入口
        ▼
  查 t_script_ref 找出这个挂载点挂的是哪个脚本
        │  没挂 → 返回 Optional.empty()（正常情况，不是错误）
        ▼
  ScriptFileLoader 按 script_code 取出可执行体（内存，不查库）
        │
        │  ScriptEngine.evaluate(场景, 脚本, 上下文, 类型)
        ▼
  EngineExecutionMonitorAspect          ★ 切在接口上，不是实现类上（§1.4）
        │  计时 / 慢日志 / 异常分流
        ▼
  DefaultScriptEngine                   门面，只认 ScriptEvaluator SPI
        │
        ▼
  QLExpressEvaluator.evaluate()         ★ 防腐层，唯一认识 qlexpress 类型的地方
        │
        ├─ variables = new HashMap<>(ctx.getScriptVariables())  ← 只有这条通道脚本可见
        ├─ QLOptions: timeout / precise / polluteUserContext(false)
        │             / cache(source == TRUSTED) / attachments{ctx}   ← 每次执行现建
        │
        ▼
  Express4Runner.execute(String, Map, QLOptions)     ★ 必须是这个重载（§1.5）
        │
        └─ 脚本调到 member_getLevel(...) 时
              └─▶ QLExpressFunctionAdapter.call(QContext, Parameters)
                     ├─ injectContext ? 从 QContext.attachment() 取回 EngineContext
                     ├─ ParametersTypeConvertor.cast 只作用于脚本实参
                     └─ method.invoke，拆 InvocationTargetException
```

### 五个必须先记住的事实

| # | 事实 | 违反 / 忘记后的症状 |
|---|---|---|
| 1 | **`EngineFunctionScanner` 是唯一的函数绑定入口** | 绕过它手动 `addFunction`，函数不会出现在 `/script/engine/view` 文档里，前端补全永远缺一块 |
| 2 | **脚本里的函数名一律带域前缀**（`member_` / `mall_` / `task_`…） | 写 `getLevel()` 会报「未知函数」。前缀不是命名规范，是引擎生成的 |
| 3 | **`ScriptEvaluator` 是防腐层边界**，它以上不允许出现 `com.alibaba.qlexpress4.*` | 一旦泄漏，「换引擎」这条退路就没了，而 SPI 存在的唯一理由就是这条退路 |
| 4 | **构造脚本必须显式选 `trusted()` 或 `untrusted()`** | 没有默认工厂。不受控内容误走 TRUSTED，QL 编译缓存以脚本原文为 key 且无上限，直接 OOM |
| 5 | **`allow-java-reflect` 任何环境都是 `false`** | 打开等于把 RCE 面暴露给脚本内容 —— 而脚本是运营在写的 |
| 6 | **`EngineContext` 有两条通道**：`bind` 脚本可见 / `bindInternal` 脚本不可见 | 把 traceId、幂等键之类 `bind` 进去，等于把内部实现细节变成运营看得见的事实契约 |
| 7 | **业务调用一律走 `ScriptRuntime.evaluate(挂载点, refId, ...)`** | 自己拼 `ExecutableScript`、自己查 ref 表，等于把三层细节抄进业务代码 |
| 8 | **脚本内容的权威是文件，`t_script` 只是镜像** | 改库不生效、下次启动被覆盖。改脚本走 git + 发版 |
| 9 | **返回 `Optional.empty()` 表示「这个挂载点没挂脚本」** | 把它和「脚本判定为 false」混成一个 false，「忘了挂」就再也查不出来 |

---

## 1. 顶层设计：为什么是这个形状

### 1.1 三层的职责切分

| 层 | 包 | 知道什么 | 不知道什么 |
|---|---|---|---|
| **契约层** | `spi` | 脚本、上下文、业务域、函数处理器 | 任何引擎实现 |
| **实现层** | `core` | QLExpress 的一切 | 具体业务 |
| **能力层** | `handler` + 各业务模块 | 具体业务 | 引擎怎么跑的 |

`domain` 包是三层共用的数据模型，`annotation` 是能力层写给扫描器看的标记。

这个切分的检验方法很简单：**在 `spi` 包里 grep `qlexpress`，应该零命中。**
现在确实是零。哪天不是了，说明防腐层破了。

### 1.2 为什么函数分类是 enum 而不是 sealed interface

原始设想是用 `sealed interface` 把各业务域封起来。**这条路在本工程编译不过。**

JLS 8.1.6：未命名模块（本工程没有 `module-info.java`）下，`permits` 列出的实现类必须与
接口**同包**。而各域的 Handler 必然散落在 `sa.member` / `sa.mall` / `sa.task`，还跨
`common-api` 和 `sa-marketing` 两个 Maven 模块。

所以拆成两层（[ScriptDomain.java:23](../smart-admin-api/common-api/src/main/java/sa/scriptengine/spi/ScriptDomain.java#L23)、
[ScriptFunctionHandler.java:20](../smart-admin-api/common-api/src/main/java/sa/scriptengine/spi/ScriptFunctionHandler.java#L20)）：

- **域的取值集合**用 `enum` 封闭 —— enum 天生是封闭集合，且能 switch 穷举、能直接当数据库字段值，
  在这个位置比 sealed 更合适
- **Handler 本身**用普通 interface 开放，业务模块随便实现

> **认知点**：Java 的"封闭性"有三档 —— `enum`（值封闭，最强）/ `sealed`（类型封闭，但有同包同模块约束）/
> `interface + 扫描`（开放）。选哪档要看**你想封闭的到底是什么**，以及**实现者住在哪个包**。

### 1.3 为什么 `domain()` 是抽象方法而不是注解

旧版是空标记接口 + 可选的 `@ScriptFunctionGroup` 注解，注解可以不写，不写就落到"默认通用工具"。
这条路的终点是所有函数挤在默认分类里，**而且没有任何人会发现**。

改成抽象方法后，忘了归类 = 编译不过。

> **认知点**：把"应该做"变成"必须做"的最短路径，是把它变成**方法签名的一部分**。
> 加校验、写文档、给注解设默认值，都是更弱的手段。

### 1.4 切面为什么切在接口上

这是重构前一个隐藏了很久的 bug：切点写的是实现类所在包，**一个方法都没拦到**。

因果链：`ScriptEngine` 是通过 `@Bean` 返回**接口类型**装配的
（[ScriptEngineAutoConfiguration.java:45](../smart-admin-api/common-api/src/main/java/sa/config/ScriptEngineAutoConfiguration.java#L45)）
→ Spring 选 JDK 动态代理 → 代理对象只实现接口、不继承 `DefaultScriptEngine`
→ 按实现类写的切点必然落空。

现在切点是 `execution(* sa.scriptengine.spi.ScriptEngine+.evaluate(..))`
（[EngineExecutionMonitorAspect.java:31](../smart-admin-api/common-api/src/main/java/sa/aspect/EngineExecutionMonitorAspect.java#L31)）。

> **认知点**：切面作用于**运行时结构**，不是源码结构。
> Bean 的暴露方式决定代理方式，代理方式决定切点该写在哪一层。
> 同类陷阱还有：自调用不走代理、`final` 方法 CGLIB 拦不住。

⚠️ 顺带记住：`DefaultScriptEngine.evaluate(script, ctx, Class<T>)` 内部调用了两参版
（[DefaultScriptEngine.java:46](../smart-admin-api/common-api/src/main/java/sa/scriptengine/core/DefaultScriptEngine.java#L46)），
这是**自调用，不走代理**。所以三参版只被切一次，不会重复计时 —— 这是期望行为，不是漏切。

### 1.5 `execute` 的重载陷阱

`Express4Runner` 有两个签名相近的重载：

| 重载 | 语义 |
|---|---|
| `execute(String, Map<String,Object>, QLOptions)` | Map 的 key 就是脚本里的变量名 ✅ |
| `execute(String, Object, QLOptions)` | 把对象当"属性根"，脚本按属性访问 ❌ |

重构前传的是 `EngineContext` 对象，命中了第二个重载 —— **`bind()` 进去的变量在脚本里一个都取不到**。
现在显式拷贝成 `HashMap` 再传（[QLExpressEvaluator.java:74](../smart-admin-api/common-api/src/main/java/sa/scriptengine/core/QLExpressEvaluator.java#L74)）。

这条被 `ScriptEngineTest.bound_variables_are_visible_to_script` 钉住了，别再改回去。

### 1.6 上下文注入为什么不用 ThreadLocal

QL 4.x 的 AST 是无状态递归传参的，Java 函数拿上下文走 `QContext.attachment()` 即可
（[QLExpressFunctionAdapter.java:42](../smart-admin-api/common-api/src/main/java/sa/scriptengine/core/QLExpressFunctionAdapter.java#L42)）。

识别规则：**方法首参声明为 `EngineContext` 就自动注入**，脚本侧完全不感知这个参数。

```java
@ScriptFunction(name = "currentLevel", description = "取当前会员等级")
public Integer currentLevel(EngineContext ctx) {          // ← 自动注入
    Long memberId = ctx.getAs("memberId", Long.class);
    ...
}
// 脚本里写：member_currentLevel()      ← 不传参
```

> **认知点**：为什么不发明一个 `@Context` 注解？因为 `EngineContext` 这个**类型本身**
> 在参数位置上已经是无歧义的信号，加注解纯属冗余。
> 反之，如果要注入两个都是 `String` 的东西，类型不够用了，那才轮到注解。

### 1.7 脚本模型上为什么是「来源」而不是「要不要缓存」

QL 的编译缓存以**脚本原文**为 key，且**没有容量上限**。危险不在"缓存会变多大"，而在
**key 的基数是否有界**：

| 脚本来源 | key 基数 | QL 该不该缓存 |
|---|---|---|
| `TRUSTED` —— 项目内 git 管理的 `.ql` 文件 | 有界（几十上百条） | 该，收益极高 |
| `UNTRUSTED` —— 在线试跑 / 动态拼装 | **无界** | 绝对不能，进去就出不来 |

关键在于**这两列分属不同的抽象层**：

- 左列是**业务事实** —— "这段脚本是不是我们自己 git 管理的"，任何引擎下都成立
- 右列是**实现决策** —— "编译缓存"是 QLExpress 独有的东西，换 Aviator 未必有

所以 [`ExecutableScript`](../smart-admin-api/common-api/src/main/java/sa/scriptengine/domain/ExecutableScript.java)
上只放 [`ScriptSource`](../smart-admin-api/common-api/src/main/java/sa/scriptengine/domain/ScriptSource.java)，
从来源推导出缓存决策的那一行住在
[QLExpressEvaluator.isCacheable()](../smart-admin-api/common-api/src/main/java/sa/scriptengine/core/QLExpressEvaluator.java#L150)。

> **认知点一**：**抽象泄漏最常见的形态不是类型穿透，而是概念穿透** —— 编译器不报警，所以更难发现。
> 自查方法：把模型上每个字段拿出来问一句「换掉底层实现之后，这个字段还成立吗？」
> 不成立的，就是漏上来的实现细节，该往下沉。
>
> **认知点二**：看到任何 map-as-cache，第一个问题永远是**"key 的基数上界是多少"**。
> 有界 → 随便缓存；无界 → 必须有淘汰策略或开关。这比"缓存要设上限"通用得多。

⚠️ 没有提供 `of(...)` 这样的短名默认工厂，必须显式写 `trusted()` / `untrusted()`。
**留一个短名默认值，正是不受控内容混进受控通道的典型路径** —— 人总会挑最短的那个名字写。

### 1.8 EngineContext 的两条通道

`EngineContext` 内部是**两个物理隔离的 Map**：

| | 脚本变量通道 | 内部数据通道 |
|---|---|---|
| 写 | `bind(k, v)` / `bindAll(map)` | `bindInternal(k, v)` |
| 读 | `getAs(k, Type)` | `getInternal(k, Type)` |
| **脚本能读到吗** | **能**，按变量名直接引用 | **不能**，完全不可见 |
| Java 函数能读到吗 | 能 | 能 |

**为什么要拆**：脚本是运营在写的，脚本变量是给他们的输入参数。
但 traceId、操作人、幂等键、活动快照这类东西 Java 函数需要、**却不该出现在运营的视野里** ——
暴露出去等于把内部实现细节变成事实契约，以后想改都改不动。拆开之前只能全给或全不给。

实现上是两个独立 Map，不是一个 Map 加 key 前缀约定
（[StandardEngineContext.java](../smart-admin-api/common-api/src/main/java/sa/scriptengine/spi/StandardEngineContext.java)）——
**前缀约定只要有人拼错就漏了，物理隔离漏不了。**

🔴 `getScriptVariables()` 是**脚本可见性的唯一出口**，只有从这里流出去的东西脚本才读得到。
改 `QLExpressEvaluator` 时千万别把整个 context 喂给引擎。

> **认知点**：一个对象同时服务两类读者、而两类读者的可见范围不同时，
> 别用命名约定或文档来区分，**用类型或结构把它们物理分开**。
> 约定要靠人守，结构不用守。

### 1.9 异常的三分法

旧版把一切收敛成 `BusinessException("脚本执行异常，请检查语法或联系管理员！")` —— 语义全丢了。
现在分三类（[QLExpressEvaluator.java:70](../smart-admin-api/common-api/src/main/java/sa/scriptengine/core/QLExpressEvaluator.java#L70)）：

| 情况 | 处理 | 调用方看到 |
|---|---|---|
| 脚本语法 / 运行错误 | 转 `EngineScriptException`，带行列号 | 切面拼成 `msg + 详细原因: 第 2 行…` |
| **Java 函数抛的业务异常** | 从 `QLRuntimeException.getCatchObj()` **拆出来原样抛** | 原始的「积分不足」等业务语义 |
| 超时 | 转 `EngineScriptException("脚本执行超时")` | 明确的超时提示 |

> **认知点**：**异常收敛 ≠ 异常统一**。防腐层要挡的是底层**类型**泄漏（`QLException` 不能露出去），
> 不是把语义差异一起抹平。判据只有一条：**调用方需不需要根据这个差异做不同的事？**
> 需要 → 必须区分。

### 1.10 fail-fast 的位置选择

启动期抛异常的地方有四处，全部是**故意**的：

| 检查 | 位置 |
|---|---|
| `domain()` 返回 null | [EngineFunctionScanner.java:59](../smart-admin-api/common-api/src/main/java/sa/scriptengine/core/EngineFunctionScanner.java#L59) |
| 函数非 public | 同上，`buildMeta` |
| 自己写了域前缀 | 同上 |
| 函数重名 | [EngineFunctionRegistry.java:29](../smart-admin-api/common-api/src/main/java/sa/scriptengine/core/EngineFunctionRegistry.java#L29) |

为什么不打 WARN 继续跑？因为函数没绑上的后果是**运行期报「未知函数」** ——
那时候你面对的是一条线上告警，不是一次启动失败。

> **认知点**：错误发现成本随阶段大致指数上升：
> **编译期 < 启动期 < 首次执行 < 生产偶发**，每往后一档涨约一个数量级。
> 设计任何注册/装配机制时都该主动问：这个错误**最早**能在哪一层被发现？

### 1.11 场景契约：让「这脚本挂这儿合不合法」有答案

在 `ScriptScene` 出现之前，「这段脚本挂在这个槽位上对不对」只能等它真跑起来才知道，
而且答案通常是一个 NPE 或者一个**静默的 null**。

[ScriptScene](../smart-admin-api/common-api/src/main/java/sa/scriptengine/spi/ScriptScene.java)
把一个槽位的契约写死：**会拿到哪些变量（名字/类型/必不必填）、必须返回什么**。
目前三个场景，正好对应三个真实切入点：

| 场景 | 域 | 挂在哪 | 返回 |
|---|---|---|---|
| `TASK_RULE` | TASK | `t_task_template` | `Boolean` |
| `POOL_ENTRY` | DRAW | `t_prize_pool_config` | `Boolean` |
| `ACTIVITY_RULE` | ACTIVITY | `t_activity_config` | `Boolean` |

🔴 **变量清单必须来自调用点真实存在的数据。**三个场景的变量分别抄自
`TaskEventContext`、`DrawExecuteForm` 和活动准入入口 —— 照想象写出来的契约比没有契约更糟，
因为它会让人以为已经校验过了。

业务调用走带场景的重载：

```java
Boolean allowed = scriptEngine.evaluate(
        ScriptScene.POOL_ENTRY, script, ctx, Boolean.class);
```

它比裸 `evaluate` 多做三件事：

1. **执行前**校验必填变量 —— 报的是"缺少必填变量 [activityCode]"，不是脚本里的空指针
2. **执行后**校验返回值类型，且 **null 一律算违约**（下面这条很重要）
3. **交叉校验**调用方写的 `Boolean.class` 与场景声明是否一致 —— 挡住"几个月后改了场景返回类型、调用方没跟上"

方法名沿用 `evaluate` 不是随意的：切面的切点是 `evaluate(..)`，换个名字就不被计时和异常兜底覆盖。

#### ⚠️ 一个必须记住的 QL 语义（我在实现时判断错过一次）

**QLExpress 4.x 里，最后一个表达式的值就是返回值。**所以 `memberId > 0;` 不写 `return`
照样返回 `true` —— 「漏写 return」在 QL 下**不会**产生 null。

真正产生 null 的是**分支没覆盖全**：

```javascript
if (memberId < 0) {
    return true;
}
// memberId >= 0 时走到这里，整段脚本没有任何返回值 → null
```

这比漏写 return 隐蔽得多：调试用的账号刚好走了有 `return` 的那条分支，一切正常；
上线换个人就静默变成"判定不通过"，不报错、不告警，只是活动少发一批奖。
场景契约的 `validateOutput` 就是为了把这种故障从"没人发现"变成"立刻抛异常"。

> **认知点**：给一个扩展点定契约时，**入参和返回值要分开想**。
> 入参校验防的是"调用方少给了东西"，返回值校验防的是"脚本没给出东西"——
> 后者往往被忽略，而它恰恰是最难发现的那类故障：**没有异常、没有日志，只有一个说得通的错误结果。**

#### 编辑器侧

`GET /script/engine/scene/view` 吐出所有场景及其变量清单，前端据此补全**变量名**。
在此之前编辑器只知道函数、不知道变量，变量名全靠运营记。


### 1.12 存储：为什么脚本在文件里，而 `t_script` 只是镜像

三条决策，已确认，不要在没有新信息的情况下推翻：

| | 文件为权威（当前） | DB 可编辑 |
|---|---|---|
| 语法错误何时暴露 | **应用启动就失败**，坏脚本进不了生产 | 运营点保存那一刻，或更晚 |
| 能不能 review / 回滚 | 能，走 PR / `git revert` | 不能 / 得自己做版本表 |
| 改了谁知道 | `git blame` | 猜 |
| 运营能自助改吗 | **不能**，要发版 | 能 |

最后一行是代价，前三行是收益。第一行是这套方案<b>唯一不可替代</b>的地方 ——
`ScriptFileLoader` 在启动期逐个 `check()`，一个脚本语法错，整个应用拒绝启动。

**两张表各自回答一个问题：**

- `t_script` —— 「现在有哪些脚本、各自什么契约」。只由加载器写，
  所以它<b>没有 create_by / update_by 两列</b>：不存在「人写这张表」这回事，
  列的缺席本身就是文档。
- `t_script_ref` —— 🌟 **「改这个脚本会影响哪些业务对象」**。整张表就是为这一个问题存在的，
  设计取舍时以能不能干净地回答它为准。

`params_schema` 由 `ScriptScene.getParams()` 生成而不是人工维护 —— 同一份契约存两处必然漂移，
一处生成不会。

### 1.13 挂载点：把「挂在哪」和「是什么脚本」焊死

`t_script_ref` 有 `ref_type` 和 `ref_slot` 两列，代码侧却合成<b>一个</b>枚举
[ScriptRefPoint](../smart-admin-api/common-api/src/main/java/sa/scriptengine/spi/ScriptRefPoint.java)：

```java
TASK_TEMPLATE_RULE ("TASK_TEMPLATE", "RULE",  ScriptScene.TASK_RULE,     "任务模板", "完成判定规则")
PRIZE_POOL_ENTRY   ("PRIZE_POOL",    "ENTRY", ScriptScene.POOL_ENTRY,    "奖池",    "准入判定")
ACTIVITY_ENTRY     ("ACTIVITY",      "ENTRY", ScriptScene.ACTIVITY_RULE, "活动",    "准入判定")
```

拆成两个枚举等于允许写出 `(ACTIVITY, RULE)` 这种不存在的挂载点 —— 这两列从来不自由组合。

**每个挂载点声明它期望的场景**，这是这个枚举最重要的作用：挂载时校验
「脚本的场景 == 挂载点期望的场景」，于是「把任务判定脚本挂到奖池准入上」
在<b>写入 `t_script_ref` 的那一刻</b>就被拒绝，而不是等某天真有人来抽奖时才炸。

> **认知点**：两个字段如果只有固定几种搭配有意义，就别让它们在类型上自由组合。
> 把合法组合<b>枚举出来</b>，非法状态就变得不可表示 —— 这比事后加校验强一个数量级。

### 1.14 为什么加载器和函数扫描器之间没有先后依赖

`ScriptFileLoader` 和 `EngineFunctionScanner` 都是 `SmartInitializingSingleton`，
而它们之间的回调顺序 Spring <b>不保证</b>。

那脚本里写了 `tool_isBlank(...)`，校验时函数还没绑上会不会挂？

**不会** —— `ScriptEngine.check()` 只做 ANTLR 语法解析，不解析函数是否存在。
这条被 `ScriptEngineTest.check_validates_syntax_only_not_function_existence` 钉住了。

> **认知点**：发现「我的代码依赖某个不保证的顺序」时，有两条路 ——
> 强行制造顺序，或者<b>证明自己不需要顺序</b>。后者更稳，因为前者靠约定维持，
> 而约定会在某次重构里被人无意间打破，且不报错。
> 但「不需要顺序」这个结论必须用测试钉住，否则它只是今天碰巧成立。

---

## 2. 文件地图

```
common-api/src/main/java/sa/
├── scriptengine/
│   ├── annotation/
│   │   └── ScriptFunction.java          方法级：暴露给脚本，写域内短名
│   ├── spi/                             ★ 契约层，零 qlexpress 依赖
│   │   ├── ScriptDomain.java            业务域 enum（10 个），含 qualify() 前缀生成
│   │   ├── ScriptFunctionHandler.java   能力提供方契约，domain() 是抽象方法
│   │   ├── EngineContext.java           上下文契约，脚本可见 / 内部数据两条通道
│   │   ├── StandardEngineContext.java   默认实现，两条通道两个独立 Map
│   │   ├── ScriptEngine.java            业务方唯一入口（门面）
│   │   ├── ScriptEvaluator.java         引擎厂商 SPI（防腐层边界）
│   │   └── ScriptScene.java             场景契约：槽位要什么变量、必须返回什么
│   ├── core/                            ★ 实现层
│   │   ├── QLExpressEvaluator.java      QL 实现，唯一认识 qlexpress 的地方
│   │   ├── QLExpressFunctionAdapter.java Java 方法 → QL CustomFunction 的桥
│   │   ├── DefaultScriptEngine.java     门面实现
│   │   ├── EngineFunctionScanner.java   启动期扫描 + 绑定
│   │   ├── EngineFunctionRegistry.java  函数中央注册表 + 文档导出
│   │   └── ScriptEngineProperties.java  script.* 配置
│   ├── domain/                          共用模型
│   │   ├── ExecutableScript.java        record，带 ScriptSource，无默认工厂
│   │   ├── ScriptSource.java            TRUSTED / UNTRUSTED，业务事实非实现细节
│   │   ├── ScriptParam.java             场景的单个入参声明
│   │   ├── ScriptSceneDocDTO.java       吐给编辑器的场景文档（变量补全用）
│   │   ├── EngineFunctionMeta.java      函数元数据（运行 + 文档双用）
│   │   ├── ScriptFunctionDocDTO.java    吐给前端 Monaco 的文档
│   │   └── ScriptCheckResultVO.java     校验结果（合法/不合法都是正常返回）
│   ├── handler/                         内置能力
│   │   ├── ToolStringHandler.java       tool_isBlank / tool_substring
│   │   └── ToolDateTimeHandler.java     tool_now / tool_today / tool_daysBetween
│   ├── loader/                          ★ 启动期加载（链路 B）
│   │   ├── ScriptFileParser.java        文件头解析，纯静态函数、可单测
│   │   ├── ScriptFile.java              解析结果
│   │   └── ScriptFileLoader.java        扫描 → 语法校验 → 入库 → 常驻内存
│   ├── runtime/
│   │   └── ScriptRuntime.java           ★ 业务侧执行脚本的唯一入口
│   ├── service/
│   │   ├── ScriptRefService.java        挂载 / 摘除 / 「改这个脚本影响谁」
│   │   └── ScriptQueryService.java      脚本只读查询
│   ├── dao/ + manager/                  t_script、t_script_ref 各一套
│   ├── domain/entity/                   Script、ScriptRef
│   └── controller/
│       ├── ScriptEngineController.java  /view /scene/view /check /online/test
│       ├── ScriptAdminController.java   /script/list /detail /refs /ref/bind /ref/unbind
│       └── form/ScriptTestForm.java
├── config/ScriptEngineAutoConfiguration.java   引擎插槽 + 门面装配
└── aspect/EngineExecutionMonitorAspect.java    计时 / 慢日志 / 异常分流

common-api/src/main/resources/scripts/   ★ 脚本内容的唯一权威（git 管理）
├── README.md                            格式、目录规则、两个常见坑
├── task/streak_sign_7d.ql
├── draw/vip_pool_entry.ql
└── activity/basic_entry.ql

common-api/src/test/java/sa/scriptengine/
├── ScriptEngineTest.java                26 个引擎契约测试，不依赖 Spring
├── ScriptFileParserTest.java            10 个文件头解析测试
└── ScriptRefPointTest.java              4 个挂载点契约测试

sa-admin/src/test/java/sa/admin/script/
└── ScriptRuntimeAcceptanceTest.java     8 个端到端验收（对真实库跑）

smart-admin-web/src/
├── api/support/script-api.js                        脚本管理接口（无编辑接口）
├── views/support/script/script-list.vue             ★ 脚本管理页（系统配置 → 脚本管理）
└── views/support/engine-doc/index.vue               函数文档页（按域分组 + 检索）
```

配置在四个环境的 `sa-base/src/main/resources/{dev,test,pre,prod}/sa-base.yaml` 的 `script:` 段，
**四份内容一致**，默认值就是安全值。

---

## 3. 扩展指南

### 3.1 给某个业务域加一个脚本函数

三步，没有第四步：

```java
// 1. 在业务模块里建 Handler（一个域可以有多个 Handler，按子领域拆）
@Component
@RequiredArgsConstructor
public class MemberLevelScriptHandler implements ScriptFunctionHandler {

    private final MemberService memberService;

    @Override
    public ScriptDomain domain() {
        return ScriptDomain.MEMBER;          // 2. 声明域 —— 决定前缀和文档分组
    }

    // 3. 标注函数，写域内短名，不要自己加 member_ 前缀
    @ScriptFunction(name = "getLevel", description = "查会员当前等级，不存在返回 0")
    public Integer getLevel(Long memberId) {
        return memberService.getLevel(memberId);
    }
}
```

脚本里调用：`member_getLevel(memberId)`。重启后自动出现在 `/script/engine/view` 里。

需要拿执行上下文就把首参声明为 `EngineContext`（见 §1.6），注意读的时候分清两条通道：

```java
@ScriptFunction(name = "deductPoint", description = "扣减积分，不足时抛业务异常")
public Boolean deductPoint(EngineContext ctx, Long amount) {
    Long memberId = ctx.getAs("memberId", Long.class);          // 脚本变量：运营写脚本时看得见
    String traceId = ctx.getInternal("traceId", String.class);  // 内部数据：运营完全不感知
    ...
}
```

调用方组装上下文时同理：

```java
EngineContext ctx = EngineContext.create()
        .bind("memberId", memberId)          // 脚本可见，运营会在脚本里引用
        .bind("orderAmount", amount)
        .bindInternal("traceId", traceId)    // 脚本不可见
        .bindInternal("operator", operator);

scriptEngine.evaluate(ExecutableScript.trusted("task/sign_streak_7", content), ctx);
```

**🔴 写 Handler 时必须知道的两件事：**

1. **方法里的耗时操作必须自己做超时控制。**`script.timeout-millis` 是"假超时" ——
   它只能中断脚本自身的指令循环，**无法强行中断已经进入 Java 的 HTTP / DB 调用**。
   脚本会无视超时配置一直阻塞在你的方法里。
2. **方法抛的 `BusinessException` 会原样冒到调用方**（§1.9），所以异常信息要写成运营看得懂的话。

### 3.2 写一个新脚本并挂到业务对象上

**① 建文件**（路径决定 `script_code`，目录必须等于场景所属域）：

```
common-api/src/main/resources/scripts/draw/newbie_pool_entry.ql
```

```javascript
/**
 * @name  新人奖池准入
 * @scene POOL_ENTRY
 * @desc  只放行注册 7 天内的会员。
 */
return poolCode == "NEWBIE_POOL" && memberId > 0;
```

**② 启动应用。**格式错、场景不存在、目录放错、语法错 —— 都会直接启动失败并指出是哪个文件。
起来之后它就已经在 `t_script` 里了。

**③ 挂到业务对象上**：

```bash
POST /script/ref/bind
{ "refPoint": "PRIZE_POOL_ENTRY", "refId": "NEWBIE_POOL", "scriptCode": "draw/newbie_pool_entry" }
```

挂错场景会被拒绝（见 §1.13）。

**④ 业务代码里调用**：

```java
Optional<Boolean> allowed = scriptRuntime.evaluate(
        ScriptRefPoint.PRIZE_POOL_ENTRY, poolCode,
        EngineContext.create()
                .bind("memberId", memberId)
                .bind("activityCode", activityCode)
                .bind("poolCode", poolCode)
                .bindInternal("traceId", traceId),
        Boolean.class);

// empty = 这个奖池没挂准入脚本。默认行为由你决定，不要替调用方决定
if (allowed.isPresent() && !allowed.get()) {
    return 拒绝;
}
```

**⑤ 想知道改这个脚本会影响谁**：`GET /script/refs?scriptCode=draw/newbie_pool_entry`

### 3.3 加一个新的脚本槽位（场景）

在 `ScriptScene` 里加一个枚举常量，变量清单**必须回到调用点抄真实数据**：

```java
LOTTERY_TICKET_ISSUE(ScriptDomain.LOTTERY, "发号准入", Boolean.class,
        "返回 true 表示允许该会员本次领号。",
        List.of(
                ScriptParam.required("memberId", Long.class, "会员号（关联键）"),
                ScriptParam.required("lotteryCode", String.class, "彩票编码"),
                ScriptParam.required("issueNo", String.class, "期号")
        )),
```

加完之后自动生效：`t_script.scene` 多一个合法取值、`/script/engine/scene/view` 多一条、
调用方可以用 `evaluate(ScriptScene.LOTTERY_TICKET_ISSUE, ...)` 拿到入参与返回值校验。

### 3.4 加一个新的业务域

在 `ScriptDomain` 里加一个枚举常量，完事。这是唯一的登记处。

### 3.5 换掉 QLExpress

1. 实现 `ScriptEvaluator`（5 个方法）
2. 在 `ScriptEngineAutoConfiguration` 加一个带 `@ConditionalOnProperty` 的 `@Bean`
   （文件里已经留了 Aviator 的注释模板）
3. 改 yaml 的 `script.engine`

上层业务代码**一行都不用动** —— 这是 SPI 存在的唯一理由，也是为什么 §0 事实 3 是条红线。

---

## 4. 红线

| # | 红线 | 理由 |
|---|---|---|
| 1 | 任何环境不得开 `script.allow-java-reflect: true` | 等于把 RCE 面暴露给脚本内容，而脚本是运营写的 |
| 2 | 不受控脚本必须走 `ExecutableScript.untrusted()` | 编译缓存 key 无界 → OOM（§1.7） |
| 3 | `spi` 包不得出现 `com.alibaba.qlexpress4.*` | 防腐层破了就没有换引擎的退路 |
| 4 | 不得复活 ThreadLocal 传上下文 | 3.x 时代在 Tomcat 下漏内存的老路，4.x 有 attachments（§1.6） |
| 5 | 不得绕过 `EngineFunctionScanner` 手动 `addFunction` | 函数会从文档里消失，前端补全缺块 |
| 6 | 执行路径上不得调用 `check()` | 每次执行多一遍解析，且会撑大编译缓存 |
| 7 | 内部数据一律走 `bindInternal`，不得 `bind` | `bind` 的东西脚本读得到；traceId/幂等键暴露给运营 = 内部细节变成事实契约（§1.8） |
| 8 | 业务调用必须走带 `ScriptScene` 的 `evaluate` 重载 | 裸调用没有入参/返回值校验，脚本无返回值时静默当「判定不通过」（§1.11） |
| 9 | 场景的变量清单必须抄自调用点真实数据 | 照想象写的契约比没有契约更糟 —— 它让人以为已经校验过了（§1.11） |
| 10 | 不得手工改 `t_script` 的内容 | 它是文件的镜像，改了不生效、下次启动被覆盖（§1.12） |
| 11 | 改表结构后必须重新导出基线 | 本项目没有迁移文件，基线就是唯一真相（`数据库SQL脚本/mysql/README.md`）|

---

## 5. 排查手册

| 症状 | 先看哪里 |
|---|---|
| 应用起不来，日志有「脚本函数名冲突」 | 同一个域里两个 Handler 写了同一个短名，改掉其中一个 |
| 脚本报「未知函数」 | 忘了写域前缀；或 Handler 没加 `@Component`；或方法不是 public |
| 脚本取不到变量 | ① 是不是误用了 `bindInternal`（那条通道脚本看不见）；② 确认 key 与脚本里的变量名完全一致 |
| 脚本改了不生效 | 该脚本是 `trusted()`（QL 开了编译缓存），调 `ScriptEngine.clearCompileCache()` |
| 函数里抛的异常前端看到的是「未知异常」 | 抛的不是 `BusinessException` 子类，穿透逻辑只认它（§1.9） |
| 慢日志刷屏 | 调 `script.slow-log-millis`；默认 200ms |
| 报「缺少必填变量 [xxx]」 | 调用方漏了 `bind`，或误绑到了 `bindInternal`。变量清单见 `ScriptScene` 或 `/script/engine/scene/view` |
| 报「脚本 [x] 没有返回值」 | 脚本有分支没写 return（QL 下漏写 return 不会 null，分支没覆盖才会）。见 §1.11 |
| 报「与场景契约不一致」 | 场景的返回类型改过了，调用方那行 `Xxx.class` 还是老的 |
| 应用起不来，报「脚本文件不合法」 | 按报错里的文件名去改。格式规则见 `resources/scripts/README.md` |
| 应用起不来，报「脚本语法校验失败」 | 一次会报出全部出错脚本，不用修一个重启一次 |
| `ScriptRuntime.evaluate` 返回 empty | 该挂载点没挂脚本。查 `t_script_ref`，或 `GET /script/refs/owner` |
| 报「挂载的脚本在项目里不存在」 | 脚本文件删了但引用没摘。查 `t_script` 里 status=0 的行 |
| 脚本文件改了但行为没变 | 没重启。文件是启动期加载的，不热更新 |
| 挂载时提示「场景不匹配」 | 这个脚本的场景不被该挂载点接受。管理页的挂载点下拉已按场景过滤，只会列出兼容的 |
| 想确认引擎起来时是什么配置 | 启动日志搜 `[ScriptEngine]`，三行：引擎初始化 / 门面装配 / 函数绑定统计 |

启动日志长这样，能一眼确认安全姿态和绑定结果：

```
[ScriptEngine] QLExpress 引擎初始化完成，安全策略=isolation, 超时=3000ms, 精确小数=true
[ScriptEngine] 门面装配完成，底层实现：QLExpress-4.x
[ScriptEngine] 函数绑定完成，共 5 个：
  TOOL       通用工具     5 个函数
```

---

## 6. 测试钉住了什么

`ScriptEngineTest`（10 个用例，纯 POJO 装配，不起 Spring）钉的是**契约**不是实现 ——
用例名换掉底层引擎之后依然成立：

| 用例 | 钉住的契约 |
|---|---|
| `bound_variables_are_visible_to_script` | bind 的变量脚本取得到（§1.5 那个 bug 的回归） |
| `internal_channel_is_invisible_to_script_but_readable_by_java` | **内部数据脚本读不到、Java 函数读得到**（§1.8） |
| `script_variables_view_excludes_internal_channel` | `getScriptVariables()` 是可见性唯一出口 |
| `two_channels_are_independent_for_the_same_key` | 两条通道同名 key 互不串味 |
| `script_source_must_be_explicit` | 来源必须显式声明，为 null 直接拒绝构造 |
| `both_sources_execute_identically` | 来源只影响引擎内部优化，不影响执行语义 |
| `scene_evaluation_passes_when_contract_is_satisfied` | 场景契约满足时正常返回 |
| `missing_required_variable_fails_before_execution` | 缺必填变量执行前就失败，且点名变量 |
| `required_variable_type_mismatch_is_rejected` | 变量类型严格校验，不做数值宽松转换 |
| `optional_variable_may_be_absent` | 可选变量缺失不影响执行 |
| `qlexpress_returns_the_value_of_the_last_expression` | **QL 语义备忘**：最后一个表达式即返回值 |
| `uncovered_branch_without_return_is_reported_...` | **分支没覆盖导致无返回值时明确报错**（§1.11） |
| `return_type_violating_scene_contract_is_rejected` | 返回值类型必须符合场景契约 |
| `caller_return_type_must_match_scene_contract` | 调用方类型与场景交叉校验 |
| `every_scene_declares_usable_params` | 每个场景都有变量清单和描述（前端补全依赖） |
| `check_validates_syntax_only_not_function_existence` | **check 不验函数存在性** —— 加载器的时序自由度靠它（§1.14） |

`ScriptFileParserTest`（10 个）钉住文件头解析：三个标签缺一不可、场景必须存在、
**目录必须等于场景所属域**、script_code 由路径推导、hash 能检出内容变化。
每条不合法用例都断言报错信息里带得出文件名 —— 启动失败时人要能立刻知道去改哪个文件。

`ScriptRefPointTest`（4 个）钉住挂载点：`(ref_type, ref_slot)` 组合唯一、
每个挂载点都声明期望场景、能从库里的两列反查回枚举。

`ScriptRuntimeAcceptanceTest`（8 个，`@SpringBootTest` 对真实库跑）钉住整条链路：

| 用例 | 钉住的契约 |
|---|---|
| `scripts_are_loaded_from_files_at_startup` | 启动期文件确实进了内存 |
| `unbound_point_returns_empty` | 没挂脚本返回 empty，不是 false |
| `bind_then_evaluate_end_to_end` | 🌟 挂载 → 取出 → 按场景执行，整条链路 |
| `binding_a_script_of_the_wrong_scene_is_rejected` | 🔴 挂错场景在写入那一刻被拒 |
| `binding_a_nonexistent_script_is_rejected` | 挂不存在的脚本被拒 |
| `ref_table_answers_who_is_affected` | 🌟 「改这个脚本会影响谁」查得出来 |
| `rebinding_replaces_instead_of_duplicating` | 重复挂等于替换，不产生两行 |
| `context_violating_scene_contract_is_reported` | 上下文不满足契约时报出缺哪个变量 |
| `script_list_carries_display_fields` | 管理页列表依赖的字段（中文名/引用数）都在，且列表不带内容 |
| `script_detail_carries_content_and_live_ref_count` | 详情带内容，引用数随挂载实时变化 |
| `script_local_variables_do_not_pollute_host_context` | 脚本局部变量不回写宿主 |
| `functions_are_registered_with_domain_prefix` | 域前缀自动生成 |
| `duplicated_function_name_fails_fast` | 重名抛异常不覆盖 |
| `exported_docs_carry_domain_and_signature` | 文档字段契约（前端依赖） |
| `engine_context_is_injected_into_first_parameter` | 上下文自动注入 |
| `business_exception_from_java_function_propagates_as_is` | 业务异常穿透 |
| `isolation_strategy_blocks_arbitrary_java_access` | 隔离策略拦得住 `getClass()` |
| `syntax_error_is_reported_with_line_number` | 语法错误带行号 |
| `return_type_mismatch_is_rejected` | 返回类型校验 |

> **认知点**：测试名如果在换掉底层实现后依然成立，它钉的就是契约；
> 如果写成「evaluate 调用了 execute(String, Map, QLOptions)」，那钉的是实现，换引擎时会全红。

---

## 7. 已知短板与下一轮

### 7.1 内核层面的短板清单

#### 已修复（2026-08-23，动存储之前清掉的三条）

| # | 原短板 | 怎么解决的 |
|---|---|---|
| ~~a~~ | `ExecutableScript.cacheable` 是概念泄漏 | 换成 `ScriptSource`（业务事实），缓存决策下沉到 `QLExpressEvaluator.isCacheable()`。见 §1.7 |
| ~~b~~ | `EngineContext` 承担两个职责 | 拆成脚本可见 / 内部数据两条物理隔离的通道。见 §1.8 |
| ~~c~~ | 缺「场景契约」抽象 | 新增 `ScriptScene` 枚举，三个真实槽位各一条，含入参/返回值校验。见 §1.11 |

#### 待办

| ~~d~~ | 两张表 + 文件加载器缺位 | `t_script` / `t_script_ref` 已建、加载器已落地、存量三处已迁走。见 §1.12 / §1.13 |
| ~~e~~ | 后台脚本管理页缺位 | 已上线：系统配置 → 脚本管理（`/support/script/script-list`），只读 + 挂载。见 §2 |

#### 待办

| # | 短板 | 为什么现在改便宜、以后改贵 |
|---|---|---|
| 1 | **`ScriptRuntime` 每次执行查一次 ref 表** | 唯一索引查询，当前不是瓶颈。真成热点时这里是最该加缓存的地方 —— 但缓存要连着失效一起做，没有证据之前不做 |
| 2 | **没有可观测性出口，只有日志** | 脚本是**运营会频繁改动的生产逻辑**。按 scriptName 维度的执行次数、耗时分位、失败率是刚需 —— 运营改坏了脚本，这是唯一能看出来的地方。切面有拦截点，埋点位置现成 |
| 3 | **两阶段注册无原子性** | scanner 先全量入 registry 再逐个入 evaluator，第二阶段中途失败两边不一致。当前无害（启动直接失败），但将来要支持热加载函数就是真问题 |
| 4 | **内部数据通道仍是字符串 key** | `bindInternal("traceId", ...)` 拼错 key 只会静默拿到 null。等 traceId / 操作人 / 幂等键这组字段稳定下来，应该收敛成一个 typed record，由编译器保证 |

### 7.2 存储方案的实施记录（2026-08-23 完成）

三条决策全部落地：

1. **脚本文件是唯一权威，DB 只读镜像** —— `resources/scripts/**/*.ql` 走 git，
   后台无编辑接口（`ScriptAdminController` 只有查询与挂载）
2. **两张表** —— `t_script` + `t_script_ref`，`ref_slot` 保留（见 §1.13）
3. **存量三处一次性迁走** —— `t_activity_config.script_id`、`t_prize_pool_config.script_id`、
   `t_task_template.rule_script` 三列已 DROP

**迁移时的实际发现**（迁移方案因此比预想简单得多）：

| 列 | 库里的实际数据 |
|---|---|
| `t_activity_config.script_id` | 8 行活动，**全为空** |
| `t_prize_pool_config.script_id` | 3 个奖池，**全为空** |
| `t_task_template.rule_script` | 5 行有内容，但**全是注释**，其中两条是变量模型根本不存在的伪代码草稿 |

所以没有任何真实脚本数据需要迁进 `t_script_ref`，三列直接 DROP 即可。
那 5 条注释在删列前已存档到
[`数据库SQL脚本/mysql/归档-任务模板rule_script删列前内容.md`](../数据库SQL脚本/mysql/归档-任务模板rule_script删列前内容.md)，
不静默丢失。

**库变更按项目约定执行**：直接改库 → `DumpSchema` 重新导出基线 → `git diff` 核对 →
`SyncFromBaseline` 同步分域文件 → `CheckModuleDrift` 验证 PASS。没有迁移文件，基线是唯一真相。

