# 枚举改造 · 存量数据对账报告（dev 库）

> 自动生成，全程只读查询。**dev 库的数据量和真实性都不代表生产** —— 
> 上线前必须拿同目录的 `枚举改造-生产对账.sql` 在生产只读库再跑一遍。

> ## ✅ 改造已于 2026-08-29 全部完成
>
> 本文是**当时的数据快照**，留档用；改造进度看 `枚举改造-阶段3工作清单.md`。
>
> **⚠️ 别把这份报告当成完整清单** —— 它是「按列名筛 + 按常量抓」生成的，
> 结构上看不见两类列，收尾时靠逐个回查实体字段声明类型才翻出来：
>
> 1. **列名不像 status 的字典列**：`t_member.gender`、`t_employee.gender`、
>    `t_mall_commodity.is_home`、`t_mall_address.is_default` —— 一条都没进报告。
> 2. **枚举早就存在、只是字段还写成 Integer 的**：`t_file.status`（`FileStatusEnum`）、
>    `t_member_operation_limit` 三列、`t_member.gender`（`GenderEnum`）等共 6 处。
>    没有裸常量可抓、枚举也在，两种筛法都判它「已经对了」。
>
> 下次做同类普查，**直接读实体字段的声明类型**，别只依赖列名和常量。
>
> 补的 7 列已加进 `枚举改造-生产对账.sql`（现共 87 列），并在 dev 上真跑过一遍
> 确认列名无误。跑出来两件值得记的事：
>
> - **`t_member.gender` 全库 4192 行<b>清一色是 0（未知）</b>** —— 这个字段从来没有被写入过
>   有效值。做 C 端注册/资料页时别默认它有数据。
> - **`t_mall_address` 是零行**，所以 `is_default` 没有任何真实数据覆盖。

## 一、逐列对账

对照基准是**列注释里声明的取值**（`0-未支付, 1-已支付` 这种写法）。
它是独立于 Java 枚举的第二份事实，正好拿来交叉验证。

### `t_activity_config.status`  ✅
- 注释声明：0=未开始, 1=上线, 2=下线
- 实际取值：0 × 7, 1 × 1（共 8 行）

### `t_draw_prize_log.status`  ✅
- 注释声明：0=未中奖, 1=已中奖, 2=库存不足, 3=异常
- 实际取值：1 × 315, 2 × 185（共 500 行）

### `t_lottery_config.status`  ✅
- 注释声明：0=下线, 1=上线
- 实际取值：0 × 2, 1 × 1（共 3 行）

### `t_lottery_issue.status`  ✅
- 注释声明：0=待开奖, 1=部分开奖, 2=已开奖
- 实际取值：0 × 2, 2 × 2（共 4 行）

### `t_lottery_record.dispatch_status`  ✅
- 注释声明：0=待派发/无需派发, 1=已投递, 2=投递失败
- 实际取值：0 × 39, 1 × 1（共 40 行）

### `t_lottery_record.win_status`  ✅
- 注释声明：0=未开奖, 1=未中奖, 2=已中奖
- 实际取值：1 × 39, 2 × 1（共 40 行）

### `t_mall_category.status`  ✅
- 注释声明：0=禁用, 1=启用
- 实际取值：0 × 2, 1 × 2（共 4 行）

### `t_mall_commodity.pay_type`  ✅
- 注释声明：1=纯积分, 2=积分+现金
- 实际取值：1 × 2, 2 × 1（共 3 行）

### `t_mall_commodity.status`  ✅
- 注释声明：0=下架, 1=上架, 2=草稿。新建默认落草稿
- 实际取值：1 × 2, 2 × 1（共 3 行）

### `t_mall_sku.sku_status`  ✅
- 注释声明：0=停用, 1=启用
- 实际取值：1 × 6（共 6 行）

### `t_member.status`  ✅
- 注释声明：1=正常, 2=冻结(风控/违规), 3=已注销
- 实际取值：1 × 3392（共 3392 行）

### `t_member_asset_transaction.transaction_type`  ✅
- 注释声明：1=收入, 2=支出
- 实际取值：1 × 1380（共 1380 行）

### `t_member_coupon.status`  ✅
- 注释声明：0=未使用, 1=已使用, 2=已过期, 3=已作废
- 实际取值：0 × 639（共 639 行）

### `t_member_wallet.status`  ✅
- 注释声明：0=冻结, 1=正常
- 实际取值：1 × 1125（共 1125 行）

### `t_physical_delivery.status`  ✅（原判为脏值，实为漏定义）
- 注释声明：0=待发货, 1=已发货, 2=已签收, 3=异常退回
- 实际取值：-1 × 1（共 1 行）
- 当时判成「注释里没有的值」，**结论错了**：-1 是业务上真实存在的「发货取消」，
  是<b>注释漏写</b>，不是脏数据。已给 `DeliveryStatusEnum` 补上 `CANCELLED(-1, "已取消")`。
- 🔴 教训：注释是「第二份事实」，不是「唯一权威」。注释与数据打架时，
  先问业务，别默认数据是脏的 —— 差一点就把这行当垃圾删了。

### `t_prize_config.approve_mode`  ✅
- 注释声明：0=自动免审, 1=人工审批
- 实际取值：0 × 9, 1 × 10（共 19 行）

### `t_prize_config.status`  ✅
- 注释声明：0=停用, 1=启用
- 实际取值：0 × 1, 1 × 18（共 19 行）

### `t_prize_log.approve_status`  ✅
- 注释声明：0=无需审批, 1=待审批, 2=已批准, 3=已驳回
- 实际取值：0 × 1230, 1 × 66, 3 × 1（共 1297 行）

### `t_prize_log.status`  ✅
- 注释声明：0=等待, 1=成功, 2=失败
- 实际取值：0 × 67, 1 × 1130, 2 × 100（共 1297 行）

### `t_prize_pool_config.draw_mode`  ✅
- 注释声明：1=按概率(probability), 2=按库存比例(stock_ratio)
- 实际取值：1 × 3（共 3 行）

### `t_promotion_config.review_level`  ✅
- 注释声明：0=无需审核, 1=单层审批, 2=双层审批
- 实际取值：0 × 4（共 4 行）

### `t_promotion_config.status`  ✅
- 注释声明：0=停用, 1=启用
- 实际取值：1 × 4（共 4 行）

### `t_proposal_record.status`  ✅
- 注释声明：0=等待中, 10=待一审, 11=待二审, 20=驳回, 30=待执行, 40=执行中, 50=成功, 60=部分成功, 70=彻底失败, 80=风控拦截
- 实际取值：50 × 1030, 80 × 100（共 1130 行）

### `t_script.status`  ✅
- 注释声明：0=停用, 1=启用
- 实际取值：1 × 3（共 3 行）

### `t_solvela_job_log.status`  ✅
- 注释声明：0=待执行, 1=执行中, 2=成功, 3=失败, 4=超时中断, 5=阻塞丢弃, 6=错过调度, 7=中断
- 实际取值：2 × 488, 3 × 5, 4 × 1, 5 × 2, 6 × 66, 7 × 1（共 563 行）

### `t_task_config.status`  ✅
- 注释声明：1=待生效, 2=生效中, 3=已下线
- 实际取值：1 × 11（共 11 行）

### `t_task_event.discard_log_flag`  ✅
- 注释声明：1=记录, 0=不记录（高频事件建议关）
- 实际取值：0 × 1, 1 × 8（共 9 行）

### `t_task_event.status`  ✅
- 注释声明：0=停用, 1=启用
- 实际取值：1 × 9（共 9 行）

### `t_task_record.status`  ✅
- 注释声明：0=进行中, 1=已完成, 2=已发奖, 3=已过期
- 实际取值：0 × 2257, 2 × 840（共 3097 行）

### `t_task_record_flow.flow_type`  ✅
- 注释声明：1=进度推进(已生效), 2=事件丢弃(未生效)
- 实际取值：1 × 4693, 2 × 676（共 5369 行）

### `t_task_template.status`  ✅
- 注释声明：0=禁用, 1=启用
- 实际取值：1 × 5（共 5 行）

## 二、结论

- 候选整型状态列：**80** 个（已排除 `t_menu_26081523` 这类手工备份表）
- 有数据、且注释里写了取值、可自动对账的：**31** 个
- 其中与注释对不上的：**1** 个 —— `t_physical_delivery.status` 的 -1，
  事后确认是注释漏写而非脏值，已补进枚举，见上文。**全库无脏值。**

### 空表：没有数据，对不了账

- `t_login_fail.lock_flag`
- `t_login_fail.user_type`
- `t_mall_order.status`
- `t_member_login_log.status`
- `t_member_operation_limit.operation_type`
- `t_member_operation_limit.status`
- `t_member_operation_limit.unlock_type`
- `t_member_verify.verify_status`
- `t_password_log.user_type`
- `t_script_ref.status`

这些列在 dev 上是零行，对账时**风险完全没有被覆盖**。

**✅ 10 列现已全部枚举化**（靠 DDL 注释而非数据推导取值）。覆盖情况分两档：

- `t_member_operation_limit` 的 3 列**已有真实验证**：
  `OperationLimitEnumMappingTest` 用 `@Transactional + @Rollback` 自己插一行再读回来，
  顺带验到了「枚举当 `@Param` 传进手写 `@Select`/`@Update`」这条路径。
- 其余 7 列只有编译期 + DDL 注释两重保障，用例里只断言「查询链路不炸」。
  **上线前仍应拿生产数据再对一次**，尤其 `t_mall_order.status`（订单模块服务端零实现）。

### 注释没写取值，只能人工确认的列

- `t_data_tracer.type`（注释：单据类型）
- `t_data_tracer.user_type`（注释：用户类型：1 后管用户 ）
- `t_dict.disabled_flag`（注释：禁用状态）
- `t_dict_data.disabled_flag`（注释：禁用状态）
- `t_employee.administrator_flag`（注释：是否为超级管理员: 0 不是，1是）
- `t_employee.deleted_flag`（注释：是否删除0否 1是）
- `t_employee.disabled_flag`（注释：是否被禁用 0否1是）
- `t_file.category_id`（注释：分类）
- `t_file.deleted_flag`（注释：删除标记）
- `t_file.status`（注释：1临时 2已确认）
- `t_file_category.category_id`（注释：空）
- `t_login_log.login_result`（注释：登录结果：0成功 1失败 2 退出）
- `t_login_log.user_type`（注释：用户类型）
- `t_lottery_prize_rule.prize_level`（注释：奖品奖级）
- `t_mail_template.disable_flag`（注释：是否禁用）
- `t_mall_commodity.category_id`（注释：分类id）
- `t_menu.cache_flag`（注释：是否缓存）
- `t_menu.deleted_flag`（注释：删除状态）
- `t_menu.disabled_flag`（注释：禁用状态）
- `t_menu.frame_flag`（注释：是否为外链）
- `t_menu.menu_type`（注释：类型）
- `t_menu.perms_type`（注释：权限类型）
- `t_menu.visible_flag`（注释：显示状态）
- `t_operate_log.operate_user_type`（注释：用户类型）
- `t_operate_log.success_flag`（注释：请求结果 0失败 1成功）
- `t_position.deleted_flag`（注释：空）
- `t_prize_config.prize_level`（注释：奖品级别）
- `t_prize_log.prize_level`（注释：奖品级别）
- `t_prize_pool_config.status`（注释：0关闭，1开启）
- `t_role_data_scope.data_scope_type`（注释：数据范围类型）
- `t_role_data_scope.view_type`（注释：数据可见范围类型）
- `t_solvela_job.deleted_flag`（注释：删除状态）
- `t_solvela_job.enabled_flag`（注释：是否开启）
- `t_solvela_job.handler_missing_flag`（注释：执行器在代码中不存在：该任务不会被执行，列表需标红）
- `t_solvela_job.manual_modified_flag`（注释：衍生任务是否被人工改过：改过的向导不再覆盖（第三档用））
- `t_solvela_job.terminal_flag`（注释：ONE_TIME 任务执行完置 1，列表默认折叠）
- `t_table_column.user_type`（注释：用户类型）
- `t_task_prize_mapping.stage_level`（注释：任务阶段：单次任务填1，阶梯任务填 1, 2, 3...）

### 不是枚举：注释里写的是数值区间

- `t_lottery_record.prize_level`（注释：奖励等级：1..N 为中奖奖级(数字越小奖越大)，99-未中奖/未开奖）

这些列的取值是**开放的数值等级**，不是有限集合，不该改成枚举，保持 `Integer`。

## 三、实现了 BaseEnum 的枚举清单

> 下面两张表由脚本从源码重新生成（2026-08-29 收尾时），手写维护必然漂移。

### 整型值枚举（本次改造范围）

| 枚举 | 取值 | javadoc 里的表列提示 |
|---|---|---|
| `ActivityStatusEnum` | 0=未开始, 1=上线, 2=下线 | `t_activity_config.status` |
| `ApproveModeEnum` | 0=自动免审, 1=人工审批 | `t_prize_config.approve_mode` |
| `CouponStatusEnum` | 0=未使用, 1=已使用, 2=已过期, 3=已作废 | `t_member_coupon.status` |
| `DataScopeWhereInTypeEnum` | 0=以员工IN, 1=以部门IN, 2=自定义策略 | - |
| `DataTracerTypeEnum` | 1=商品, 2=OA-通知公告, 3=OA-企业信息, 10=定时任务 | - |
| `DeliveryStatusEnum` | -1=已取消, 0=待发货, 1=已发货, 2=已签收, 3=异常退回 | `t_physical_delivery.status` |
| `DrawModeEnum` | 1=按概率, 2=按库存比例 | `t_prize_pool_config.draw_mode` |
| `DrawResultEnum` | 0=未中奖, 1=已中奖, 2=库存不足, 3=异常 | `t_draw_prize_log.status` |
| `EnableStatusEnum` | 0=停用, 1=启用 | `t_prize_config.status` |
| `FileStatusEnum` | 1=临时, 2=已确认 | - |
| `GenderEnum` | 0=未知, 1=男, 2=女 | - |
| `IssueStatusEnum` | 0=待开奖, 1=核销中, 2=已开奖 | - |
| `LoginDeviceEnum` | 1=电脑端, 2=安卓, 3=苹果, 4=H5, 5=微信小程序 | - |
| `LoginLogResultEnum` | 0=登录成功, 1=登录失败, 2=退出登录 | `t_login_log.login_result` |
| `LotteryConfigStatusEnum` | 0=下线, 1=上线 | `t_lottery_config.status` |
| `LotteryDispatchStatusEnum` | 0=待派发/无需派发, 1=已投递, 2=投递失败 | `t_lottery_record.dispatch_status` |
| `MallCommodityStatusEnum` | 0=下架, 1=上架, 2=草稿 | `t_mall_commodity.status` |
| `MallOrderStatusEnum` | 0=待支付, 10=待履约, 20=履约中, 30=已完成, 40=已取消, 50=已退款, 60=履约失败 | `t_mall_order.status` |
| `MallPayTypeEnum` | 1=纯积分, 2=积分+现金 | `t_mall_commodity.pay_type` |
| `MemberOperationLimitStatusEnum` | 0=冻结中, 1=已解冻 | `t_member_operation_limit.status` |
| `MemberOperationTypeEnum` | 1=登录, 2=修改密码 | `t_member_operation_limit.operation_type` |
| `MemberOperationUnlockTypeEnum` | 1=自动到期, 2=重置密码, 3=人工解冻 | `t_member_operation_limit.unlock_type` |
| `MemberStatusEnum` | 1=正常, 2=冻结, 3=已注销 | `t_member.status` |
| `MemberVerifyStatusEnum` | 0=未认证, 1=认证中, 2=已认证, 3=认证失败 | `t_member_verify.verify_status` |
| `MenuPermsTypeEnum` | 1=Sa-Token模式 | - |
| `MenuTypeEnum` | 1=目录, 2=菜单, 3=功能点 | - |
| `PrizeApproveStatusEnum` | 0=无需审批, 1=待审批, 2=已批准, 3=已驳回 | `t_prize_log.approve_status` |
| `PrizeDispatchStatusEnum` | 0=等待, 1=成功, 2=失败 | `t_prize_log.status` |
| `PrizePoolStatusEnum` | 0=关闭, 1=开启 | `t_prize_pool_config.status` |
| `ProposalStatusEnum` | 0=等待中, 10=待一审, 11=待二审, 20=驳回, 30=待执行, 40=执行中, 50=成功, 60=部分成功, 70=彻底失败, 80=风控拦截 | `t_proposal_record.status` |
| `ReviewLevelEnum` | 0=无需审核, 1=单层审批, 2=双层审批 | `t_promotion_config.review_level` |
| `SolvelaJobExecuteStatusEnum` | 0=待执行, 1=执行中, 2=成功, 3=失败, 4=超时中断, 5=阻塞丢弃, 6=错过调度, 7=中断 | - |
| `TaskConfigStatusEnum` | 1=待生效, 2=生效中, 3=已下线 | `t_task_config.status` |
| `TaskFlowTypeEnum` | 1=进度推进, 2=事件丢弃 | `t_task_record_flow.flow_type` |
| `TaskRecordStatusEnum` | 0=进行中, 1=已完成, 2=已发奖, 3=已过期 | `t_task_record.status` |
| `TicketStatusEnum` | 0=未开奖, 1=未中奖, 2=已中奖 | - |
| `TransactionTypeEnum` | 1=收入, 2=支出 | `t_member_asset_transaction.transaction_type` |
| `UserTypeEnum` | 1=员工, 2=会员 | `t_login_log.user_type` |
| `WalletStatusEnum` | 0=冻结, 1=正常 | `t_member_wallet.status` |

### 字符串值枚举（对应 varchar 列，属于另一批 166 个字符串列，本次不动）

| 枚举 | 取值 | javadoc 里的表列提示 |
|---|---|---|
| `ActivityTypeEnum` | BASIC=基础活动, DRAW=奖池抽奖, TASK=任务驱动, LOTTERY=FPE彩票 | - |
| `CodeDeleteEnum` | Single=单个删除, Batch=批量删除, SingleAndBatch=单个和批量删除 | - |
| `CodeFrontComponentEnum` | Input=输入框, InputNumber=数字输入框, Textarea= 文本, BooleanSelect=布尔下拉框, SolvelaEnumSelect=枚举下拉, DictSelect=字典下拉, Date=日期选择, DateTime=时间选择, FileUpload=文件上传 | - |
| `CodeGeneratorPageTypeEnum` | modal=弹窗, drawer=抽屉, page=新页面 | - |
| `CodeQueryFieldQueryTypeEnum` | Like=模糊查询, Equal=等于, DateRange=日期范围, Date=指定日期, Enum=枚举, Dict=字典 | - |
| `ConfigKeyEnum` | super_password=万能密码, level3_protect_config=三级等保配置 | - |
| `DictDataStyleEnum` | 默认=default, 主要=primary, 成功=success, 信息=info, 警告=warning, 危险=danger | - |
| `MailTemplateTypeEnum` | string=字符串替代器, freemarker=freemarker模板引擎 | - |
| `MatchRuleEnum` | EXACT=全号匹配, TAIL=尾号匹配, HEAD=首号匹配 | - |
| `ProposalSourceTypeEnum` | TASK=任务, DRAW=抽奖, LOTTERY=彩票, MANUAL=人工 | `t_proposal_record.source_type` |
| `RiskBlockCode` | SINGLE_MAX_AMOUNT_LIMIT=单次金额超限（系统兜底）, USER_FREQUENCY_LIMIT=参与过于频繁, GLOBAL_BUDGET_LIMIT=预算已耗尽 | `t_proposal_record.risk_code` |
| `SolvelaJobBlockStrategyEnum` | DISCARD=丢弃本次, SERIAL=排队串行, OVERRIDE=中断上一次 | - |
| `SolvelaJobLaneEnum` | FAST=快车道, SLOW=慢车道 | - |
| `SolvelaJobMisfireStrategyEnum` | SKIP=跳过并记录, FIRE_ONCE=立即补跑一次 | - |
| `SolvelaJobPresetEnum` | LIGHT=轻量高频, NORMAL=常规, HEAVY=重批处理, CUSTOM=自定义 | - |
| `SolvelaJobRoleEnum` | ADMIN=仅管理, WORKER=仅执行, ALL=管理+执行, NONE=不启用 | - |
| `SolvelaJobTriggerSourceEnum` | SCHEDULE=定时调度, MANUAL=手动触发 | - |
| `SolvelaJobTriggerTypeEnum` | cron=cron表达式, one_time=一次性 | - |
| `TaskDiscardCode` | AMOUNT_MISSING=事件未携带金额, AMOUNT_BELOW_MIN=未达单笔门槛, STREAK_SAME_DAY=当日已计入连续进度, RECORD_NOT_RUNNING=任务已完成或已过期, AUDIENCE_MISMATCH=人群不符, AUDIENCE_UNKNOWN=上游未告知会员属性, ROUND_LIMIT_EXCEEDED=本周期次数已用尽, CONFIG_INVALID=任务配置异常, POOL_REJECTED=系统繁忙被拒 | `t_task_record_flow.discard_code` |
| `TaskTypeEnum` | SIMPLE=单次节点型, COUNT=计次型, AMOUNT=计额型, STREAK=连续型 | - |

### 常量形态特殊、未能自动解析

- `DataScopeTypeEnum`
- `DataScopeViewTypeEnum`
- `SolvelaJobMsg`
- `SystemEnvironmentEnum`

共 40 个枚举实现了 `BaseEnum`：整型 16 个，字符串 20 个，未解析 4 个。
