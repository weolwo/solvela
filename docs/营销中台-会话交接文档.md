# 营销中台（任务 / 抽奖 / 彩票）开发交接文档

> 本文档用于跨会话交接。新会话只需先读本文件，即可无缝接续。
> 项目：Solvela v3 二次开发 · **当前分支 `upgrade/springboot4`**（从 `task` 拉出，是它的严格超集）· 主分支 `master`

## 现状速览

| 模块 | 状态 | 细节 |
|---|---|---|
| 任务 | 配置态 + 运行态 P0/P0.5 已验收（真连库、端到端到账已对账） | §2 |
| 抽奖 | 配置态 + 运行态 + 回显全通，压测通过 | §3 |
| 彩票 | 全链路打通。**四条不可逆约束，动之前必读** | §4 |
| 活动创建向导 | P0~P3 完成，验收 27/27 | §4.5 |
| 首页营销作战大屏 | 已落地，真数据实测 | §9.5 |
| 文件模块 / 素材库 | 重写完成；选图器、活动公开图已真跑打通 | **§12** |
| 环境升级与瘦身 | Spring Boot 4.1.0 + 前端 26MB→8.3MB + guava/hutool 全删 | §9 / §10 |
| **会员域地基** | 已收口：`member_id` 换键 + 租户维度删除全部落地，`sql-update-log/` 已清空，基线即权威 | **§13** |
| **积分商城** | **7 张表已建进开发库**、`MALL_COMMODITY` 文件分类已播种；履约单已泛化成可接商城单号；**Java 代码仍是零行** | **§13.6 / §13.8** |

## 动手前必须知道的七条

**这几条都是踩过才写下来的，且都属于"不知道就会静默出错"那一类。**

1. **文件下载路径是 `/support/file/download/{id}`，别漏掉 `/support`。**
   漏了会让所有 `fileUrl` 变成死链，而 `<img>` 加载失败是静默的 —— 素材库缩略图因此
   从来没显示出来过，却一直是 358 单测 + build 全绿。见 §12.7。
2. **文件一律公开，没有「私有文件」这个概念。** `visibility` 这条线在 git 历史里只活了一天
   （v3.55.0 建、v3.56.0 删）——**看到任何"私有文件"的字样都是漏网的**。见 §12.8.5。
3. **管理端显示图片统一走 `FileThumb` 组件**，不要直接 `<img :src="fileUrl">`：
   登录态下载接口带不了 token。新增素材分类**一律按 `categoryCode` 引用，不要用自增 ID**
   （各环境的数字不同）。现有分类：COMMON / NOTICE / HELP_DOC / FEEDBACK / ACTIVITY / CONTENT。
4. **文件引用登记不是可选步骤**：业务保存时必须调 `fileAssetService.confirm(...)`，
   不登记的图会被孤儿清理任务当垃圾删掉。清理任务尚未上线，所以现在欠的账不会立刻发作。见 §12.2。
5. **图形验证码整个功能已删除**（前后端）。`/login/getCaptcha` 与 `/login/getCaptchaFlag`
   接口已不存在，登录只要 `loginName` + `password`(SM4密文) + `loginDevice`。见 §10.2。
6. **guava、hutool 已彻底移除，不要再往回引。** 替代物在 `solvela-base/common/util`，
   写工具方法前先翻一遍那个包。🔴 这些自研类多数配了**语义固化测试**（期望值是趁被替换的库
   还在 classpath 上逐条比对得来的），**改实现前先看测试**。见 §0 与 §10.3。
7. **状态/类型列已全部枚举化（2026-08-29），`Integer` 状态字段一个都不剩了。**
   接口形状没变（`@JsonValue` 出去仍是 0/1/2），但**代码里有两个编译器抓不到的坑**：
   - `Integer.valueOf(1).equals(x.getStatus())` —— `Integer.equals(Object)` 接受任何类型，
     `getStatus()` 返回枚举时它**恒为 false**，编译通过、测试不报。
     这个坑真发作过 4 次（奖品列表整片标成「已停用」、奖池排序失效……）。
   - `SolvelaBeanUtil.copy` 对类型不匹配的属性**静默跳过**，见铁律 7。

   两条棘轮用例（`EnumMigrationRatchetTest`）盯着这两类，名单都是空集。
   新增状态列请照铁律 3 的口径写。
> **一条贯穿全文的方法论**：编译、单测、前端 build 都证明不了"它真的能用"。
> 本项目多次出现"全绿但功能从未生效"——见 §12.6、§12.7、以及铁律 16 / 18 / 20。

---

## 0. 环境与铁律（务必先看）

- **JDK**：已升级到 **25.0.3**（旧 `jdk-25.0.1` 目录已删）。系统 `JAVA_HOME` 已指向 `D:/dev_tools/java/jdk-25.0.3`。
- **Maven**：3.9.16。命令行编译若报 JAVA_HOME 错误，用 `JAVA_HOME="D:/dev_tools/java/jdk-25.0.3" mvn ...`（正斜杠）。
- **前端栈**：Vue 3.5.40 + Vite 8.2.0（rolldown）+ Ant Design Vue 4.2.6 + Tailwind CSS v4（`@tailwindcss/vite`）+ Pinia 3.0.4 + vue-router 5.2.0 + decimal.js 10.6.0 + lodash 4.18.1 + echarts 6.1.0 + `@guolao/vue-monaco-editor` + vue3-json-viewer 2.4.1。
  - **前端优化与编辑器改造见 `docs/前端优化白皮书.md`**：产物 26MB → 8.3MB； monaco-editor 已整体换成 CodeMirror 6（
    `SolvelaCodeEditor` / `SolvelaCodeDiff`），
    代码编辑器语言只支持 java / json，且**只做着色不做语法检查**。
  - **没有 eslint / stylelint**：两套配置早已失效（eslint 9 起不认 eslintrc、stylelint 配置 extends 了没装的包），且 `package.json` 里从来没有 lint 脚本，已整条卸载。格式化只保留 prettier（`.prettierrc.cjs`，IDE 保存时生效）。**不要"顺手补回来"**，要补就先写 `eslint.config.js` flat config 并加 lint 脚本。
- **后端栈**：**Spring Boot 4.1.0**（Spring Framework 7.0.8 / Jakarta EE 11 / Tomcat 11）+ **Jackson 3.1.4（`tools.jackson`）** + MyBatis-Plus 3.5.17（**boot4 starter**）+ **Redisson 4.6.1** + sa-token 1.45.0（**boot4 starter**）+ **knife4j(baizhukui fork) 5.2.1 + springdoc-openapi 3.0.3** + QLExpress 4.1.2 + Lombok 1.18.46 + MySQL Connector/J 26.7.0 + ip2region 3.3.7 + tika 3.3.2 + BouncyCastle 1.85。
  - ⚠️ **guava（2026-08-06）与 hutool（2026-08-08）均已整体移除，不要再往回引**， 见 §10。替代物都在 `solvela-base/common/util`
    ，动手前先翻一遍那个包，别重复造：
    `SolvelaStringUtil`（判空/trim/join/hide/cleanBlank）、`SolvelaCaseFormat`（命名风格转换）、
    `SolvelaRandomUtil`（随机数/UUID， **凭证类必须用 `secureRandom()`**）、
    `SolvelaServletUtil`（客户端 IP）、`SolvelaBeanUtil.beanToMap`、
    `SolvelaDateFormatterEnum` + `SolvelaLocalDateUtil`（时间格式化）、`SolvelaIpUtil`（本机 IP）；
    雪花 ID 在 common-api 的 `SnowflakeIdGenerator`。
    线程工厂用 `Thread.ofPlatform().name(前缀, 0).factory()`。
    🔴 这些类多数配了**语义固化测试**（期望值是与被替换库逐条比对得来的），改实现前先看测试。
  - ⚠️ **接口文档换成了第三方 fork `com.baizhukui:knife4j-openapi3-boot4-spring-boot-starter:5.2.1`**。
    官方 knife4j 与主流社区分支（`com.github.xiaoymin` / `com.github.xingfudeshi`）都把 springdoc 钉死在 2.7.0、没有 Boot 4 版本，中途曾换成官方 springdoc 3.1.0（`/doc.html` 一度不可用），最终改用这个 fork 把 `/doc.html` 恢复了。
    **地址仍是 `/doc.html`（实测 200），`/swagger-ui/index.html` 与 `/v3/api-docs` 同样可用**，三者都在 `SwaggerConfig.SWAGGER_WHITELIST` 里免登录。
    🔴 代价是**版本由 fork 传递决定**：springdoc 被拉低到 3.0.3、swagger-core 到 2.2.47（父 pom 已移除 springdoc 的 depMgmt 条目）。要锁高版本就脱离了它的适配矩阵，需重新验证。引入前请自行评估该发布者。
    ⚠️ `knife4j:` 配置块 **四个 profile 的 `solvela-base.yaml` 必须一起加**，只加 dev 会让其它环境启动即失败。
  - ⚠️ **Jackson 2 的 API 在这个工程里已经不能用了**：包名是 `tools.jackson.*`，`ObjectMapper` 不可变（配置一律走 `JsonMapper.builder()`），`JsonSerializer/JsonDeserializer` 叫 `ValueSerializer/ValueDeserializer`，`SerializerProvider` 叫 `SerializationContext`，`JsonProcessingException` 变成非受检的 `JacksonException`（`serialize`/`deserialize` 不能再 `throws IOException`）。完整映射表见 `docs/后端升级白皮书-SpringBoot4.md` §3.3。
  - 升级的完整改动点、难点与回滚方案见 **`docs/后端升级白皮书-SpringBoot4.md`**（分支 `upgrade/springboot4`）。
- **前端构建验证**：`cd solvela-admin-web && npx vite build`。组件未挂路由时，临时在 `src/router/routers.js` 的 `];` 前插一条
  dev 路由再 build，验证后 `git checkout` 还原。
- **后端编译验证**：`JAVA_HOME="D:/dev_tools/java/jdk-25.0.3" mvn -q compile -pl solvela-marketing -am -DskipTests`。
- **单测**：`mvn test -pl solvela-marketing -am -Dtest=DrawEngineTest -Dsurefire.failIfNoSpecifiedTests=false`。

### 已确立的工程铁律（贯穿全程，务必延续）
1. **浮点比较用容差**：任何概率/金额求和判等，前端用 decimal.js、后端用 BigDecimal，容差 `0.0001`，严禁 `=== 100`。（曾因此出现"界面显示100%却保存不了"死锁）
2. **前端 UI 校验只是防呆**，后端必须服务端重算一遍（防绕过页面直接 POST）。
3. **消除魔法值 —— ⚠️ 2026-08-29 起口径变了，别照老文档写**：
   状态/类型这类**有字典的列一律是枚举**（`implements BaseEnum`，放 `solvela-model/enums`），
   字段类型直接写枚举，**不是 Integer + 具名常量**。照老口径写一个
   `private static final int XXX_STATUS = 1`，`EnumMigrationRatchetTest` 会当场红。
   - 0/1 的 `*_flag`、`is_*` 一律 **`Boolean`**（全仓 70 个标记字段无一例外）。
     不要为它们造 `YesNoEnum` —— 枚举值钱是因为值本身是字典，`visible_flag = 1` 没有字典可背。
   - 真数值（`prize_level` / `sort` / `quota` / `limit`）**保持 Integer**，
     只有哨兵值配具名常量（`PRIZE_LEVEL_NONE = 99`、`UNLIMITED = -1`）。
   - 枚举一律实现 `BaseEnum`：`getValue()` 上的 `@JsonValue` 管住 Jackson，
     `MybatisPlusConfig#enumTypeHandlerCustomizer` 管住 MyBatis，两端都按 **value** 收发，
     所以**前端拿到的仍然是 0/1/2，接口形状没变**。
   - 详见 `docs/枚举改造-阶段3工作清单.md`；棘轮用例 `EnumMigrationRatchetTest` 有两条规则，
     名单都是空的，加任何一条状态魔法常量都会红。
4. **单一状态源**：向导类表单收拢在一个 `reactive` 对象。
5. **Tailwind v4 important 是后缀**：`text-purple-600!`（不是 `!text-purple-600`）。
6. **in-DOM 模板陷阱**（仅限 CDN 直写 HTML 的原型）：浏览器把 camelCase 属性强制小写。
   - prop 可以靠 kebab 绕过：`:customRow` → `:custom-row`、`valueFormat` → `value-format`（Vue 会 camelize 回去）。
   - **⚠️ 插槽名没有 kebab 退路**：`#bodyCell` 被小写成 `#bodycell`，而 **Vue 不会 camelize 插槽名**，antd 查的是 `slots.bodyCell`，永远匹配不上 —— 表现是**表格自定义列整片渲染成空白且不报错**。
   - **正解：整个模板挪进 `<script type="text/x-template">`**，script 内容浏览器按纯文本存储、不做 HTML 解析，驼峰原样保留，一次堵死整类问题，不用逐个属性打地鼠。做法见 `docs/彩票中台-实现技术方案.md` §0.1，`彩票中台原型-FPE彩票.html` 已按此改造并实测。
   - SFC（.vue）编译期处理模板，完全不受影响。
7. **SolvelaBeanUtil.copy** 同名同类型自动拷贝——加实体字段后 Form→Entity 自然带过去。
   - 🔴 **「同类型」是致命细节：类型不匹配会被静默跳过，不报错、不警告，字段变 null。**
     枚举化改造期间这个坑发作了三次，每次都是「链路只改了一半」：
     实体已经是枚举、DTO/Form 还是 Integer，于是拷过来是 null。
     其中 `ActivityWizardCreateCommand.approveMode` 那次的后果是
     **运营在向导里勾了「需人工审批」的奖品会绕过审批直接发**（`PrizeDispatchHandler` 判
     `== ApproveModeEnum.MANUAL`，null 走免审分支）。
   - 所以**改一个列的类型，就得把整条链路一次改完**：
     Entity + DTO + Command + Query + Form + VO + DAO 形参 + 调用点，缺一个就是一处静默 null。
8. **业务编码统一规范**：活动编码、奖品编码、奖池编码、任务模板编码一律「**10 位大写字母 + 数字**」随机组合（如 `H88JHKJFNE`），**全局唯一**。
    - 唯一真源：`solvela-base` 的 `SolvelaCodeUtil`（`BIZ_CODE_REGEX` / `generateBizCode()` /
      `generateUniqueBizCode(判重函数)` / `isValidBizCode()`）。前端同源常量在 `constants/regular-const.js` 的
      `regular.bizCode`。
   - 运营两条路径：手工输入 or 点「生成」按钮（`GET /activityConfig/generateCode`、`GET /prizeConfig/generateCode`、`GET /prizePoolConfig/generateCode`、`GET /taskTemplate/generateCode`，服务端生成时已判重）。
    - **手输路径必须服务端重校验**：AddForm 上挂 `@Pattern(SolvelaCodeUtil.BIZ_CODE_REGEX)`，Service 再查一次重（表上的唯一索引直接抛
      SQL 异常对运营不友好）。
   - **编码创建后不可改**：UpdateForm 本就不含编码字段，前端编辑态把输入框和生成按钮一起禁掉；否则改一次编码，奖品/奖池/流水的引用全断。
   - 编码是随机码，凡是「填别人的编码」的地方一律改成下拉选择，别让人手打（奖品表单、奖池 CRUD 表单的「归属活动」均已改为 select）。
   - **落点清单**（新增编码类字段时照此三处补齐）：① AddForm 挂 `@Pattern`；② Service 的 add / 聚合保存里判重并给人话提示（唯一索引只是兜底）；③ 前端表单挂 `regular.bizCode` 规则 + 生成按钮 + 编辑态禁用。
   - 奖池编码有两个创建入口，都已统一：工作台「新建奖池」弹窗（打开即预填一个编码，可改可重新生成）与奖池 CRUD 表单。`workbenchSave` 里对每个 poolCode 都做了格式校验与跨活动占用判重。
   - **模板编码同时是 upsert 唯一键**：设计器进页面即预填一个新编码（并入基线，不算脏改动）；手输一个已存在的编码 = 覆盖那个模板，这是刻意保留的能力，`save()` 的返回值 `Boolean` 会告诉前端是新建还是覆盖。
   - **存量数据要迁移**：编码规范是后加的，老库里 `DAILY_SIGN_TPL` 这类编码存不进新规则。迁移脚本见 `数据库SQL脚本/mysql/任务模块-编码规范迁移与造数.sql`，改 `template_code` 必须级联 `t_task_config.template_code`。
9. **时间戳只认数据库一个时钟**：`create_time` / `update_time` 全部由 DDL 的 `DEFAULT CURRENT_TIMESTAMP` 与 `ON UPDATE CURRENT_TIMESTAMP` 产生，**代码里不要再填**。
   - `MybatisPlusFillHandler` 已停止填充时间字段，21 个实体上共 41 处 `@TableField(fill = FieldFill.*)` 已全部摘除。新建实体时**不要再加回来**。
   - **✅ 2026-08-02 已用 MP 3.5.17 源码坐实**：`TableFieldInfo#getInsertSqlPropertyMaybeIf` 里
     `if (withInsertFill) return sqlScript;` —— 直接返回、不走 `convertIf`，而
     `withInsertFill = fill == INSERT || fill == INSERT_UPDATE`。本条铁律成立。
     同日发现两处与之冲突、会让它复发的地方，均已修正：
     ① **代码生成器实体模板 `Entity.java.vm` 硬编码了这两个注解**（每生成一张新表就复发一次），
     ② `MybatisPlusFillHandler` 的 javadoc 写着"注解无需摘除"，结论正好说反。
     实测代价：`t_task_prize_mapping` 已有 2 行 `create_time` 为 NULL。
   - **⚠️ 只改 handler 不够**：`@TableField(fill=...)` 只要存在，MyBatis-Plus 生成 INSERT 时就会**直接带上该字段且跳过 `<if>` 判空**（源码里 `withInsertFill` 为 true 即 `return`，不走 `convertIf`），null 会被显式写入并覆盖 DDL 默认值 —— 实测曾导致 `create_time` 整列为 NULL。**注解必须一起摘。**
   - **新建表必须给这两列写全** `DEFAULT CURRENT_TIMESTAMP` 和 `ON UPDATE CURRENT_TIMESTAMP`，否则时间不会自动产生/刷新。自查：
     ```sql
     SELECT table_name FROM information_schema.columns
     WHERE table_schema='solvela' AND column_name='update_time' AND extra NOT LIKE '%on update%';
     ```
   - 自定义 `<update>` 里**不要写** `update_time = now()`：DDL 的 ON UPDATE 已覆盖，显式写属于冗余且多引入一个时钟源。但 `approve_time` / `first_review_time` 这类**没有** ON UPDATE 兜底的列必须显式写。
10. **JDBC 连接串必须锁定会话时区**：`connectionTimeZone=Asia/Shanghai&forceConnectionTimeZoneToSession=true`（四个环境的
    `solvela-base.yaml` 均已加）。
   - **`serverTimezone` 解决不了这个问题**：它只影响 JDBC 读写 TIMESTAMP 时的 Java 对象转换，**不改变会话时区、也不影响 `now()` 的求值**。这是个很常见的误解。
   - 不加会怎样：MySQL 部署在 UTC（云厂商默认很常见）而应用在东八区时，`now()` 与 `ON UPDATE CURRENT_TIMESTAMP` 比 Java 时间早 **整 8 小时**。曾导致 `t_member_wallet` 497 行「更新时间早于创建时间」，上游 `NoticeMapper` 的 `publish_time < now()` 也会让公告晚 8 小时才显示。
   - 排查一句话：`SELECT @@session.time_zone, now();` 与 JVM 时间对比，不一致即中招。
   - **写诊断脚本时自己也要带这两个参数**，否则脚本里的 `now()` 同样是 UTC，统计口径全错（本项目排查时踩过）。
11. **`@Transactional` 绝不能被同类自调用**（2026-07-28 彩票开发中**连踩两次**，都是同一个人写的）：
   注解靠 Spring AOP 代理生效，**同类内部方法互调不经过代理**，注解<b>静默失效</b> —— 没事务、不报错、
   `mvn compile` 和单测全都通过，只在数据不一致时才暴露。
   - 判别口诀：**凡是「循环调用一个带 `@Transactional` 的方法」，先问一句它俩是不是同一个类。**
   - 修法：把事务方法拆到独立 Bean（如 `TicketPersistService` / `LotteryDispatchBatchService`）。
   - 后果的隐蔽程度取决于场景：领号那次是「记录插进去了但 sold_count 没加」；
     派奖那次更毒 —— `PrizeDispatchHandler` 挂在 `@TransactionalEventListener(AFTER_COMMIT)` 上，
     **没有事务上下文事件根本不会投递**，表现是「记录都标成已投递了，但下游一条都没收到」。
12. **实现 `BaseEnum` 的枚举，字段必须叫 `value`，且不要手写 `getValue()`**：
   `@CheckEnum` 的校验器是用 `map(BaseEnum::getValue)` 建合法值白名单的。
   字段叫 `code` 会导致 Lombok 生成不出 `getValue()`，若再手写一个 `return null` 糊过编译，
   白名单就成了 `[null,null,null]`，**任何取值都判非法**（`TicketStatusEnum` 曾因此让「按中奖状态筛选购彩记录」恒返回 400）。
    编译器和 IDE 都不会报警，只在运行期以「校验莫名不过」的形式出现。对齐 `solvela-base` 的 `GenderEnum` 写法。
13. **判空口径必须全项目一致，尤其别用 `!arr` 判空数组**：`[]` 是 truthy，
   `!item.children` 会把「有属性但是空数组」判成「有内容」。
   四套菜单组件的 `sub-menu.vue` 都犯了这个错，而它们的顶层 `recursion-menu.vue`
   用的是 `$lodash.isEmpty` —— **同一份数据两种判法**，只是恰好当前数据构造不出空数组才没爆。
   （空 `a-sub-menu` 会让 antd 抛 `Cannot destructure property 'prefixCls' of useInjectMenu(...)`。）
   新写判空一律 `isEmpty`，并检查同一数据的其它判断点是否同口径。
14. **跨模块调用要看依赖方向，别想当然**：`solvela-marketing` 依赖 `common-api`， 所以 common-api 里的类 **不能引用**
    solvela-marketing 的 Dao（循环依赖）。 需要反向取数时用依赖倒置：common-api 定接口、solvela-marketing 实现并注册为 Bean
   （见 §4.5 的 `ActivityRefProvider`）。
   **多实现装配优先用 `List` + 各实现自报 `supportType()`，不要 `Map<Enum, Handler>`** ——
   后者正是铁律里那个「枚举键 Map 用字符串查」的温床，本项目已因此踩过三次静默失效。
   若 common-api 可能单独运行（如自身单测），用 `ObjectProvider` 而非直接注入 `List`，
   否则找不到候选 Bean 会直接启动失败。

15. **前端「看到的值 ≠ 模型里的值」**：本会话踩了三次，全都是同一族 ——
   `a-input-number` 程序化赋值不触发 Vue 模型、`form_input` 对 `RangePicker` 无效、
   `@blur` 里读 `e.target.value` 拿到的是 DOM 字符串而非组件状态。
   **统一做法：用 `@change` 维护本地草稿，提交时只读草稿、不碰 DOM。**
   另外 `@pressEnter` 在本项目的 `a-input-number` 上**不触发**，要用原生 `@keyup.enter`。
   自动化验证时同理：**每次操作前重新取元素，不复用上一次的坐标**（布局位移会让点击落偏）。
16. **断言前必须先确认前提条件真实成立**，否则「通过」和「空过」分不出来。
   本会话空过三次：`includeInactive`（库里没有已下线数据，过滤分支根本没执行）、
   `online` 接口路径写错（404 也返回 `!ok`，断言照样通过）、
   活动启用守卫（假定某活动未配置、实际已配好，「应被拦截」的断言全部落空）。
   **反证类断言要同时校验 http 状态与失败原因，不能只看 `ok=false`。**
17. **「捕获 DuplicateKeyException 后就地重查」在 REPEATABLE READ 下是失效的**
   （2026-08-01 任务运行态实测踩到：10 并发只推进了 1 笔）。
   MySQL 默认 REPEATABLE READ，事务的一致性视图在**第一次读**时就固定；
   赢家是在那之后才提交的，它插入的行**不在本事务的快照里** ——
   于是出现「INSERT 报重复、重查却查不到」这种看起来自相矛盾的现象。
   - 讽刺之处：这个到处都能见到的写法，只在「记录早已存在」时成立，
     而在「**刚刚被并发创建**」时失效 —— 后者恰恰是它被写出来要解决的那个场景。
   - 两条修法：① 重查改 `SELECT ... FOR UPDATE`（当前读）；
     ② **抛异常回滚、换新事务重试**（新事务有新快照）。任务模块选了 ②，
     因为它顺带把同事务里已插入的幂等流水一并回滚，不会留下「流水占了坑但进度没推」的半截状态。
   - 推论：**「换个事务重试」不只是重来一次，更是为了拿到新快照** ——
     就地循环重试多少次都是同一个结果。这也是事务方法必须拆独立 Bean 的又一条硬理由（配合铁律 11）。
18. **两个不同原因产生同一现象时，用例分不出对错**（铁律 16 的另一种形状）。
   任务并发用例第一版借用了「目标 500」的任务跑 10×100，结果 500 ——
   看起来像 Lost Update，实际是**完成闸门正确挡下了后 5 笔**。两者现象一模一样。
   写并发/边界用例时，先确认**待验证的机制是唯一能产生该现象的机制**，否则修错方向都不会被发现。
19. **跑 `@SpringBootTest` 必须带 `SPRING_PROFILES_ACTIVE=dev`**。
   根 pom 的两个 `<resource>` 指向同一目录，后一个 `<filtering>false</filtering>` + `*.*`
   会把前一个过滤后的 `application.yaml` 覆盖回去，于是 `@profiles.active@` **从未被替换**，
   Spring 启动直接报 `Profile '@profiles.active@' must start and end with a letter or digit`。
   这是既有构建配置的问题，不是测试写错了；用环境变量覆盖即可，未擅自改 pom。
20. **验证之前，先确认跑的是不是刚构建出来的那个包**（铁律 16 的第三种形状，2026-08-02 连踩两次）。
   Spring Boot 启动失败后，Redisson/Netty 的**非守护线程会把 JVM 吊住不退**。
   这个僵尸进程在 Windows 上锁着 fat jar 和日志文件，于是：
   ① `mvn clean` 删不掉 jar，构建**静默失败**，启动的还是旧包；
   ② `rm` 删不掉日志，新日志追加进旧文件，读到的是上一轮的堆栈。
   表现就是「改了配置重启，报的还是一模一样的错」，极易误判成"这个升级做不通"。
   - 识破的线索：两次报错的 netty channel id 和本地端口**完全相同** —— 那不可能是两次独立运行。
   - 做法：重启前先按命令行过滤掉残留进程
     （`Get-CimInstance Win32_Process -Filter "Name='java.exe'"`），
     并且**核对产物时间戳**；端口被占时换端口跑，不要盲目 kill（可能是别人 IDE 里的调试实例）。
21. **「段数不变、位置错位」是最难发现的一类数据格式变更**。
   ip2region 的 xdb 数据 v2 是 `国家|区域|省份|城市|ISP`，v3 是 `国家|省份|城市|ISP|iso-alpha2-code` ——
   都是 5 段，换数据文件既不报错也不长度异常，**只会静默把「省份」读成「区域」**，
   而字符串看上去"还挺像那么回事"，不逐字段比对根本发现不了。
   - 通则：**解析外部数据格式时不要按位置硬编码**，要从数据自带的版本标记推导布局 （本项目改成读 xdb 文件头的 `version`
     字段，见 `SolvelaIpUtil`）。
   - 并且**归一化后再落库**：让存储内容的含义和形状不随数据文件版本漂移，否则历史数据与新数据没法比。
22. **改一批表之前，表清单必须来自 `information_schema`，不能靠 grep DDL 文件**（2026-08-22 **连踩两次**）。
   本项目的建表语句分散在两处：主 schema 文件（`activity.sql`/`lottery.sql`/`mysql/task.sql`）
   **和历史迁移文件**（`mysql/sql-update-log/v3.xx.0.sql`）。后建的表往往只存在于后者，
   于是 `grep -rn "tenant_id.*DEFAULT" 数据库SQL脚本/` 这种找法**天然漏表**，而且漏得毫无征兆。
   - 第一次：租户改造按文件 grep 得 22 张，实际 25 张，漏了 `t_prize_group` /
     `t_task_event` / `t_task_record_flow`，其中 `t_task_record_flow` 有 **2933 行**会被留在旧值。
   - 第二次：换关联键按文件 grep 得 9 张，实际 10 张，又是 `t_task_record_flow`。
   - 正确做法：
     ```sql
     SELECT TABLE_NAME FROM information_schema.COLUMNS
      WHERE TABLE_SCHEMA = DATABASE() AND COLUMN_NAME = '要改的列名';
     ```
     并且**把「脚本覆盖的表」与「库里实际的表」做集合比对**，差集不为空就不许往下走。
     两次都是这个比对兜住的 —— 见 §13.5。
   - 同族提醒：索引名、列定义也别靠转述。本次 `t_prize_log` 上那条索引真名叫
     `idx_prize_log_`（**末尾带下划线**），照着记忆写迁移必错。一律从 `information_schema.STATISTICS` 导。

23. **多行 SQL / JSON / 脚本一律用文本块，不要 `+` 拼接**（JDK 15+ 特性，本项目底座是 **JDK 25 + Spring Boot 4.1**）。
   `+` 拼多行字符串有两个实打实的坑，都不报错：
   - **漏空格**。`"... FROM t "` 和 `"WHERE x=1"` 少写一个空格就拼成 `FROM tWHERE`，
     只有运行到那条 SQL 才炸；而它常常藏在某个少走的分支里。
   - **改一行要动三行**（前一行的结尾引号、本行、后一行），diff 噪音大，review 时看不出真实改动。
   写法：
   ```java
   String sql = """
           SELECT TABLE_NAME, COLUMN_NAME
             FROM information_schema.COLUMNS
            WHERE TABLE_SCHEMA = '%s'
            ORDER BY TABLE_NAME""".formatted(schema);
   ```
   带变量就用 `.formatted(...)`，**不要**退回 `+`。行尾 `\` 可以续行而不产生换行（拼 JDBC URL 常用）。
   - 主工程已有先例：`LuaScriptEnum`、`DrawStockService`、`FileCloudStorageMissingGuard`。
   - ⚠️ **不要用脚本批量把 `+` 改成文本块**。2026-08-22 试过一次，脚本把「带变量的拼接」
     从中间切断，生成出 `...COLUMNS""" + "WHERE ..."` 这种少一个空格的坏 SQL ——
     语法能编译、SQL 是错的。含变量的拼接必须人工改成 `.formatted()`。

### 环境升级连带修复（已完成，说明背景）
- Vite 8：`vite.config.js` 里 `/@/` 别名导入改成相对路径（rolldown 配置加载不认应用级别名）。
- Pinia 3：5 个 store 从 `defineStore({id,...})` 改为 `defineStore(id, {...})`。
- vue-router 5：`router/index.js` 守卫从 `next()` 回调改为返回值风格。
- MP 3.5.17：`ServiceImpl` 包名从 `extension.service.impl` → `spring.service.impl`（全工程已迁移）；starter 不再传递
  `mybatis-plus-extension`，solvela-base 已显式引入。
- Lombok：根 pom 的 maven-compiler-plugin 显式声明 `annotationProcessorPaths`（JDK 23+ 默认禁用 classpath 隐式注解处理）。
- `SolvelaReloadManager` 用 `@Lazy` + `ApplicationReadyEvent` 消除 BeanPostProcessorChecker 警告。
- `ThreadPoolUtil` 删除阿里 TTL 依赖，改用 MDC 快照透传（ScopedValue 穿不透线程池，故不用）。

### Spring Boot 4 升级连带修复（2026-08-02，分支 `upgrade/springboot4`）

> 完整版见 **`docs/后端升级白皮书-SpringBoot4.md`**，这里只列「动代码时会撞上」的几条。

- **starter 改名**：`spring-boot-starter-aop` 在 Boot 4 已删除，改叫 `spring-boot-starter-aspectj`。
- **Jackson 3**：见上面 §0 前端/后端栈里的警告。新写序列化器一律继承 `ValueSerializer`/`ValueDeserializer`。
- **Spring 7**：`MappingJackson2HttpMessageConverter` 没了 → `JacksonJsonHttpMessageConverter`；
  `HttpComponentsClientHttpRequestFactory.setConnectTimeout` **被删了**，连接超时改配在连接管理器的 `ConnectionConfig` 上。
- **`@SpringBootApplication(exclude = ...)` 里不能写 classpath 上不存在的类**，Boot 会直接启动失败
  （`UserDetailsServiceAutoConfiguration` 在 Boot 4 挪进了独立的 `spring-boot-security` 模块，本项目没有它，排除项已删）。
- **Redisson 4**：`RScript.ReturnType.INTEGER` 改名 `LONG`；
  **不再把空密码当成"没有密码"** —— yaml 里那行空的 `spring.data.redis.password:` 必须删掉，否则对无密码 Redis 会照发 AUTH 而启动失败。
- **`spring.jackson` 的枚举/日期开关挪位置了**：`serialization.write-enums-using-to-string` 之类
  要写到 `spring.jackson.datatype.enum.*` / `.datatype.datetime.*` 下，**写错是启动期硬失败**，不是运行期才暴露。
- **依赖清理**：common-api 的 pom 原先整段抄了 solvela-base 的 40 条依赖（它本就 depend on solvela-base，那份拷贝对
  classpath 零影响），
  已删到只剩 7 条直接依赖。**别再往里抄传递依赖**，要用什么直接声明什么。
- **poi 版本必须成对管**：只在 depMgmt 声明 `poi-ooxml` 不够，`poi` 核心会按 Maven「同深度先声明者胜」被 fastexcel 拽回旧版，两者版本错配。
- **`mvn dependency:analyze` 要显式指定 3.11.0**（仓库里的 3.7.0 读不了 JDK 25 字节码，报 `Unsupported class file major version 69`），
  且它是基于字节码的，**所有靠反射/配置装配的依赖都会被误报成 unused**，不能照着告警删。

---

## 1. 目录与模块地图

```
solvela-api/   后端
├── solvela-base          框架基础（含代码生成器模板 code-generator-template）
├── solvela-admin         系统管理
├── common-api       跨模块公共（ledger账本 / consumer事件派发 / risk风控 / prize奖励 / activity活动 / scriptengine）
└── solvela-marketing     营销业务
    └── sa/
        ├── task/          任务模块（taskconfig / tasktemplate / record / prizemapping）
        ├── lottery/       彩票模块（config / issue / prizerule / record / engine / runtime / settle / constant）
        │                   numberpool 已停用，仅存代码作算法参考，不进 Spring 容器
        └── draw/          抽奖模块（poolconfig / poolitem / prizemapping / drawlog / engine / runtime）

solvela-admin-web/src/       前端
├── views/business/activity/activity-wizard/   活动创建向导（ActivityWizard + PrizeQuickPanel）
├── views/business/activity/activity-config/   活动配置列表与表单（已改造：完备度列/配置玩法/升级玩法）
├── views/business/task/task-wizard/       任务配置向导（含 SchemaFormRenderer / PrizeLadderBuilder）
├── views/business/task/task-template/     任务模板设计器
├── views/business/draw/draw-workbench/    抽奖工作台（DrawWorkbench + 2个Tab组件）
├── views/business/draw/prize-pool-item/   奖池物资 CRUD（生成器产物）
├── views/business/lottery/lottery-workbench/  彩票工作台（LotteryWorkbench + EngineConfigPanel + IssueConsole）
├── views/business/prize/prize-log/            发奖审批台（改造自生成器产物）
└── api/business/task|draw|lottery|activity/   API 封装

⚠️ 三个玩法组件（DrawWorkbench / TaskWizard / LotteryWorkbench）都支持 embedded 模式，
   被活动创建向导第二步动态挂载。改它们时必须同时回归独立入口，见 §4.5。

数据库SQL脚本/
├── mysql/task.sql              任务模块建表
├── activity.sql               活动/奖池/彩票建表
├── lottery.sql
└── mysql/                      schema-baseline.sql + data-baseline.sql（唯一权威）
                                ⚠️ sql-update-log/ 已于 2026-08-22 整个删除，见 mysql/README.md
docs/                           原型 HTML + 技术方案 + 本交接文档
```

---

## 2. 任务模块（🔴 配置态完整，**运行态一行代码都没有**）

> ⚠️ **2026-07-30 读码核实，此前「已完成，前后端联调就绪」的表述是错的。**
> 完整评估与改进方案见 `docs/任务中台-改进技术方案.md`（8 节，待评估）。

### 🔴 实测缺口（全部有 grep 实据）

| 缺口 | 证据 |
|---|---|
| `trigger_event` **没有任何消费方** | 全工程 grep 只命中实体与 Form/VO，**零处业务代码** |
| `rule_script` **从未被执行** | 业务代码里零命中；QLExpress 引擎只被彩票模块用了 |
| 进度无法推进 | `TaskRecordService` 是生成器裸产物，只有 CRUD |
| 达标不发奖 | 没有任何地方 `publishEvent(UserPrizeEvent)` |

**准确的状态是**：运营能把任务配出来、落进库，但**没有任何东西会让它动起来**。
库里那 3 条 `task_name='ggg'` 就是配置态跑通、运行态从未存在的证据。

### 表设计里已经做对的地方（实现运行态时不要绕开）
- `period_key` + `uk(member_name, task_config_id, period_key)` = **周期幂等**，
  「每日签到」每天一条，重复领取由唯一索引挡住，不需要额外分布式锁。
- `rule_snapshot` / `prize_snapshot` = **规则变更不影响进行中的任务**，
  与抽奖的 `DrawPrizeSnapshot`、彩票的奖级快照同一思路。

### 核心思想
"模板即交互"：新增任务玩法 = 运营在**任务模板设计器**配一条 `ui_schema` + 开发写一个 `rule_script`(QLExpress)，**向导前端零改动**自动就绪；派发/派奖走公共链路。

> ⚠️ 这个设想只实现了一半：`ui_schema` 解决了「长什么样」，
> 但「怎么算」全塞进了 `rule_script` —— 那不是零开发，只是把开发从前端挪到了脚本，
> 而且脚本是自由文本，无法被策略工厂分类。方案 §3 给了三层分离的改法。

### ui_schema 契约（最重要，三方共享）
- `params[]` 驱动管理端动态表单，`render` 驱动 C端卡片。
- `widget`：number/text/select/radio/switch/slider/**image_upload**。
- **`visibleWhen` 契约主形态 `{field, eq}`**，兼容 `{key, value}`（`isSchemaParamVisible` 双兼容）。**新增模板强制用 `{field, eq}`**。
- `image_upload` 值随 `rule_config` 提交时，服务端按 widget 拆分：image_upload → `uiConfig`，其余 → `ruleConfig`（`splitSchemaValues` 前后端同函数语义）。
- 后端 `TaskTemplateService.save()` 保存前做 ui_schema 结构校验；向导提交 `TaskConfigService.wizardSubmit()` 做**按 schema 反向校验**（必填且 visibleWhen 可见的参数必须有值，隐藏字段不校验也不该提交）。

### 前端文件
- `task-wizard/TaskWizard.vue`（5步向导 + 成功页 + 离开拦截 + 本地草稿）。**活动大类与模板列表均已接口化**：活动来自 `/activityConfig/optionList?activityType=TASK`，模板来自 `/taskTemplate/optionList`；模板拉回后自动选中第一个；模板为空时第1步渲染引导去设计器建模板。
  - ⚠️ 模板异步化带来的坑：模板切换的 watch 里必须先判 `selectedTemplate` 是否已解析，否则「恢复草稿」时模板列表还没回来，会把草稿里的 `ruleParams` 冲成 `{}`。
- `task-wizard/task-wizard-const.js`（所有枚举/`isSchemaParamVisible`/`splitSchemaValues`/`buildDefaultWizardForm`）。**`ACTIVITY_OPTIONS` 与 `TASK_TEMPLATES` 两份 mock 已删除**，`buildDefaultWizardForm()` 的 templateCode 改为留空。
- `task-wizard/SchemaFormRenderer.vue`（动态表单渲染引擎，向导第2步与设计器共用）
- `task-wizard/PrizeLadderBuilder.vue`（阶梯奖励）
- `task-template/TaskTemplateDesigner.vue`（双 Monaco：rule_script[java] + ui_schema[json]，右侧实时预览，脏标记/最后保存时间/离开拦截/本地草稿）
- `api/business/task/task-api.js`

### 后端接口
- `POST /taskConfig/wizard/submit` → `TaskConfigService.wizardSubmit`（主子表事务，返回新建ID）
- `POST /taskTemplate/save` → `TaskTemplateService.save`（按 templateCode upsert，返回 Boolean 区分新建/覆盖）
- `GET /taskTemplate/optionList` → `TaskTemplateOptionVO`（record）：**ui_schema 以 JSON 对象下发**（列表VO里是原始字符串），前端拿到即可直接喂 `SchemaFormRenderer`；卡片的 `icon`/`description` 取自 `ui_schema.icon`/`ui_schema.desc`（这两个是约定字段，不入列，只活在 JSON 里，缺省给兜底图标 🧩）。单个模板 ui_schema 解析失败只剔除该模板并告警，不影响整个向导。
- `GET /taskTemplate/generateCode` → 生成模板编码

### 已知约定
- 成功页三出口：返回列表 / 同活动再建一个（保留 activityCode+时间+人群）/ 查看刚创建的任务。
- 任务列表页路由 `/business/task/task-config`，支持 `?taskName=` `?activityCode=` 回填定位。
- 富文本：`rule_desc` 用项目现成 `components/framework/wangeditor`，C端渲染要用 `rich-text`/`v-html`。
- 相关增量 SQL：v3.32（会员钱包 SKU 化）、v3.33（t_task_template 加 trigger_event）。

---

## 3. 抽奖模块（配置态 + 运行态 + 回显全通，代码完结，只差跑联调压测）

### 3.1 表结构关键点（activity.sql）
- `t_prize_pool_item`（SKU化）：`activity_code` / `prize_code`（关联 t_prize_config）/ `total_stock`(-1不限) / `user_max_count`(-1不限) / `used_stock` / `version`(乐观锁) / `white_list`(json数组)。**已删除 item_name/item_value/item_image**（SKU化，展示信息来自 t_prize_config）。
- `t_prize_pool_config`：`pool_code`（`uk_pool_code` 全局唯一，建表时已有，无需迁移；取值遵循铁律 8 的 10 位随机码）/ `pool_name` / `cost_asset_type`(CREDIT积分/TICKET券/NONE) / `cost_value` / `draw_mode` / `status`(1开启)。
- `t_pool_prize_mapping`：`pool_code` / `prize_item_id` / `probability`(万分位decimal) / **`is_fallback`**(v3.34新增) / `sort_weight`。
- `t_draw_prize_log`：`trace_id` / `activity_code` / `pool_code` / `member_name` / `prize_item_id` / `prize_code` / `status`(0未中/1中/2库存不足/3异常)。
- `t_activity_config`：`activity_code`(唯一) / `status`(0未开始/1上线/2下线)。**activity_code 无默认值（前端必传）。**
- `t_prize_config`：`prize_code` 原先只是普通列（跨活动可重复），v3.35 补了 `uk_prize_code` 唯一索引，对齐「编码全局唯一」的约定；两个 AddForm 的 `tenant_id` 也从 @NotBlank 放开了（前端已删除该字段，DB 有默认值 '0'，再必填会直接 400）。

### 3.2 前端：抽奖工作台（3组件已完成，已全部去 mock 接真实接口）
- `draw-workbench/DrawWorkbench.vue`（外壳）：顶部活动下拉来自 `/activityConfig/optionList?activityType=DRAW`；**isOnline 由服务端活动状态决定，改成只读 Tag**（原来是可手拨的 switch，会和服务端结构锁打架）；切活动即 `loadDetail()` 整体换血；`a-tabs` 两个 force-render 的 pane；**Tab1→Tab2 库存联动**（Tab1 @change 触发 `tab1Ref.getData()` 喂给 Tab2 的 `prizeItems` prop）；保存前校验 `tab2.isProbabilityValid()`；保存成功后再拉一次 detail 拿服务端权威 usedStock；有未保存改动时 `onBeforeRouteLeave` 拦截。
- **两个 Tab 各自的「保存」按钮已删除**：主子表是一次事务，半截保存会给运营错误的安全感，落库统一走顶部「保存并发布」。
- `components/PrizeItemLibrary.vue`（Tab1 物资库）：读写分离（名称/价值只读），可配 totalStock/userMaxCount/**whiteList**；资产抽屉引入；decimal.js 预算；isOnline 锁；**白名单弹窗**（换行/中英文逗号切分→trim→去空→Set去重）。
- `components/PoolProbabilityEngine.vue`（Tab2 概率配置 V2 多池）：`poolList` 多奖池隔离，左侧菜单增删池，右侧概率表；兜底自动配平（decimal，每池独立）；沙盘模拟器含**库存超发预警**（按 prizeItems 剩余库存对比模拟发放量）；`getData()` 返回完整 poolList；`isProbabilityValid()` 横扫所有池。
- `components/PrizeItemLibrary.vue` 的物资列表与资产大库抽屉均已接口化：`setData()` 回显，资产大库来自 `/prizeConfig/optionList?activityCode=`。
- `components/PoolProbabilityEngine.vue` 增加 `setData()`；无奖池时右侧渲染 a-empty 引导建池，不再出现「0% 未闭环」的无主体报错态；「新建奖池」弹窗按铁律 8 生成/校验 poolCode（旧的 `POOL_<时间戳base36>` 拼法已废弃）。
- `api/business/draw/draw-workbench-api.js` → `GET /prizePoolConfig/workbench/detail` + `POST /prizePoolConfig/workbench/save`。
- **契约**：`{ activityCode, prizeItemList:[{prizeCode,totalStock,userMaxCount,whiteList}], poolList:[{poolCode,poolName,prizeMappingList:[{prizeCode,probability,isFallback}]}] }`，回显 VO 与之同构（额外多 activityName/activityStatus/online 和只读的 prizeName/prizeType/prizeValue/usedStock）。
- ⚠️ **坑（已踩）**：`JsonConfig` 把 **BigDecimal 全局序列化成字符串**，所以 `probability`/`prizeValue` 回显时是 `"10.9500"` 这种字符串，前端 `setData` 必须 `Number()` 归一化，否则概率输入框行为异常、且首次自动配平会把配置误判成「有未保存改动」。

### 3.3 后端：配置态聚合回显（已完成）
`GET /prizePoolConfig/workbench/detail?activityCode=` → `PrizePoolConfigService.workbenchDetail()`：
- 返回 `DrawWorkbenchVO`（record，四个 VO 在 `poolconfig/domain/vo`），与保存入参同构。
- 物资 SKU 化后名称/价值回查 `t_prize_config` 补齐；坑位的 `prize_item_id` 服务端翻译回 `prizeCode`。
- 空配置返回空列表而非报错，前端据此进入「从零配置」态。
- 白名单脏 JSON 单点降级为空名单，不让一行脏数据把整个工作台打不开。
- 配套下拉接口：`GET /activityConfig/optionList?activityType=DRAW`、`GET /prizeConfig/optionList?activityCode=`。

### 3.4 后端：配置态聚合保存（已完成）
`POST /prizePoolConfig/workbench/save` → `PrizePoolConfigService.workbenchSave()` `@Transactional` 八步 + 预热：
1. 活动存在性 + status==1 判上线
2. 物资编码去重 + 校验存在于 t_prize_config
3. 逐池：**池编码格式校验(铁律8)**、概率闭环(BigDecimal容差)、池编码本次提交内唯一、坑位引用完整性、**每池最多一个兜底**
4. 加载 DB 现状
5. **上线结构锁**（`checkOnlineStructureLock`）：禁删/禁增物资、库存只增不减(-1改回限量也算缩减)、禁增删池、池内坑位集合冻结但概率可调
6. 物资 upsert（used_stock/version 永不收前端值）
7. 奖池 upsert（只管 poolName，不覆盖 cost_asset_type 等门票配置；新建池先查 `uk_pool_code` 是否被别的活动占用，给人话提示而不是抛 SQL 异常）
8. 坑位映射整池删+insertBatch（prizeCode→prize_item_id 服务端解析）
9. **预热库存缓存**（DrawStockService.warmStock）
- Form：`DrawWorkbenchSaveForm` / `DrawWorkbenchPoolItemForm` / `DrawWorkbenchPoolForm` / `DrawWorkbenchMappingForm`（poolconfig/domain/form 下）。
- `PoolPrizeMappingDao` 已换 `CustomizedBaseMapper` 拿 insertBatch。
- `ActivityConfigService` 已加 `getByActivityCode`。

### 3.5 后端：抽奖引擎（纯内存，9单测全绿）
`draw/engine/` 包，record + sealed 主战场：
- `DrawPrizeSnapshot`(record，紧凑构造器防null白名单) / `ProbabilityRange`(record，构造校验区间) / `DrawPoolSnapshot`(record，构造校验概率闭环，`of()`工厂累加建区间) / `DrawResult`(**sealed** permits Hit/NoStock)。
- `DrawEngine`（纯函数，随机数可注入供测试）：判定优先级 = **白名单必中(仍受库存约束) → 概率区间命中 → 库存不足降级兜底 → 兜底也干涸则 NoStock**。兜底自己被抽中且无库存不自我降级。
- 测试 `DrawEngineTest`（solvela-marketing/src/test）：概率未闭环拒绝构造、区间边界、10万次固定种子分布 (±0.5pp)
  、白名单必中、白名单无库存回落、降级兜底、双干涸NoStock、switch模式匹配消费。

### 3.6 后端：抽奖运行态（已完成，待联调压测）
`draw/runtime/` 包，`POST /drawPrizeLog/execute` → `DrawExecuteService.execute()` `@Transactional`：
链路 = **幂等防重(requestId SETNX) → 防刷限流(RRateLimiter 单用户单活动2次/秒) → 奖池校验 → 组装快照 → 解析限领周期 → DrawEngine判定 → 双层扣减 → 落流水**。
- **双层防超发**：① Redis Lua 原子预扣（`DrawStockService` Text Block，返回1/-1/-2/-3）扛并发；② DB 条件更新 `increaseUsedStock`（`WHERE total_stock=-1 OR used_stock<total_stock`）做最终一致性；DB 拒绝则补偿回滚 Redis。

#### 🔴 v3.64 契约变更：不再扣费，因此也不再退费

**接口行为变了，接入方必须知道。** 此前本链路会按奖池的 `cost_asset_type`/`cost_value`
在抽奖前扣钱包积分、并在 NoStock 时自动退还。这两个字段连同 `deductDrawCost`/`refundDrawCost`
已一并移除（迁移脚本 `v3.64.0.sql`）。

- **谁来扣**：抽一次消耗什么、消耗多少，改由**上游业务**在调用本接口前自行扣减 ——
  与「去哪个奖池抽」是同一类决策，属于业务规则而非奖池属性。
  这也让抽奖与彩票两个引擎的定位终于一致（见 §4「定位：号码派发引擎，与业务解耦」）。
- **谁来退**：本接口返回 `ofMiss（手慢了，奖品已被抽完）` 时，上游若已扣过资产**需要自行退还**。
  判断依据就是返回值，不需要额外接口。
- **为什么拆**：① 定价是业务规则 —— 同一奖池对新人免费、对老用户收 100 分是正常诉求，
  单价钉在奖池行上这类规则一条都写不出来；② 那个三选一实际只有一个值可用 ——
  TICKET 运行态直接返回「暂未开放」，CREDIT 恒定映射钱包 SCORE；
  ③ 开发库两个奖池都是 `CREDIT` + `cost_value=0`，而扣减处判 `signum()<=0` 直接跳过，
  **配置写着「消耗积分」、运行时一分不扣**，这种矛盾随字段一起消失。
- **存量影响**：无。变更前 `cost_value` 全为 0，实际从未扣过费。
- **异步派发**：中奖 `publishEvent(UserPrizeEvent)`，经 `GlobalEventDispatcher` 的 `@TransactionalEventListener(AFTER_COMMIT)` 在**事务提交后**进入 `PrizeDispatchHandler`（审批/免审分流 → consumer→risk→ledger）。traceId 作 sourceBizId 配合 t_prize_log 唯一索引防重。
- 相关：`StockDeductResult`(record) / `DrawCacheKey` / `DrawExecuteForm` / `DrawExecuteVO`(record)。

#### v3.64：`reset_period` 周期重置已落地（原待办第 7 项）

此前 `reset_period` 是**装饰字段**，后端从未读取 —— 限领计数 key 没有周期维度也没有 TTL，
`user_max_count` 恒等于「整个活动期间累计 N 次」，运营选了「每天」用户抽完一次就再也抽不到。

- **实现**：`DrawPeriodResolver`（纯函数，15 单测）把 `reset_period` 翻译成周期桶 + TTL，
  key 变成 `draw:user:{活动}:{奖项}:{周期桶}:{会员}`。换周期即换 key，计数天然归零，
  不需要定时任务，也不存在「清到一半」的中间态。
- **桶格式**：DAY=`20260816` / WEEK=`2026W33`（带 ISO week-based-year，否则跨年周撞桶）
  / MONTH=`202608` / ACTIVITY 与脏值=`ALL`（不重置）。
  **脏值刻意退回不重置**：多重置一次是资损，少重置一次只是偏严格。
- **时钟**：周期桶按**数据库时间**算（`PrizePoolItemDao.selectDbNow`，铁律 9/10）。
  多实例下跨零点用 JVM 时间会出现「A 节点认为还是昨天、B 节点认为已是今天」，
  同一用户在两个桶里各拿一次额度 —— 正好是限领要防的那件事。
- **热路径成本**：两级短路 —— `reset_period` 不需要时钟、或本池所有奖项都不限领时，
  直接返回 ALL 桶不查库。只有真正按天/周/月重置的池才多一次 `SELECT NOW()`。
- **TTL 在 Lua 内、`INCR` 之后**：拆成两次调用的话，INCR 完成而 EXPIRE 未执行时崩溃，
  会留下永不过期的计数 key，那个用户就被永久锁死了。
- ⚠️ **同一次请求的 deduct 与 rollback 必须传同一个 period**，否则回滚会去减另一个桶。

- 钱包扩展：`MemberWalletDao.deductBalanceWithVersion` + XML；`BizErrorCode.BALANCE_NOT_ENOUGH(50002)`。
  （抽奖链路已不再使用，保留给上游业务扣费用）

### 3.7 压测工具
`docs/压测-抽奖并发验证.js`（零依赖 node）：拿浏览器 token 跑并发，验证绝不超发。注释含判定标准 A/B/C。典型场景：iPhone 库存5+概率50%+100并发500次，中奖必须恰好5次。

---

## 4. 彩票模块（2026-07-28 全链路打通，可结案）

> 完整方案与验收记录见 `docs/彩票中台-实现技术方案.md`（16 节）。这里只放**接手前必须知道的硬约束**。

### 定位：号码派发引擎，与业务解耦
消耗多少积分、单人限购几张，**都由上游业务算完再调进来**，本模块只发号。
所以领号链路里没有资产扣减也没有退还，比 `DrawExecuteService` 短得多。
保留的幂等（requestId SETNX）与限流防的是网络重试和恶意刷接口，**与业务限购是两回事** ——
本模块必须假定同一个 memberName 可以合法领任意多张号，别给 `(issue_no, member_name)` 加唯一索引。

后续会通过 `@ScriptFunction` 挂到 QLExpress 上，`TicketIssueService.obtain` 同时提供
平铺参数与收 Form 的两个重载，核心实现只有一份。

### 🔴 四条不可逆约束（违反会造成资损或历史数据失效）

1. **发号游标绝不回滚**。抽奖的库存预扣失败要 `rollback` 回滚 Redis；**彩票方向相反** ——
   回滚游标 = 两个用户拿到同一个 `sequence_no` = 同一个号码。
   **浪费一个号（留空洞）可以接受，发重号不行。** 事务回滚时游标同样不退回。
   任何在发号路径上写 `DECR` 的改动都要按资损处理。
2. **`lottery.fpe.master-secret` 一旦有期号发过号就不可轮换**。号码是「连续游标经 FPE 加密」得来的，
   换密钥等于换一套加密映射：已发号码再也反解不回游标（客服验真、对账自证清白全失效），
   新号码还可能与历史号码重复。dev 已配，test/pre/prod 留的是注释而非 `${VAR}` 占位
   —— 占位符在环境变量缺失时会让整个后台启不来，而彩票只是众多模块之一。
3. **`issue_no` 创建后不可修改**。它是 FPE 的 tweak（`key = HMAC(secret, lotteryCode|issueNo)`），
   改了等于换密钥。`LotteryIssueUpdateForm` 里刻意不定义该字段，让越权意图在编译期就无处安放。
4. **`number_length` / `total_count` 在发过号后永久冻结**（结构锁）。但两者冻结的**理由不同，别混为一谈**：
   前者是密码学约束（FPE 定义域由它决定），后者是业务约束（防已承诺的发行量被追溯篡改）。
   **只冻结这两个引擎参数，名称与奖级规则仍可改** —— 锁太死会逼运营绕过流程。

### 关键设计（省得重新推导）
- **号码固定十进制、长度 4~9 位**。字母对彩票没意义，且 `0/O`、`1/I` 形近会制造「我明明中了」这类
  **无法自证**的纠纷 —— 而本方案的号码本来是可反解验真的。下限 4 位是为了号码空间不小于 1 万。
- **Redis 游标 1-indexed（顺着 INCR 的天然行为），`sequence_no` 0-indexed（直接就是 FPE 输入），
  换算只在 `toSequenceNo` 一处**。放在 Redis 这侧是因为两个边界容错不对等：
  Redis 侧算错只是跳一个号，加密侧算错会越界或号码错位。
- **冷启动回源必须 `SET NX` + 取 `MAX(sequence_no)` 而非 `COUNT`**：有空洞时 COUNT 偏低，回填后必然重号。
- **奖级互斥 = SQL 里 `win_status = 0` 的守卫 + 按 `prizeLevel` 升序执行**，
  等价于 `TicketMatcher` 的「命中即止」。**两者是同一套规则的两种实现，改一边必须同步另一边**，
  `SettleSemanticsTest` 就是防漂移的。
- **期号 `status=1` 被复用为可恢复的核销断点**：开奖号码在抢到闸门那一刻定案且不再变，
  续跑时忽略入参号码，避免「中途换号」。
- **未中奖 `prize_level=99` 而非 NULL**：C 端要按奖级排序，NULL 在 `ORDER BY ASC` 里会排最前、正好排反。
- **派奖与核销分开**，用 `dispatch_status` 列做扫描标记，不靠唯一索引的异常去重
  （本项目已证明基于异常的静默去重会掩盖真问题）。

### 验收强度（都是真跑，不是推断）
- 彩票自身 **36 单测**全绿（FpeCipher 12 / TicketMatcher 12 / SequenceCursor 7 / SettleSemantics 5），
  含 `length=4/5` 的**全域遍历证双射**（零重复零遗漏）。 加上抽奖的 `DrawEngineTest` 9 个，solvela-marketing 全模块共 45
  个 —— 旧文档写的「45 单测」是这个全模块数，别误当成彩票一家的。
- 卖光一期：游标恰好 `[0..totalCount)` 无空洞、号码去重 == 记录数、第 N+1 次正确售罄。
- 冷启动：DB 有 206 条时删掉 Redis 游标，再领接上 206/207，没有从 0 重来。
- 200 并发领号零重复。
- 全域 1 万张开奖：分奖级 1/99/990/8910 **与理论值完全吻合**，且反向核对
  （二等奖里无全号命中、未中奖里无任一规则可命中等）**全部为 0**。
- 派奖 1090 == `t_prize_log` 1090 == 幂等键去重 1090，零失败。
- 审批：并发批两次第二次被拒；驳回留痕；通过后立即派发成功。

### 遗留
- C 端页面（产品明确不做）。
- 压测规模上限目前是 1 万张 + 200 并发，未做大规模压测。
- 上游 QLExpress 接入（`@ScriptFunction` 注解由使用方按需添加）。

---

## 4.5 活动创建向导（2026-07-29 完成，P0~P3）

> 完整方案见 `docs/活动创建向导-实现技术方案.md`（12 节）。交互以
> `docs/活动创建向导原型-一体化向导.html` 为准。这里只放**接手前必须知道的**。

### 形态：一体化向导，不是跳转式

```
活动中心 › 活动配置 →「创建活动向导」
  ① 创建活动（左：名称/时间/类型/编码　右：就地建奖品）
       ↓ 提交即落库（聚合接口，活动+奖品同事务）
  ② 配置玩法 —— 按类型动态挂载对应玩法组件（embedded=true）
       BASIC 无此步，步骤条收缩为两步
  ③ 完成
```

### 🔴 新增第四种活动类型 `BASIC`（基础活动）

**改动了 `t_activity_config.activity_type` 的取值集合**，动活动域前必知。

BASIC = 只有活动外壳、不挂玩法引擎，但**可以挂奖品**（`t_prize_config.activity_code`
是它的归属键且与玩法无关）。引入它是为了消除一处语义歧义：原先「没有玩法配置」同时意味着
**半途放弃**和**本来就不需要**两件事，向导第二步该显示什么、列表该标什么都无从判断。

配套的类型冻结规则**不是一刀切**：

```
BASIC → DRAW / TASK / LOTTERY     ✅ 放行（无玩法下游，升级不产生孤儿数据）
DRAW / TASK / LOTTERY → 任何类型   ❌ 拒绝（含互转与降级回 BASIC）
```

`UpdateForm` 里**不定义** `activityType`（编译期挡死普通编辑），升级走独立窄接口
`POST /activityConfig/upgradeType`，服务端双校验「当前是 BASIC」+「下游玩法表为空」。

### 🔴 三条不要踩回去的约束

**① 完备度判据必须是「能不能上线」，不是「有没有记录」。**
`t_lottery_config` 完全可以在没配任何奖级规则时保存成功，只看「有无记录」会把一个
上不了线的半残废显示成「已配置」。故各玩法把上线前校验抽成可复用方法
（`LotteryConfigService.checkOnlineReady`），`online()` 与完备度统计**共用一份**，防漂移。
⚠️ 注意这与「升级」的判据**刻意不同**：升级问「有没有东西会被孤立」，看有无记录就够。
两个问题不同，别为了统一而合并。

**② 删除守卫的下游统计必须包含资产大库 `t_prize_config`。**
BASIC 活动按定义没有玩法下游，**资产大库是它唯一的引用来源** —— 漏了这张表等于对 BASIC
完全没有守卫，一个挂了 10 个奖品的活动会被判「无引用」而顺滑删除，奖品当场悬空。
另外：**上线过的活动永不可删，只能下线**（`t_prize_log.activity_code` 是发奖流水的追溯键）。
评估过逻辑删除，不采用 —— 会与 `uk_act_code` 唯一索引打架（删掉的记录仍占编码，
运营复用编码被判重却看不到那条记录），且替代不了守卫。

**③ 外壳不持有玩法组件的「保存」按钮。**
`TaskConfigService.wizardSubmit` 是单个 `@Transactional` 一次性提交，`TaskWizard`
**根本不存在可供外部调用的「中途 save」**。保存权留在各玩法组件自己手里，
外壳第二步只放「稍后再配」，推进完成页由子组件 `emit('saved')` 驱动。
离开拦截同理只留内层一处（它注册在子组件里同样对本路由生效），外壳不加第二层，
否则内外各弹一次确认框。

### 三个玩法组件的内嵌契约

统一加 `activityCode` + `embedded` 两个 prop 与 `emit('saved')`。
`embedded=false` 时行为与改造前完全一致（三个独立入口已逐项回归）。

| 组件 | 内嵌时 |
|---|---|
| `DrawWorkbench` | 隐藏活动下拉 |
| `LotteryWorkbench` | 只隐藏活动下拉，**玩法下拉保留**（一个彩票活动可有多个玩法） |
| `TaskWizard` | 活动置灰而非隐藏、抑制自带成功页、跳过草稿恢复 |

⚠️ 三处都必须**同时处理 `onMounted` 与 `watch(props.activityCode)`** ——
外壳从活动 A 切到 B 时若组件没重新挂载，只写 onMounted 会让配置落到上一个活动下。
**外壳自己也踩过同族的坑**：从「继续配置」的 URL 导航回无 query 的菜单入口，
路由 path 没变、组件不重挂载，结果点开菜单看到的是上一个活动停在第二步的配置页。
现已 `watch(route.query)` 与 onMounted 共用 `initFromQuery()`。

### 依赖倒置：ActivityRefProvider

三个玩法表的 Dao 都在 **solvela-marketing**，而 `ActivityConfigService` 在 **common-api**（上游）， 直接引用会造成循环依赖。故
common-api 定 `ActivityRefProvider` 契约、solvela-marketing 提供三个实现。
新增第四种玩法只需加一个实现类。

**装配刻意用 `List` 注入 + 各实现自报 `supportType()`，不用 `Map<Enum, Provider>`** ——
本项目已因「枚举键 Map 用字符串 get」踩过三次静默失效（铁律见 §5）。
用 `ObjectProvider` 而非直接注入 `List`：common-api 单独跑时一个实现都没有，直接注入会启动失败。

### 奖品类型只开放 4 个

`LOTTERY` / `CUSTOM` 两层派发策略都没有（`@PrizeStrategy` 与 `@AssetStrategy`
各只注册了 SCORE/BALANCE/COUPON/PHYSICAL），已从 `PRIZE_TYPE_OPTIONS` 过滤。
**刻意不给它们补优惠配置** —— 缺的是策略不是配置，补了只会让运营建出一个必然发不出去的奖品，
而失败在 `AFTER_COMMIT` 里对主链路无感。补齐策略后把常量里的 `dispatchable` 改 `true` 即可。

### ✅ 验收 27/27 全部通过

**第 15 条（受限角色权限）已于 2026-07-29 实测通过**：用一个非超管、且恰好只有
`activityConfig:query` 的账号跑 —— **查询通过**，新增/向导创建/删除全部被拒。
「查询通过」是决定性的那条：若 Controller 里仍是修复前的串，它必然也被拒。
界面层同样成立（无活动菜单、直接访问路由 404）。

这也印证了权限串核对的结论（方案 §11）：

菜单侧 `373~376` 功能点**早就存在且串是对的**，错的一直是 Controller
（它要 `:addProposal` 和不带前缀的 `:query`）。两边永远对不上 ——
**任何受限角色即便被正确授予 `activityConfig:add` 也依然会被拒**，
这不是「权限配得不够」，是「权限根本无法配对」。
长期没人发现的原因也实测到了：`admin` 的 `administratorFlag = true`，超管绕过全部权限校验。

### 🔴 验证时发现的基座缺陷：改角色权限不清缓存

给角色授权后，`getLoginInfo` 立刻能看到新权限，但**调接口仍被拒** ——
`@SaCheckPermission` 走的是 `LoginManager.getUserPermission()`，那是
`@Cacheable(USER_PERMISSION)`；而清缓存的 `clearLoginEmployeeCache` 只在**登出**与
**员工信息变更**时调用，**`RoleMenuService.updateRoleMenu` 不清缓存**。

**运营侧表现**：管理员加了权限，对方刷新页面菜单都出来了，一点功能却还是「没有权限」，
界面上看不出任何线索。**必须登出再登录才生效。**
属 Solvela 基座行为，非本次改动引入；修法是 `updateRoleMenu` 成功后对该角色下的员工
调一次 `clearLoginEmployeeCache`。本次未擅自动（影响面超出本需求）。

### 遗留
- `t_promotion_config` 目前只有 4 条「联调-」前缀的压测造数，生产需运营自建。
- 侧边栏偶发一条 antd 内部报错 `Cannot destructure property 'prefixCls' of useInjectMenu(...)`：
  排除了菜单树结构/我加的菜单/初次加载/展开收起/两种导航方式/页签恢复/四种布局共八个假设，
  **本会话完全无法复现**，菜单渲染 100% 正常、零功能影响，根因未定位。
  顺手修了唯一确证的真问题：四套 `sub-menu.vue` 用 `!item.children` 判空（空数组是 truthy，
  会渲染出空 `a-sub-menu`，正是该报错的形状），已统一为 `$lodash.isEmpty`，与顶层同口径。

---

## 4.6 列表页与编码体系的通用改造（2026-07-30）

这几项不属于某一个模块，但**动任何列表页或编码前都要知道**。

### ① 业务编码加了 1 位类型前缀

```
A=活动  P=奖品  D=奖池  T=任务模板  L=彩票
```

实现是「 **1 位前缀 + 9 位随机，总长仍是 10**」，见 `SolvelaCodeUtil.BizCodePrefix`。
这样 `BIZ_CODE_REGEX = ^[A-Z0-9]{10}$` **一个字都不用改**，前后端所有 `@Pattern`、
`regular.bizCode`、唯一索引全不受影响，**存量数据也不需要迁移**
（实测：老编码 `VJR856K8DW`、数字开头的 `12278CBYW7` 依然通过格式校验）。

🔴 **前缀只在生成时加，绝不参与校验。**
存量编码没有前缀，一旦写进 `@Pattern`，老活动一打开编辑就会被判非法；手输的编码也不该被拦。
**前缀是给人看的线索，不是给代码依赖的契约** ——
任何「按前缀判断业务类型」的写法都会立刻把它变成必须迁移存量的硬约束。

> 澄清一个直觉误区：加前缀让随机位从 10 降到 9，碰撞空间**小了 36 倍**（不是变大）。
> 在本系统量级下仍可忽略，且 `generateUniqueBizCode` 本就带判重重试。
> 真正的收益是**跨业务类型不会看混**，不是熵变大。

> 评估过用系统自带的单号管理（`t_serial_number`），**不采用**：它产出递增有序号
> （会泄露业务体量且可枚举，与彩票用 FPE 防猜号的取向相反）、
> `SerialNumberIdEnum` 是硬编码枚举（加业务必须改 Java 发版，「没有新增页面」是设计如此）、
> 格式长度不定会破坏 `BIZ_CODE_REGEX`。它适合订单号/合同号那类**需要人工对账**的单据号。

### ② 列表页「列设置」此前是坏的，已修

**根因不是「默认显示所有列」，而是 23 个页面传的是 `:tableId="null"`**，
而 `TableOperator` 无 tableId 时直接 return —— 点开「列设置」只会看到一段
「您尚未设置 tableId」的说明，**功能压根没接上**，既调不了也不持久化。

三处改动：

- `solvela-table-column-merge.js` 里 `fontColumn.showFlag = true` 是硬编码，
  页面声明的 `showFlag:false` 会被覆盖。改为按优先级解析：
  **页面显式声明 > `DEFAULT_HIDDEN_COLUMNS` 全局名单 > 显示**。
- `table-id-const.js` 新增 `MARKETING` 段（50000 起）覆盖营销中台 20 张表，
  并导出 `DEFAULT_HIDDEN_COLUMNS`（createBy/updateBy/updateTime/deletedFlag/scriptId/ext）。
  **刻意不含 `createTime`** —— 创建时间是排查问题最常用的一列。
- 23 个页面接上对应常量。

🔴 **表格 id 一经分配不要再改** —— 它是 `t_table_column` 里用户列设置的存储键
（后端 `selectByUserIdAndTableId`，每用户每表），改了等于把所有人已保存的设置作废。

### ③ 活动 / 奖品列表改为「启用 / 禁用」，删除入口已移除

活动的生命周期用状态表达，不再提供删除 —— `t_prize_log.activity_code` 是发奖流水的追溯键。
`delete` 接口与删除守卫**保留为 API 层兜底**。

- **活动**：管理端只有 1-启用 / 2-禁用；库里历史值 0 一律按禁用展示，打开后变 1，**永不写回 0**。
  「活动有没有开始」是业务层按起止时间实时算的，不是后台开关。
  **启用前校验玩法完备度**（复用 `ActivityRefProvider.checkConfigured`），禁用不校验。
- **奖品**：1-启用 / 0-禁用（⚠️ 与活动的 2 不同，别混用两套常量）。
  审批模式也改成了开关，排序权重支持行内编辑。

⚠️ **连带影响**：删除守卫第一层判的是 `status == 0`，而开关只在 1↔2 之间切换 ——
**任何被拨动过一次开关的活动此后都不再满足删除条件**。当前形态下自洽（界面本就不提供删除），
但将来若恢复删除能力，应改用「下游有无发奖记录」判定，而不是靠状态倒推。

### ④ 奖品域权限串也是错的，已修

与活动域是**同一处复制粘贴错误**：Controller 上写的是 `:addProposal` 和不带前缀的 `:query`，
而库里 menu 321~324 的串一直是对的（`prizeConfig:query/add/update/delete`），两边永远对不上。

🔴 ~~这个错误模式可能还散布在其他生成器产出的 Controller 里，值得全工程扫一遍。~~
**✅ 2026-08-01 已扫完并修完营销中台部分**（v3.46.0 + v3.48.0）：

- 共 **21 个 Controller、96 处** `@SaCheckPermission` 缺模块前缀
  （任务域 4 个 → v3.46.0；draw/lottery/ledger/risk/prize 共 17 个 → v3.48.0）。
  全工程现已无「以冒号开头」的裸串。
- 对照 `t_menu` 后发现：**生成器产出的 CRUD 功能点是齐的，缺的全是后来手写的业务动作**。
  这个分布本身很说明问题 —— 功能点跟着生成器走，凡是人手加的接口都没人记得补。
  补了 5 个：`prizeLog:approve` / `proposalRecord:approve` / `drawPrizeLog:execute` /
  `prizePoolConfig:workbench:save` / `lotteryTicket:query`。
- 🔴 **其中两条最要命的是两套审批**：在此之前 `prizeLog:approve` 与 `proposalRecord:approve`
  在库里连功能点都不存在 —— **除了超管，没有任何角色能被授予审批权限**，
  审批工作台对受限角色是彻底不可用的。
- ⚠️ 三个工作台/向导页面（435 任务向导、436 模板设计器、437 抽奖工作台）此前
  **一个功能点都没有**：受限角色能看见页面、点保存必被拒、界面上没有任何线索。

### ⑤ 🔴 同一个 `addProposal` 错误在**基座框架**里也有（未动，待决策）

全工程扫描顺带查出：`solvela-admin` / `solvela-base` 的 **16 个 Controller** 同样把「新增」写成了
`xxx:addProposal`，覆盖 `system:department` / `system:employee` / `system:menu` / `system:role` /
`system:role:employee` / `oa:bank` / `oa:enterprise` / `oa:invoice` / `oa:notice` /
`support:config` / `support:dict` / `support:dictData` / `support:helpDoc` / `support:changeLog` /
`category` / `goods`。

**已核实这 16 个在 `t_menu` 里都有精确的 `xxx:add` 对应项**（逐条比对过），
所以修法是纯机械的、且可验证。影响面：**系统管理 / OA / 支撑各模块的「新增」按钮，
对所有受限角色都是失效的**（超管照旧无感）。

另有 4 个动作在库里没有功能点：`support:reload:update`（同前缀只有 query/result）、
`system:message:query/delete/send`（该前缀一个功能点都没有）。

**未擅自动的理由**：这是 Solvela 上游基座而非本项目二次开发的部分，改了会与将来的版本升级冲突，
且超出本轮「营销中台权限串」的范围。要修的话是 16 行替换 + 4 个功能点，成本很低。

### ⑤ 前端「看到的值 ≠ 模型里的值」——本会话踩了三次

| 场景 | 表现 |
|---|---|
| `a-input-number` 程序化赋值 | DOM 显示改了，Vue 模型没变 |
| `form_input` 对 `RangePicker` 无效 | 它管理自己的内部状态，必须走真实面板点选 |
| `@blur` 里读 `e.target.value` | 界面显示改了、请求却没发出去 |

**统一做法**：用 `@change` 维护本地草稿，提交时只读草稿、不碰 DOM。
另外 **`@pressEnter` 在这个版本的 `a-input-number` 上不触发**，要用原生 `@keyup.enter`。

---

## 4.7 前端依赖升级善后与生成器产物缺陷（2026-08-01）

这一节记的是**前端侧**的坑，此前交接文档几乎没覆盖过。

### ① 依赖升级把 dev server 打挂 —— 但根因不是升级

报错是 `NoResourceFoundException: No static resource .`，看着像后端问题，实际是
`vite.config.js` 的 proxy：

原本 proxy 写在 `server.server` 下，**嵌套层级不对、Vite 根本不识别，等于没配** ——
而「没配」恰好是对的：`axios.baseURL` 直连 `VITE_APP_API_URL`(1024)，整条链路不经过代理。
有人把它「修正」到正确层级后 proxy 生效，而模式是 `'/'` **匹配所有请求**，
首页 HTML、`/@vite/client`、每个源码模块全被转发到后端，于是白屏。

🔴 **这个配置长得像 bug，却是靠「错着」才对的。** 现已还原并加注释，别再「顺手修」。
真要加代理必须给具体前缀（如 `/api`）并同步改 axios 的 baseURL。

### ② 三个问题层层遮挡，且前两轮控制台一条错都没有

修好 proxy 才看得见 `main.js` 500，修好它才看得见 `router/index.js` 500：

| 依赖 | 变更 | 影响 |
|---|---|---|
| `vue3-json-viewer` 2.3.1→2.4.1 | CSS 改名 `dist/index.css` → `dist/vue3-json-viewer.css` | ⚠️ 它自己 package.json 的 `style` 字段写的 `dist/style.css` **根本不存在**，别信 |
| `nprogress` 0.2.0→1.0.0-1 | CSS 挪进 `css/` 子目录 | 已借机整个替换掉，见 ③ |

**为什么没有报错**：`main.js` 顶层用了 `await`，模块解析失败时整个入口静默中断。
排查手法：在页面里 `import('/src/main.js')` 把错误逼出来，再逐个 `fetch` 依赖模块定位。

### ③ nprogress 已替换为 `src/lib/progress-bar.js`

项目只用到 `start`/`done` 两个 API，为两个函数扛一个停更十年的包不划算。
**新实现刻意不引任何 CSS 文件、样式由 JS 注入** —— 从根上消灭「依赖改 CSS 路径把构建打挂」
这一整类问题（本次已踩两次）。

顺带比 nprogress 好一点：**延迟 120ms 才显示**，路由在窗口内切完就压根不出现，
避免 SPA 快速跳转时「闪一下」。

> 供应链核对结论：nprogress 包本身是正品（原作者、原仓库、零依赖、无 install 钩子），
> 问题只在于 `1.0.0-1` 是预发布版 —— 其 CHANGELOG 首行就写着 `1.0.0 - unreleased`。

### ④ 🔴 生成器产物的三类系统性缺陷

这三类散布在**所有**生成器产出的页面里，动任何生成器页面前先看这里：

**(a) 用了组件却没 import** —— 全项目 21 处用 `SolvelaEnumSelect`，1 处漏导入
（`lottery-record-list`），运行期报 `Failed to resolve component`，组件不渲染但页面其余正常，
所以能潜伏很久。已修。

**(b) `enum-name=""` 空串** —— 组件渲染后去查一个空名字的枚举，报
「无法找到变量名称：」。**根因有两层**：生成器默认给空串；且 `constants/index.js`
是 `SolvelaEnum*` 查枚举的唯一来源，而 lottery 常量从未被引入过。

已补彩票模块两个（`WIN_STATUS_ENUM` 新增 + `LOTTERY_STATUS_ENUM` 本就有只是没注册）。
⚠️ 注册时**具名引入**，别 `...lotteryConst` 整包展开 —— 那里面还有
`MIN_NUMBER_LENGTH`(数字)、`prizeIcon`(函数) 这类非枚举，会污染枚举查找空间。

**仍剩 6 处**，分两类：`prizeCode`/`lotteryCode` 2 处应改为**接口驱动下拉**（铁律 8，
不是静态枚举）；`assetType`/`transactionType`/`bizType`/`status` 4 处需各补枚举常量。

**(c) `tenantId` 被当成表单字段** —— 租户ID 是 varchar、DDL 有默认值 `'0'`，
前端本就不该让人填。生成器不仅生成了它，还在 `rules` 里设了必填。

其中 7 处是 `SolvelaEnumSelect` 形态： **必填 + 空下拉 = 表单根本提交不了**。已清（v3.50 同批提交）。
🔴 清理必须**两侧一起改**：7 个后端 AddForm 全都有 `@NotBlank(tenantId)`，
只删前端会从「客户端校验过不去」变成「提交后 400」。
按奖品域先例：保留字段、去掉 `@NotBlank`、`@Schema` 改为「不传落库取默认值 '0'」。

**仍剩 9 处**是 `a-input` 形态 + rules 必填。它们能手打值提交，属「能用但不该有」，
优先级低于上面那 7 处。

---

## 5. 待办（下一步优先级）

### ✅ 依赖瘦身：guava / hutool / POI 全家桶 / collections4 + text 均已移除

| 轮次 | 省下 | 详见 |
|---|---|---|
| guava + hutool（08-06 ~ 08-08） | ~5.6MB | §10.1 / §10.3 |
| **POI 全家桶**（08-08，随 SonicExcel） | **18.66MB** | §11 |
| commons-collections4 + commons-text（08-08） | 1.11MB | §10.5 |
| **累计** | **约 25.4MB** | 当前 202 个 jar / 97.16MB |

`commons-lang3` / `commons-io` **不要动** —— 判据与"这条判据会随依赖变化翻面"的说明见 §0 铁律 4 与 §10.0。

### 🔴 2026-08-02 升级轮次的遗留（见 §9.7 详情）
1. `t_task_prize_mapping` 有 2 行审计时间为 NULL，订正 SQL 已备好但未执行（§9.3）
2. 上线前必须清一次 Redis —— 序列化格式变了（§9.2）
3. **Excel 导入导出跑一次真实冒烟** —— 底层已于 2026-08-08 整体换成自研 SonicExcel（POI 全摘，
   见 §11）。框架侧有 60 余条测试，但「连着 DB + Redis 的真实业务导入导出」始终没被自动化覆盖
4. ~~活动大屏的落地方案未定~~ —— **✅ 2026-08-03 已落地为首页**，见 §9.5。
   连带产生的两个小尾巴：
   - ~~首页原来那 5 个组件已无引用方~~ —— **✅ 已删（`8fb7d693`）**，
     连同 login2/login3 两套备用登录皮肤与 18 张孤儿图片；
   - 大屏没做受限角色实测：开发库 `t_role` 里没有 `role_code='ADMIN'` 的角色，
     `v3.52.0.sql` 的授权语句更新 0 行。要验就手工给某个角色勾上 460 再用该角色登录
     （注意基座缺陷：改完角色权限**必须登出重登**才生效）。

### ⭐ 当前遗留清单（**2026-08-01 更新**，按优先级）
全链路（抽奖/彩票 → 派发 → 提案 → 风控预算 → 资产到账）四种资产类型均已跑通并验证，
活动创建向导 P0~P3 亦已完成（§4.5），剩下的是：

**✅ 任务模块运行态 P0 + P0.5 已完成**（2026-08-01，详见方案 §9）
链路已通：`POST /taskEvent/report` → 独立有界线程池 → 查订阅 → 取/建记录 → 策略推进 →
阶梯发奖 `publishEvent(UserPrizeEvent)` → 复用既有 consumer→risk→ledger 链路 → 资产真实到账。

**接手时必须知道的四条**：
1. **发奖幂等键是 `recordId:stageLevel`**（`TaskConst.buildSourceBizId`）。
   只传 recordId 的话，`t_prize_log.uk_external_biz` 是单列全局唯一、`PrizeDispatchHandler:61` 靠
   `catch(DuplicateKeyException)` 防重，**阶梯任务第二档会被当成重复投递静默吞掉**，零并发必现。
2. **运行态订阅判据是「status != 3」+ 时间窗，不是「status == 2」**。
   全工程没有任何地方把 status 从 1 改成 2，判 `==2` 会让所有任务永不触发且毫无线索。
3. **`t_task_record_flow` 是这条链路唯一的可观测面**。异步 + AFTER_COMMIT 让异常不再上抛，
   排查一律先查这张表的 `discard_reason`，别翻日志。
4. **STREAK 的 `period_key` 恒为 NONE**，与 COUNT 是两套周期模型，动它前读方案 §2.1.1。

**任务模块本轮已全部补完**（P0 / P0.5 / P1 / 契约校验 / 前端 / 人群 / 频次），
单测 73 + 集成验收 25 全绿。**剩余缺口只有两个**（方案 §9.5）：
- `rule_script` 第三层脚本仍未执行 —— 不影响已上线玩法，但遇到前两层表达不了的需求
  只能改 Java，没有「写段脚本先顶上」的退路。**建议真遇上一两次再做**，
  现在做等于为想象中的需求设计；
- 记录自动过期 —— 无扫描任务，`status=3 已过期` 永不出现。功能上不缺
  （到期靠任务配置的时间窗停止收事件），只是历史记录会一直挂在「进行中」。

其它挂着的：异步入口队列打满验证（拒绝路径已实现并留痕，但没真压满过）、
`is_high_frequency` 的 Redis 路由优化（字段已建、实现未做）、MQ（按决策不在本期）。

✅ **实施中查出并修复了两个「公共派发链路」的既有缺陷**（v3.45.0，方案 §9.4；
抽奖/彩票同样受影响，不是任务模块引入的）：
① 四个 `@PrizeStrategy` handler 都硬编码 `sourceType = LOTTERY_DRAW`，
**任务发的奖在提案表里被记成「彩票抽奖」**，而 DDL 注释与 `@Schema` 写的一直是 TASK/DRAW/MANUAL
—— 代码与契约从第一天起就对不上。改为 `ProposalSourceResolver` 从
`t_activity_config.activity_type` 推导（**不动跨域契约**：这个值查一次就有，
不必新增事件字段 + DDL 列 + 回填存量三样只为搬运它）。
② `CouponAssetHandler:59` 把 `couponName` 写成了 `proposal.getRemark()`，
实测发出去的券全叫**「提案生成成功」**——用户可见。根因是依赖方向从「账务→营销」翻转后
**名称这条信息没了搬运通道**；已给 `t_proposal_record` 加 `asset_name` 由营销侧下传。
**存量 `source_type` 刻意不迁移**（它在幂等键 `uk_prop_source` 里，批量改等于改幂等命名空间）。

⚠️ 另有两个线程池隐患（方案 §4.7）：`AsyncConfig` **没设 `queueCapacity`，队列是无界的**
（拒绝策略从不触发，积压全堆在堆内存），且 `GlobalEventDispatcher`（承载抽奖/彩票派奖）用的就是它。
任务事件因此**没有复用**这个池，而是新建了 `task-event-executor` 有界池 + `AbortPolicy` 做舱壁隔离。

0a. **生成器产物的三类缺陷仍有残留**（详见 §4.7④，都是「配得出来但不对」的形状）：
   空 `enum-name` 剩 6 处（2 处应改接口驱动下拉、4 处需补枚举）、
   `tenantId` 以 a-input 形态残留 9 处（能提交，属「能用但不该有」）。
   🔴 **动任何生成器产出的页面前先读 §4.7④**，否则会重复踩同样的坑。
0c. **基座 16 个 Controller 的 `addProposal` 权限串仍是错的**（详见 §4.6⑤，已核实每个都有
   精确的 `:add` 对应项，修法纯机械）。影响：系统管理/OA/支撑各模块的「新增」按钮
   对所有受限角色失效。属上游基座，改了会与版本升级冲突，**未擅自动**。
0. **基座缺陷：`updateRoleMenu` 不清权限缓存** —— 改完角色权限必须登出重登才生效，
   否则「菜单出来了但功能没权限」且无任何线索。详见 §4.5。修法明确，未擅自动。
0b. **`LOTTERY` / `CUSTOM` 两种奖品类型缺派发策略** —— `@PrizeStrategy` 与 `@AssetStrategy`
   各只注册了 SCORE/BALANCE/COUPON/PHYSICAL。已从前端可选项过滤掉（`dispatchable` 标志），
   补齐两层 handler 后把标志改 `true` 即可。**不要用「补优惠配置」来解决** ——
   缺的是策略不是配置，补了只会让运营建出必然发不出去的奖品。
1. **券的类型与有效期是占位常量**（`GENERAL` / 30 天写死在 `CouponAssetHandler`）——**等产品规则**。
   注：`UserPrizeEvent.validUntil` 与 `t_prize_log.valid_until` 都预留了字段却从没赋过值。
2. **实物「用户补填收货信息」的 C 端接口不存在** —— 履约单能建立但地址补不了，三段式履约到第②步就断了。
3. **`asset_ref` 目前传的是 `prize_code`** —— 契约方向已经对了（营销 → 账务单向），等券模/SKU 体系建起来换个值即可，账务侧代码一行不用动。
4. **预算是「下发时先到先得」而非「提案时预留」** —— 不会超发，但运营可能批准了却发不出。是否改为预留式属产品决策。
5. **`t_promotion_config` 与活动的映射关系未建** —— 若要「按活动统计预算消耗」，应在营销域记住 activity → promotionConfig 的映射（提案域刻意不引入 activity_code，理由见铁律与提案表设计）。
6. QPS 约 141；数据源 `max-active: 10` 对 100 并发是瓶颈，想继续压这个比异步化更值得动。
7. TICKET 抽奖券资产建模。（`reset_period` 周期重置已于 v3.64 落地，见 §3.6；
   抽奖成本随 `cost_asset_type`/`cost_value` 一并移交上游业务，TICKET 已不再是抽奖引擎的待办）
8. ~~前端整体 `vite build` 是坏的~~ —— **✅ 2026-07-28 已修**（删掉了 `lottery-record-list.vue` 对
   从未生成的 `lottery-record-form.vue` 的引用；同理删除了 `prize-log-form.vue`：
   这两张都是审计流水表，本就不该有新增/编辑弹窗）。
9. 存量脏数据（压测产物，可选清理）：`t_member_coupon` 48 行 `create_time` 为 NULL、`t_member_wallet` 245 行时间倒挂，均为对应修复上线前产生。

### 压测结论（2026-07-26 首轮，100并发×500次）
**防超发验证通过**：无任何奖项 `used_stock > total_stock`；兜底奖项「谢谢参与」在约 339 次争抢下 **恰好命中 100 次 = 其全部库存**，不多不少；流水中奖数与 `used_stock` 逐项一致；500 条流水 / 500 个 traceId / 500 个用户全部对得上；单人限领（user_max_count=1）违规 0 行。双层扣减（Redis Lua + DB 条件更新）成立。
- 本轮未验证「小库存热门奖项」场景：iPhone 概率仍是 0.02%，500 次抽中 0 次。想单独验证它需把库存改 5、概率改 50%，**并把兜底奖项改成不限量**（否则兜底先干涸，后续全返回 NoStock，结论被污染）。
- RT p50=548ms / p95=1001ms、QPS 150 —— 且这还是**派奖链路实际没跑成**时的数字，修好派发后 RT 会更高，待办里给 dispatcher 挂 `@Async` 的必要性上升。

### 🔴🔴 派发链路根因：GlobalEventDispatcher 路由恒 null（已修，待验证）
`BaseBizEvent.category` 是 **String**，而 `GlobalEventDispatcher.routeTable` 是 `Map<EventCategoryEnum, RouteTarget>`。
`Map.get(Object)` 不做类型检查 → `routeTable.get("PRIZE")` **编译通过、运行恒 null** → 每次只打一行
`【事件分发】未找到分类 [PRIZE] 的处理器` 就返回。**整条奖励派发链路从来没通过**，`PrizeDispatchHandler` 一次都没被调用。
- 修复：`dispatch()` 里先 `resolveCategory(String)` 转成枚举再查表，非法值只告警不抛（避免脏事件打断监听器）。
- 这个坑的隐蔽性在于三重静默叠加：① 泛型 Map 的 get 不校验类型；② 派发在 `AFTER_COMMIT`，主事务已提交，抽奖照样返回 200 中奖；③ dispatcher 自己 `catch (Exception)` 兜底。**类似的「枚举键 map 用字符串查」在别处也要排查。**

### 🔴 压测暴露的派发链路缺陷（v3.36.0.sql，已执行）
263 次中奖但 `t_prize_log` **0 行**，派奖整条没落地。两个 DDL 缺陷：
1. **`t_prize_log.fail_reason` 是 NOT NULL 且无默认值**。`PrizeDispatchHandler.buildPrizeLog()` 正常发奖时不会设 failReason，MyBatis-Plus 省略 null 字段，MySQL 开着 `STRICT_TRANS_TABLES` → 每次插入必抛 `Field 'fail_reason' doesn't have a default value`。已改为可空。
2. **`uk_external_biz` 唯一索引不存在**。`PrizeDispatchHandler` 用 `catch (DuplicateKeyException)` 做跨系统防重，抽奖侧把 traceId 当 sourceBizId 传进来，但索引压根没建，防重一直是空转（事件重投会重复发奖）。已补。
- **为什么一直没被发现**：派发挂在 `@TransactionalEventListener(AFTER_COMMIT)`，抽奖事务早已提交，插入失败只在日志里报错，对抽奖主链路完全无感。**教训：AFTER_COMMIT 的监听器出错是静默的，验收时必须单独查下游表有没有数据，不能只看主链路返回 200。**
- 这两条 DDL 已在开发库执行完毕（`fail_reason` 已可空、`uk_external_biz` 已建），但它们只是**第二层**问题；第一层是上面那个路由恒 null，两个都修完才可能出数据。

### 压测第4轮（2026-07-26）：抽奖层定案，问题下沉到派发策略层
**抽奖层全部通过，可以结案**：
- `t_prize_log` **241 行 == 中奖数 241 == `used_stock` 总和**，`external_biz_no` 去重 241 == 行数（无重复派发）。
- 100积分、谢谢参与两项均 **恰好 100/100 抽空**，无一超发；`traceId` 500 个不重复。
- 88红包（`approve_mode=1`）正确停在 `approve_status=1 / status=0` 挂起等审批。
- RT p50=782ms / p95=1609ms、QPS 105 —— 比第3轮（派发空转时）明显变慢，**这就是派发同步执行的成本**，`@Async` 那条待办可以定了。

**新暴露：免审的 232 条全部 `status=2` 失败**，`fail_reason = 不支持的奖品类型: SCORE / COUPON`。两个原因叠加：

1. **同一个「枚举键 map 用字符串查」的坑，在派发层又出现了两次**（均已修）：
   - `PrizeStrategyFactory.getHandler(String)` 查 `Map<PrizeTypeEnum, IPrizeHandler>`
   - `AssetStrategyFactory.getHandler(String)` 查 `Map<PrizeTypeEnum, IAssetHandler>`
   连同 `GlobalEventDispatcher` 一共**三处**，全工程 `grep "Map<[A-Za-z]*Enum,"` 已扫过，现在只剩这三个 map，都已加 `resolveXxx(String)` 显式转枚举。**这是本项目最容易复发的缺陷模式，新增策略工厂时务必照此写。**
2. **派发策略本身覆盖不全**（已补 SCORE/COUPON，见下节）：
   - `@PrizeStrategy`（consumer 层，`doDispatch` 实际用的就是它）：**只有 BALANCE**
   - `@AssetStrategy`（ledger 层）：BALANCE / COUPON / PHYSICAL，**缺 SCORE**
   - 所以 SCORE 两层都没有，COUPON 只有 ledger 层没有 consumer 层。修完 map 的坑后，SCORE/COUPON 依然会失败，需要补 handler。

### ✅ 压测终轮（2026-07-26，干净基线）：抽奖 + 派发 端到端全绿
100并发×500次，全链路逐项核对通过：

| 校验 | 结果 |
|---|---|
| 防超发 | 无一奖项 `used_stock > total_stock`；兜底「谢谢参与」**恰好 100/100 抽空** |
| 三处计数一致 | 抽奖中奖 **258** == `t_prize_log` **258** == `used_stock` 总和 **258** |
| 幂等 | `traceId` 去重 500（全部请求）、`external_biz_no` 去重 258（全部中奖，无重复派发） |
| SCORE 正常入账 | 100积分 94 条 `approve_status=0 / status=1`，提案 94 条、`promotion_value=1.0000` |
| SCORE 零值短路 | 谢谢参与 100 条 `status=1` 成功，**产生提案 0 条** —— 设计如期 |
| COUPON | 52 条 `status=1`，提案 52 条、`promotion_value=20.0000` |
| 审批分流 | 88红包(BALANCE)11 条、iPhone(PHYSICAL)1 条，均 `approve_status=1 / status=0` 挂起 |
| 失败 | `fail_reason` **全为空**，零失败 |
| 提案总数 | 146 == 积分 94 + 券 52 ✓ |

**RT 演进（同为100并发×500次，可直接对比派发成本）**：
| 轮次 | 派发链路状态 | QPS | p50 | p95 |
|---|---|---|---|---|
| 第3轮 | 路由恒 null，完全空转 | 241 | 384ms | 501ms |
| 第4轮 | 落 prize_log，但策略全失败 | 105 | 782ms | 1609ms |
| 终轮 | 258 次派发 + 146 次提案真实执行 | **94** | **891ms** | **1602ms** |
派发同步执行让 QPS 掉了 60%。**给 dispatcher 挂 `@Async` 已经有充分依据，建议提到待办首位。**

### 🎉 积分已真正到账（2026-07-26 终验第1轮）
整条链路第一次打到最底层：`t_member_wallet` **97 个 SCORE 账户、总额 97.0000**，`t_member_asset_transaction` **97 条、总额 97.0000**，提案 97 条全部 `status=50 资产下发成功`。97 人 × 1 分，逐项对得上。抽奖层依旧 259 中奖 / 0 超发。

**同时暴露 COUPON 断在最后一米**（已修待验证）：51 条券卡在 `t_prize_log.status=0`，且**一条提案都没有**、`t_member_coupon` 0 行。
- 根因：`CouponAssetHandler` 里那句 `// ... (省略有效期计算等设值)` 省掉的 `coupon_type` / `valid_start_time` / `valid_end_time` 都是 **NOT NULL 且无默认值**，插入必抛 `Field 'coupon_type' doesn't have a default value`。已用 SQL 复现确认。**这是 `t_prize_log.fail_reason` 之后同一模式的第三次**，新建实体前务必先查表里有哪些 NOT NULL 无默认值的列。
- 顺带修了 `coupon_code` 被写成空串的问题：`ProposalRecord` 里没有 `prize_code`，handler 不知道该发哪种券。现在按 `source_biz_id` 回查 `t_prize_log.external_biz_no` 拿到券模编码与名称。
- ⚠️ 券类型与有效期目前是占位常量（`GENERAL` / 30天），**产品规则待定**。`UserPrizeEvent.validUntil` 和 `t_prize_log.valid_until` 都预留了字段却从没赋过值。

### ✅ 预算硬限流验证通过 + 方案A 的副作用已修（2026-07-26）
把积分池 `total_amount` 设成 50 后跑 100并发×500次，积分中奖 100 次：

| 指标 | 结果 |
|---|---|
| `used_amount / total_amount` | **50.0000 / 50.0000 恰好用尽**，一分不超 |
| 钱包 SCORE 增量 | **正好 50** |
| 资金流水增量 | **50 条** |

提案的三段分布把两道防线各自的作用演示得很清楚：
| 提案状态 | 条数 | 拦截者 |
|---|---|---|
| 50 成功 | 50 | — |
| 80 风控拦截 | 43 | `GlobalBudgetRiskFilter` 的**弱校验**（先读后判），快速失败 |
| 70 预算已耗尽 | **7** | 新增的**条件更新硬闸门** |
那 7 条读预算时余量还够、**穿过了弱校验**，最后被 UPDATE 的 WHERE 拦下 —— 没有硬闸门这 7 分就超发了。50+43+7=100 全对得上。

**🔴 同时暴露了方案A 的副作用（已修）**：积分 `t_prize_log` 里 **57 条 status=1 但实际只发出 50 条**，7 条是假成功。
- 原因：下发挪到 `afterCommit` 之后，`addProposal` 早已返回 ok，下发结果再也回不到 `PrizeDispatchHandler` 的调用栈；而 `doDispatch` 在成功分支抢先写了 `status=1`，把引擎随后写的真实状态覆盖掉了。
- 对比：那 43 条风控拦截发生在**事务内**，`addProposal` 返回非 ok → `prize_log status=2` 是对的。只有提交后才发生的失败会错。
- 修法：① `doDispatch` **成功分支不再写 status**（此刻结果还没出来），只有当场失败才落 2；② `AssetDispatchEngine` 在落提案终态时用 `PrizeLogDao.updateStatusByExternalBizNo` 反向回写发奖记录，`WHERE status = 0` 避免覆盖人工订正过的终态。
- 分层上略有妥协（ledger 引擎去改 prize 域流水），但运营看的就是发奖记录，「显示成功、用户没收到」不可接受。两者靠 `external_biz_no == source_biz_id` 这条既定契约关联。
- 若提案进了人工审批池，发奖记录合理地停在 `0-等待执行`，等审批通过后由同一条链路回写。

**修复后复验通过**：积分链路三处状态完全一致 —— `prize_log` 成功 50 / 提案成功(SCORE) 50 / `used_amount` 50.0000 / 钱包增量 50。被拦下的 50 条也如实记为失败，且 `fail_reason` 能区分是哪道防线拦的（42 条「活动过于火爆…」来自风控弱校验，8 条「预算或发放数量已耗尽」来自条件更新硬闸门）。

**🔴 该修复引入的回归（已修）**：谢谢参与 100 条卡在 `status=0`。
0 值奖品走 `ScoreHandler` 短路直接返回 ok、**不生成提案**，引擎不会跑，而改动后 `doDispatch` 成功分支又不写状态了 —— 状态永远悬着。
修法：`doDispatch` 增加 `isNoDeliveryNeeded(prizeLog)` 判定（价值 <= 0），命中就当场落 `status=1`。
判定刻意放在 `doDispatch` 而不是各 handler 里：**审批通过也会重入 doDispatch**，两条入口都得覆盖。
**通用教训：把「谁来写终态」从同步改成异步时，务必列全所有不会进入异步链路的分支（0 值短路、审批挂起、handler 直接返回…），否则总有几条会永远停在中间态。**

**终态复验全绿**（100并发×500次，积分池限额 50）：
```
0ZXXLZ0RZ1 券      status=1  46      ← 全部成功
2CJGRUOMUZ 谢谢参与 status=1  100     ← 回归已修，不再卡 0
G3TWEXS5L7 积分     status=1  50      ← 恰好等于预算上限
G3TWEXS5L7 积分     status=2  45      ← 37 条风控弱校验 + 8 条硬闸门
FBJR9BAIWI 88红包   status=0  8       ← 唯一的 0，且只因等审批
```
四处状态一致：`prize_log` 成功 50 / 提案成功 50 / `used_amount` 50.0000 / 钱包增量 50（346→446 含券轮次）。
**验收判据成立：`status=0` 只对应「等人工审批」，不再有任何其它原因导致的中间态滞留。**

### t_promotion_config：预算硬扣减 + 奖品配置级联下拉（2026-07-26，待验证）

**⚠️ 澄清一个误解**：这张表**本来就有 `prize_type` 列**（SCORE/BALANCE/COUPON/PHYSICAL），已经是按资产类型分类的，所以「下拉按奖品类型过滤」不需要加任何字段。经确认后未改表结构。

**1. 预算真实扣减（风控最后一道闸）**
- `PromotionConfigDao.deductBudget(id, amount, quota)`：把「够不够」压进 UPDATE 的 WHERE，一条 SQL 完成校验+扣减，靠行锁防并发超发。`total_amount / total_quota = -1` 表示不限，条件直接放行。
- `releaseBudget(...)`：下发失败时把占用还回去（CASE WHEN 兜底不扣成负数），否则预算只减不加，跑一段时间水位虚高到发不出奖。
- 接入点在 `AssetDispatchEngine`：`30→40 状态闸门` → **预扣预算**（扣不动直接落 70「预算或发放数量已耗尽」）→ 下发 → 成功落 50 / 失败回滚预算并落 70。异常分支用 `budgetDeducted` 标志位控制，**避免在预扣之前就出错时把没占用的预算凭空还回去**。
- 注意：`GlobalBudgetRiskFilter` 仍是「先读后判」的弱校验，高并发下读到的余量早已过期 —— 它只负责快速失败，**真正防超发的是这里的条件更新**，两者不重复。

**2. 奖品配置的优惠配置级联下拉**
- `GET /promotionConfig/optionList` → `PromotionConfigOptionVO`（record），返回启用中的全量配置。刻意不做服务端按类型过滤：配置总量不大，前端一次拉全量、按 `prizeType` 本地分组过滤，省掉切一次类型打一次接口。
- 下拉标题直接带预算水位与审批档位（`名称｜预算 已用/总额｜数量 已用/总量｜免审` ），运营选的时候就能看出这个池子还发不发得出来。
- `prize-config-form.vue`：资产类型从自由文本框改成枚举下拉（级联的前提），优惠配置改成按类型过滤的下拉；换类型时自动清空已选的优惠配置；该类型下无可用配置时给出明确提示。
- 枚举收敛到 `constants/business/prize/prize-config/prize-config-const.js` 的 `PRIZE_TYPE_OPTIONS`，取值对齐后端 `PrizeTypeEnum`。
- 前后端都把 `prizeType` / `promotionConfigId` 设为必填（表里 `promotion_config_id` 本就是 NOT NULL，不填发奖时会被判「资产配置异常」）。

**3. 服务端重算类型一致性（铁律2）**
`AssetDispatchEngine` 是按**优惠配置的 prizeType** 选发货策略的 —— 积分奖品若挂了优惠券的配置，用户实际会收到一张券。前端级联只是防呆，`PrizeConfigService.add/update` 现在都会重校验两者类型一致，绕过页面直接 POST 也穿不过去。

### 审批链路补齐（2026-07-26）：`approve()` 此前根本不存在
`ProposalRecordService` 里那句「等待财务人员在后台调用 approve() 接口」是又一处「注释即意图」—— Controller 里只有生成器产出的五个 CRUD，审批功能从未实现。

**⚠️ 本系统有两套并存的审批，语义不同、可叠加，接手时务必分清：**

| | 判定字段 | 语义 | 未通过时卡在 |
|---|---|---|---|
| **prize 域** | `t_prize_config.approve_mode` | 「这个奖该不该发给这个人」运营视角 | `t_prize_log.approve_status=1`，**不生成提案** |
| **提案域** | `t_promotion_config.review_level` | 「这笔钱该不该出」财务视角 | `t_proposal_record.status=10/11` |

压测里 BALANCE/PHYSICAL 长期停在 `status=0`，正是卡在**第一层**、压根没走到提案域。两层都配了审批就需要运营和财务各批一次。

实现：
- prize 域：`PrizeDispatchHandler.approveDispatch/rejectDispatch` → `GET /prizeLog/approve/{id}`、`/reject/{id}`，通过后直接调 `doDispatch` 走完整派发链路。
- 提案域：`ProposalRecordService.approve/reject` → `GET /proposalRecord/approve/{id}`、`/reject/{id}`，一审通过后按 `review_level` 决定进二审还是放行；放行时同样走 `dispatchAfterCommit`，与方案A 保持一致。
- 两处都用 **条件更新做并发闸门**（`WHERE status=?` / `WHERE approve_status=?`），实测两人同时点通过第二次 `rows=0`
  ，不会重复发放。审批人取 `SolvelaRequestUtil`，不信前端传值。
- `ProposalRecordAddForm` 的 `tradeNo` 去掉 `@NotBlank` 改 READ_ONLY（服务端生成的字段不该要求调用方填）、`quantity`/`tenantId` 的必填也放开。

**✅ BALANCE 审批链路实测通过**：提案 `status=50 资产下发成功`，`t_member_wallet` 出现 BALANCE 账户、余额 88.0000。值类资产全部打通。

### 实物履约是三段式，不是一步到账（v3.37.0.sql）
实物审批通过时抛 `Field 'receiver_name' doesn't have a default value`。
**根因不是漏赋值，是建模缺环**：`PhysicalAssetHandler` 里那句「收件人信息通常在生成提案前就已经存到 proposal.ext 或另外一张收货表里了」描述了一个并不存在的前置环节 —— 整条链路没有任何地方采集过收货信息。

实物与积分/现金本质不同：
① 中奖 → 生成履约单（只知道发什么、发给谁，**不知道寄到哪**）→ ② 用户 C 端补填收货信息 → ③ 运营发货回填物流单号

所以收件三要素必须可空，v3.37 已改，并加 `idx_delivery_status`。
- 「待补地址」**不新增状态值**，仍归 `status=0(待发货)`，用 `receiver_address IS NULL` 判定，避免动状态字典引发连锁。
- **语义提醒**：对实物而言「资产下发成功」= 履约单已建立，**不等于东西已寄出**。已写进 `PhysicalAssetHandler` 类注释。

### 🕐 时间戳错乱排查纪实（2026-07-26，铁律 9/10 的由来）
起点是提案表里一条记录 `update_time` 比 `create_time` **早整 8 小时**。查下来是两个独立缺陷叠加：

**缺陷一：时区不一致**（连接串修复）
MySQL 服务器在 UTC、JVM 在东八区。`create_time` 走 MyBatis-Plus 的 Java 填充(+08)，
而 raw SQL 的 `now()` 与 `ON UPDATE CURRENT_TIMESTAMP` 走 MySQL 会话时区(UTC)。
实测 `t_member_wallet` 已有 **497 行**受影响。修法与 `serverTimezone` 无效的原因见铁律 10。

**缺陷二：双时钟源的亚秒错位**（收敛时钟源）
时区对齐后仍有残留，且分布很说明问题：
| 记录类型 | 倒挂比例 |
|---|---|
| `status=80` 只 insert 从未 update | **0 / 100** |
| `status=50` 被引擎 update 过 | **18 / 48** |
精确指向「create 走 JVM、update 走 DB」这个双时钟源 —— `datetime(0)` 截断时两边舍入方向不同即差 1 秒。
治本是只留数据库一个时钟，做法见铁律 9。顺带消除了多实例部署时各节点 JVM 时钟漂移的隐患。

**排查过程中的两次误判（值得记）**
1. 先以为「去掉自定义 SQL 里的 `update_time = now()`」是修复 —— 错。`ON UPDATE CURRENT_TIMESTAMP` 照样用 MySQL 时钟，去 `now()` 纯属去冗余。**决定性证据**：`MemberWalletMapper` 根本没写过 `now()`，那 497 行照样倒挂。
2. 再以为「只改 `MybatisPlusFillHandler` 停止填充」就够 —— 错，`create_time` 整列变成 NULL。根因是 fill 注解会让字段跳过判空被硬塞进 INSERT（见铁律 9）。
**教训：验证要覆盖到实际写入的那一层。** 第 2 次误判正是因为只验了「手写 SQL 不带这两列时 DDL 默认值会生效」，却没验「ORM 会不会把这一列带上」。

### 🔁 「NOT NULL 无默认值」模式已第 5 次复发
`fail_reason` → `coupon_type` → `fail_reason` 超长 → `trade_no/asset_type` → `receiver_name`。
全部同一件事：**写实体前没对照过表里哪些列是 NOT NULL 且无默认值**，而 MyBatis-Plus 会省略 null 字段 + MySQL 严格模式直接拒绝。
新增/修改实体前，先跑：
```sql
SELECT column_name FROM information_schema.columns
WHERE table_schema='solvela' AND table_name='表名'
  AND is_nullable='NO' AND column_default IS NULL AND extra NOT LIKE '%auto_increment%';
```

### ✅✅ 全链路贯通（2026-07-26 终验，可结案）
100并发×500次，从抽奖判定一路到资产落账，全绿无一失败：

| 环节 | 数据 |
|---|---|
| 抽奖层 | 261 中奖 / 239 库存不足 / **超发 0** / `used_stock` 总和 261 |
| `t_prize_log` | 261 条 == 中奖数；**零 fail_reason** |
| SCORE | 200 条 `status=1`（100 谢谢参与走 0 值短路 + 100 积分正常入账） |
| COUPON | 51 条 `status=1` |
| BALANCE | 10 条 `approve_status=1 / status=0` 挂起等审批（正确，不该自动发） |
| 提案 | SCORE 100 + COUPON 51 = 151 条，**全部 `status=50 资产下发成功`** |
| 钱包 | 本轮 +100 SCORE（累计 197 账户 / 197.0000，与两轮中奖数吻合） |
| 资金流水 | 同上 197 条 |
| **优惠券** | **51 张真实发放** |

券的字段质量也核过：`coupon_code=0ZXXLZ0RZ1`（回查 prize_log 拿到的真实券模，不再是空串）、`coupon_type=GENERAL`、有效期 30 天区间正确、空券模 0 条、有效期倒挂 0 条、51 张对应 51 个不重复提案 ID。

**注意**：谢谢参与 100 条有 prize_log 但**不产生提案**（0 值短路），所以「提案数 = 中奖数 - 谢谢参与数 - 待审批数」，这是设计如此，不是丢单。

### 方案A 已实施：提案与资产下发解耦（2026-07-26）
`ProposalRecordService.dispatchAfterCommit()` 用 `TransactionSynchronizationManager` 注册 `afterCommit` 回调，把 `assetDispatchEngine.execute` 挪到**提案事务提交之后**。
- 提交后已无事务上下文，引擎里每次状态更新都是独立自动提交，**回滚再也冲不掉失败痕迹**：提案一定留得下，失败稳稳落在 70。
- 语义也更顺：提案是「决定发」，下发是「真的发」，本就该是两个阶段。
- 兜底：`isSynchronizationActive()` 为假时（理论上不会，addProposal 带 @Transactional）直接执行，避免静默不发。

### 🔴 「状态永远停在 0」的第二层静默：写库字段超长
报错 `Data truncation: Data too long for column 'fail_reason' at row 1`。
`t_prize_log.fail_reason` 只有 **varchar(128)**，而 `doDispatch` 直接把异常 message 塞进去 —— 实测那条 `UnexpectedRollbackException` 的 message 有 **234 字符**。
致命之处在于**这句就写在 finally 回写状态之前**：一抛异常，`status=2` 根本刷不进去，最终表现就是「状态永远停在 0、fail_reason 为空」，把真正的失败原因彻底吃掉。

- 新增 `SolvelaStringUtil.truncate(text, maxLength)`（null 安全、恰好等长不动、maxLen<=0 原样返回）。
- `PrizeDispatchHandler` 的 fail_reason 按 128 截断；`AssetDispatchEngine` 的 remark 按 255 截断。
- 两处的**状态回写都再包了一层 try/catch**：回写本身失败时至少留日志，不能让兜底动作自己把自己炸掉。
- **通用教训：凡是往库里写异常信息的地方，一律先截断；且这类写入常处在 catch/finally 里，一旦再抛异常，故障就彻底隐身。**

### 🔴 事务边界隐患已被证实（不再是理论问题）
COUPON 那 51 条**连一条 status=70 的失败提案都没留下** —— 因为 `AssetDispatchEngine` 在 `addProposal` 的事务内执行：券插入失败 → 事务被标记 rollback-only → 引擎虽然写了 `status=70`，但**提案记录连同这条失败标记一起被回滚**，失败痕迹彻底消失。
这正是上一节预警的 `executeWalletCharge` REQUIRED 传播问题的同类表现。**后果很严重：资产层任何一次失败，都会让提案表查不到任何线索，只能靠翻日志。**
建议的修法（属公共账务域改造，未擅自动）：把 `assetDispatchEngine.execute` 移出 `addProposal` 的事务（提案提交后再触发），或让引擎的状态记账走 `REQUIRES_NEW` 独立事务。

### 派发链路收尾三件事（2026-07-26，已改完待验证）
1. **开启异步派发**。`AsyncConfig` 配了线程池和异常处理器，却**从来没有 `@EnableAsync`** —— 缺它 `@Async` 完全不生效（静默同步）。已在 `AsyncConfig` 补上（开启前确认全工程无存量 `@Async`，不会有方法突然变异步），并给 `GlobalEventDispatcher.dispatch` 挂 `@Async(ASYNC_EXECUTOR_THREAD_NAME)`。派发在 AFTER_COMMIT 执行、成败不影响已提交的主事务，天然适合挪出主线程。**代价：异常不再沿栈上抛，只能靠日志 + 下游表自查发现问题。**
2. **补齐两层策略**，现在四种资产类型两层都齐了：
   - `@PrizeStrategy`（consumer）：SCORE / BALANCE / COUPON / **PHYSICAL**（新增 `PhysicalPrizeHandler`，拆掉审批流地雷）
   - `@AssetStrategy`（ledger）：BALANCE / COUPON / PHYSICAL / **SCORE**（新增 `ScoreAssetHandler`，复用按 assetType 泛化的 `executeWalletCharge`）
3. **打通资产真实到账**：
   - `ProposalRecordDao` 补 `updateStatus(id, from, to)` 条件流转 + `updateStatusAndRemark(id, status, remark)`，XML 同步。
   - `AssetDispatchEngine` 从桩改成真实现：`30 待执行 -> 40 执行中` 用条件更新做并发闸门（抢不到直接退出，同一提案只执行一次），结果落 `50 成功` / `70 失败`，**异常兜底也必须落 70**，否则状态永远卡在 40。
   - 放开 `ProposalRecordService:96` 的 `assetDispatchEngine.execute(proposal, config)`。
   - `addProposal` 本身是 `@Transactional`，提案落库 + 状态流转 + 动账同一事务，原子性成立；资金流水的 `uk_t_biz_mbr_ast_txn_ref` 唯一索引真实存在，幂等兜底可靠。

**⚠️ 已知事务边界隐患（未改，留待评估）**：`executeWalletCharge` 是 `@Transactional` 且以 REQUIRED 加入 `addProposal` 的外层事务。动账失败抛 `BusinessException` 时，内层规则会把事务标成 rollback-only，而 `AbstractAssetHandler` 把异常吞掉转成 ResponseDTO 继续走 —— 外层提交时会抛 `UnexpectedRollbackException`，且状态 70 的失败记录也一并回滚丢失。压测场景（用户名唯一、钱包自愈新建）不会触发，但同一用户并发中多个奖时会。要根治需重新划分事务边界（如动账走 REQUIRES_NEW + 失败补偿），属公共账务域改造。

### ⚠️ PHYSICAL 缺 @PrizeStrategy —— 审批流上的地雷（已补 PhysicalPrizeHandler）
iPhone/AirPods 是 `approve_mode=1`，卡在审批前不会走 `doDispatch`，所以压测一路没炸。
**但运营在审批工作台点「通过」的那一刻，就会抛「不支持的奖品类型: PHYSICAL」。**
ledger 层有 `PhysicalAssetHandler`，缺的是 consumer 层的 `@PrizeStrategy(PHYSICAL)`。补它之前，实物奖不要开审批通过入口。

### 派发策略补齐（2026-07-26，已验证）
按「走提案链路」的方案补了两个 `@PrizeStrategy`，与 `BalanceHandler` 同构（不自己动账，统一交给风控提案域，`externalBizNo` 作跨域幂等键）：
- `ScoreHandler`（SCORE）：**价值为 0 时直接判成功、不生成提案**。「谢谢参与」的 prize_value 就是 0，若照 BalanceHandler 那样把 0 当异常，兜底奖项会刷出满屏失败流水淹没真故障；负数仍然一律拦死。
- `CouponHandler`（COUPON）：面额参与风控预算核算，必须 > 0。券实例的真实发放由 ledger 层 `CouponAssetHandler` 负责。
- 顺带修 `BalanceHandler`：原来**丢弃 `addProposal()` 的返回值直接 `return ok`**，提案被风控拦截也会被记成「发货成功」。三个 handler 现在都如实上抛失败。

**⚠️ 「派发成功」目前只等于「提案已生成」，资产并未真正到账。** 提案下游还有两层是桩：
1. `ProposalRecordService:96` 的 `assetDispatchEngine.execute(proposal, config)` **被注释掉**了，免审提案不会触发下发。
2. `AssetDispatchEngine.execute()` 内部 `int rows = 0;` 写死（原本该调 `proposalRecordDao.updateStatus(id, 30, 40)`），所有状态更新语句也全是注释 —— 因为 `ProposalRecordDao` 里**压根没有 `updateStatus` / `updateStatusAndRemark` 这两个方法**。
这是资产账务域尚未建完，不是 bug。要让积分/券真正到账，需要补齐 DAO 的状态流转方法 + 放开引擎调用 + 打通 `t_promotion_config` 的 `used_amount/used_quota` 扣减。

配套造数：`数据库SQL脚本/mysql/派发链路-联调造数.sql`（**已执行**）。`t_promotion_config` 是空表而 `prize_config.promotion_config_id` 全是 0，不造数的话每条提案都会在第一步被判「资产配置异常」。脚本里的取值是按风控三条过滤器的实际判定条件配的（`single_max_amount` 远大于面额、`identify_limit=-1`、`total_amount=-1`、`review_level=0`）。

### 压测第3轮（2026-07-26）：派发链路打通，但压测本身无效
- ✅ **`t_prize_log` 首次有数据（11 行）**，且 `approve_status=1 / status=0` —— 88红包是 `approve_mode=1`，正确停在「提案挂起等审批」。`external_biz_no` 去重数 == 行数，防重索引未误伤。路由 bug 确认是根因。
- ❌ 压测结果无效：**DB 的 `used_stock` 重置了，但 Redis 仍留着上一轮的 0**。中奖分布精确地按「第2轮 Redis 是否抽空」分成两组（抽空的三项 0 中奖，有余量的三项按概率正常中奖），Lua 预扣读 Redis 直接判库存不足。
- **教训：`used_stock` 与 Redis 库存是两份状态，只重置 DB 等于没重置。** 最省事的重新预热方式不是敲 redis-cli，而是到工作台点一次「保存并发布」——`warmStock` 是无条件覆盖，会按 DB 现状把所有 key 刷一遍。

### 由第3轮引出的两处加固（已改，待验证）
1. **压测脚本改用 run 级隔离的用户名**：原来固定 `stress_user_0..N`，跨轮重复。`user_max_count=1` 时上一轮赢过某奖项的用户在 `draw:user:{活动}:{奖项}:{用户}` 里计数已是 1，之后永远赢不了该奖项，不清 Redis 就会让结果越跑越失真（表现为「明明有库存却一直不中」）。现改为 `stress_{RUN_ID}_{i}`，RUN_ID 每轮不同，`draw:user:*` 再不需要手工清。
   ⚠️ v3.64 起该 key 多了一段周期桶：`draw:user:{活动}:{奖项}:{周期桶}:{用户}`。
   扫描模式 `draw:user:{活动}:*` 仍然有效（周期段插在用户名之前就是为了这个）；
   而按天重置的奖池，隔天再跑压测计数本来就归零了，连 RUN_ID 隔离都不再必需。
2. **`resolveRemainStock` 的回源预热改走 SET NX**（`DrawStockService.warmStockIfAbsent`）：原来复用无条件 SET 的 `warmStock`，缓存冷启动遇上高并发时，A 预热 100 → B 扣成 99 → C 也没读到 key 又预热回 100，**扣减被覆盖直接超发**。现在配置态保存仍用无条件 SET（要的是以 DB 为准强刷），运行态回源用 SET NX（补空洞但绝不覆盖），两种语义分开。

### 压测第2轮（2026-07-26，未清场，承接第1轮库存）
未清场反而给出了更硬的证据 —— 每个奖项都被**精确打到剩余库存为止**：

| 奖项 | 轮次前剩余 | 本轮中奖 | 结果 |
|---|---|---|---|
| 100 积分 | 5 | **5** | 恰好抽空 |
| 满100减20券 | 43 | **43** | 恰好抽空 |
| 谢谢参与(兜底) | 0 | **0** | 已空，不再发放 |

两轮累计：1000 次请求 / 1000 条流水 / 1000 个不重复 traceId；中奖流水 325 条 == `used_stock` 总和 325；无一奖项 `used_stock > total_stock`。**防超发结论可以定案了。**
兜底干涸后 675 次返回「手慢了」，符合 `DrawEngine` 的 NoStock 语义。

### 抽奖模块（代码已完结，只剩「跑一遍」）
1. **联调 + 压测**（唯一剩余项，需要人跑）：
   - ✅ `v3.34.0.sql` 已在开发库执行（`is_fallback` 列已存在，2026-07-26 核对过）。
   - ⬜ **`v3.35.0.sql` 待执行**（`t_prize_config` 补 `uk_prize_code` 唯一索引）。
   - 造数据：执行 `数据库SQL脚本/mysql/抽奖模块-联调造数.sql`（可重复执行，只造活动 `12278CBYW7`「618年中狂欢抽奖」+ 6 个资产大库奖品，编码均已按新规范预生成；奖池/物资/坑位刻意留给工作台页面生成，顺带验证保存↔回显往返）。
   - **重启后端**（新增的 detail/optionList 接口需要新 class）→ 打开「抽奖配置工作台」（菜单 id=437 已存在）→ 配物资与概率 → 保存并发布（触发库存预热）→ 刷新页面确认回显一致 → 跑 `docs/压测-抽奖并发验证.js` 按 A/B/C 核对绝不超发。
   - 压测脚本入参已改成 `node 压测-抽奖并发验证.js <token> <活动编码> <奖池编码> [并发] [总次数]`（编码变随机码后不能再硬编码，奖池编码要到 `t_prize_pool_config` 里查工作台生成的那个）。
   - 压测注意：`DrawExecuteService` 有 **单用户单活动 2 次/秒** 的 RRateLimiter，压测脚本用的是 `stress_user_{i}` 各不相同的用户名，别改成同一个用户否则全被限流。
   - 重跑压测前必须同时清 `t_draw_prize_log` + `t_prize_pool_item.used_stock` + Redis 的 `draw:stock:*` 和 `draw:user:*`（造数脚本尾部有现成语句）。
2. ~~dispatcher 挂 @Async~~、~~补 PHYSICAL 策略~~、~~打通资产到账~~ —— **均已完成，待压测验证**（见上文「派发链路收尾三件事」）。重跑时重点看：提案状态是否从 30 走到 **50**、`t_member_wallet`/`t_member_asset_transaction`/`t_member_coupon` 是否真有数据、QPS 是否回到 200+。
3. `t_promotion_config.used_amount / used_quota` 的真实扣减仍是 TODO（`AssetDispatchEngine` 里原注释提到的那条）。现在预算只在风控层做「弱校验」读，发完不回写，跑久了预算水位是失真的。
4. 事务边界隐患（`executeWalletCharge` REQUIRED 加入外层事务，失败会引发 `UnexpectedRollbackException`），见上文说明。
5. 抽奖券(TICKET)资产建模。（`reset_period` 周期重置已于 v3.64 落地，见 §3.6；
   抽奖成本已移交上游业务，TICKET 不再由抽奖引擎负责）
4. ⚠️ **公共链路遗留隐患**：`PrizeConfigService.getByPrizeCode()` 用 `.one()`，而 `prize_code` 只在活动内唯一 —— 一旦两个活动复用同一编码就抛 `TooManyResultsException`。抽奖模块内的两处调用已改走新增的 `getByActivityCodeAndPrizeCode()`；但 `consumer/PrizeDispatchHandler` 和 `consumer/BalanceHandler` 仍在裸用，属公共派发链路，需单独评估后再动。

### 抽奖原型评审遗留（docs/奖池抽奖模块原型-变更说明.md）
- 配置页实时超发校验（填预估抽奖量，每行标红）；多奖池已做；保存前 diff 确认；操作审计；概率人话换算（0.05%→约每2000次1次）。

### 生成器修复（已完成本次）
- `QueryFormVariableService` 的 ENUM 分支已加 enumName 空判断（未配置枚举时降级普通字段，避免生成 `import ...constant.null`）。
- **仍建议排查**：其他生成场景是否也有类似裸拼；Menu.sql 等模板。

### ✅ 彩票模块（2026-07-28 全链路打通，详见 `docs/彩票中台-实现技术方案.md`）
配置 → 上线 → 发号 → 开奖 → 派奖 → 审批 → 资产落账，全流程可在界面完成。
`npx vite build` 也随之修好（那条坏引用已删）。**关键约束见本文档 §4，动这个模块前必读。**

### 任务模块
- **配置态 + 运行态均已完成**（运行态 P0/P0.5 于 2026-08-01 交付并验收，见方案 §9）。
- 剩：P1 事件注册表、前端页面、权限菜单 SQL、`t_proposal_record` 审批工作台（挂账）。
- 单测口径更新：solvela-marketing 全模块 **73 个**（原 45 + 任务 28）； 另有 `solvela-admin` 的集成验收
  `TaskRuntimeP0AcceptanceTest` **25 个**（ **真连开发库**，
  跑前需先执行 `任务模块-运行态联调造数.sql`，且要带 `SPRING_PROFILES_ACTIVE=dev`）。

---

## 6. 原型文件清单（docs/）
- `任务与抽奖模块-后台交互原型.html`（任务向导/模板设计器/监控台/数据看板/派发记录 + 抽奖转盘）
- `奖池抽奖模块原型交互设计.html` + `奖池抽奖模块原型-变更说明.md`（P0浮点bug/P1三bug/P2库存超发预警已修）
- `彩票中台原型-FPE彩票.html`（已改造为 x-template，六项交互浏览器实测通过，见铁律 6）
- `彩票中台-实现技术方案.md`（16 节：设计取舍 + 逐阶段验收记录 + 几次自我修正的复盘）
- `压测-抽奖并发验证.js`
- `活动创建向导原型-一体化向导.html`（x-template 承载，10 项浏览器实测通过；**交互以它为准**）
- `活动创建向导-实现技术方案.md`（12 节：六个既有缺陷 + BASIC 类型的由来 + 两处判据修正 + 验收记录 + 启用/禁用改造）
- `营销中台-数据统计方案.md`（**2026-08-01 新增**）—— 活动大屏的指标与口径设计。
  只用营销域自己的表（提案/优惠配置/钱包券流水属账务域，真实部署里通常是另一个系统的库）。
  🔴 三个必须先定死的口径见 §2；所有 SQL 均已在开发库实跑验证。
- `活动大屏原型.html`（**2026-08-02 已修正为 v2**）—— 上面那份方案的可视化原型。
  v1 有 3 处直接违反方案（含一个营销域根本算不出来的「预算消耗率」），
  **文件头列了相对 v1 的 9 处修正及各自依据**。大屏尚未落地到项目，仍是原型。
- `后端升级白皮书-SpringBoot4.md`（**2026-08-02 新增**，7 节）—— Boot 4 升级全记录。
- `前端优化白皮书.md`（**2026-08-02 新增**，7 节）—— 前端产物 26MB→8.3MB 的全记录。
- `首页图表-口径确认清单.md`（**2026-08-02 新增**）—— 首页参与统计的口径定义，
  **改统计逻辑前必读**；顶部是已与产品逐条确认的结论表。
- `任务引擎核心运转与排雷白皮书.md`（**2026-08-03 新增**，5 节）—— 面向接手 sa-task 运行态的人：
  策略为什么是纯函数 / sealed+record 挡住了哪三类线上 bug（含 Mermaid 类图）、
  `advance()` 生死 6 步（含时序图，标出事务边界与回滚点）、
  **`getOrCreateRecord` 的快照陷阱深度解剖**（ReadView 生成时机、快照读 vs 当前读、
  为什么不能就地重查）、防快照幻读的四把武器对比、三条红线 + 排查手册。
  **动任务运行态前先读它**，比直接读代码快。两张图已实测可渲染（IDEA 内置 Mermaid 支持）。
- `任务中台-操作说明书.md`（**2026-08-01 新增**，9 节）—— 面向运营/上游开发/客服三类人的使用文档。
  §5「排查手册」是症状→原因对照表（含全部丢弃原因的人话解释与处置），
  §8「当前能力边界」明确列出**配得出来但不会生效**的四项（人群过滤 / 频次上限 /
  rule_script / 自动过期），避免误判成 bug。
- `任务中台-改进技术方案.md`（**v2，2026-08-01**，9 节：**待评估** —— 运行态缺口盘点 +
  三个分类维度评估 + 三层分离设计 + 策略工厂 + QLExpress 双表规划 + 分阶段实施；
  v2 新增 §4.3~§4.8 **运行态并发与幂等设计**：阶梯发奖撞唯一索引、原子累加 vs 乐观锁、
  事件流水表、`period_key` 生成责任、异步隔离与线程池两个坑、阶梯任务的状态表达）
- ~~`活动创建向导.html`~~ —— **2026-07-29 已删除**。它是跳转式形态（与最终的一体化相反），
  且 LOTTERY 的路由与组件名两处都写错（`lottery-config` + `LotteryDesigner`，
  实际是 `lottery-workbench` + `LotteryWorkbench`）—— 写于 07-26，彩票工作台 07-28 才落地。
  内容可从 git 历史找回，但**不要照它实现**。

## 7. 数据脚本（数据库SQL脚本/）

> **2026-08-22 会员域地基这一批（详见 §13）**
> - `member.sql`：会员域 DDL，4 张表（`t_member` / `t_member_verify` / `t_member_id_seq` /
>   `t_member_login_log`）。**已在开发库执行。** 登录日志按月 RANGE 分区，
>   🔴 分区表的每个唯一索引（主键也算）都必须含分区键，所以主键是 `(id, create_time)`。
> - `mall.sql`：积分商城 DDL，7 张表。**2026-08-22 已建进开发库**，权威已移交基线。
>   里面含一条 `INSERT INTO t_file_category`（新增 `MALL_COMMODITY` 分类），
>   跨库执行探测时会失败属正常。
> - ⚠️ **`sql-update-log/` 已于 2026-08-22 整个删除**（全部执行完毕之后）。
>   本轮的 v3.70.0 / v3.71.0 / v3.71.1 / v3.72.0 / v3.73.0 五个文件都已在开发库跑过，
>   库的状态已经和两个基线完全一致，没有任何待执行项。要翻它们看
>   `git log -- 数据库SQL脚本/mysql/sql-update-log/`；决策与踩坑记录见 §13.4 / §13.5。
>   **从此改表结构 = 直接改库 + 重新导出基线**，不再写迁移文件（理由见 mysql/README.md）。

- `mysql/抽奖模块-联调造数.sql`：抽奖联调/压测的活动与资产大库造数，可重复执行，尾部含压测清场语句。
- `mysql/sql-update-log/v3.35.0.sql`：`t_prize_config` 补 `uk_prize_code` 唯一索引（配合业务编码全局唯一约定）。**待在开发库执行。**
- `mysql/任务模块-编码规范迁移与造数.sql`：存量 `DAILY_SIGN_TPL` 迁到新编码（级联 t_task_config）+ 建一个 TASK 类活动 + 给模板补 ui_schema 的 icon/desc。**待执行。**
- `mysql/sql-update-log/v3.44.0.sql`：任务运行态 P0 —— 建 `t_task_record_flow` +
  `t_task_record.version`，含三段自查 SQL。**已在开发库执行（2026-08-01）。**
- `mysql/任务模块-运行态联调造数.sql`：P0 验收造数（活动 `AP0TASKRUN` + 3 个模板 + 5 个任务配置
  含**一个真两档的阶梯任务** + 6 条档位映射），可重复执行，尾部带清场与对账查询。**已执行。**
  ⚠️ 它还顺手做了两件必要的修复：清掉 3 行指向已删任务配置的**孤儿档位映射**；
  把积分池 `t_promotion_config` id=1 的预算从「50/50 已跑满」重置为不限
  —— 不重置的话所有 SCORE 派发都会停在 `status=70 预算已耗尽`，而根因与任务模块毫无关系。
- `mysql/sql-update-log/v3.45.0.sql`：公共派发链路两处修正 —— `t_proposal_record` 加 `asset_name`
  （券名/商品名，由营销侧下传，避免账务域反查营销域）+ `source_type` 注释补 LOTTERY。
  **已在开发库执行（2026-08-01）。** 存量 `source_type='LOTTERY_DRAW'` 刻意不迁移，
  脚本里备了一条按活动类型回填的语句（已注释，生产用前先备份）。
- `mysql/sql-update-log/v3.49.0.sql`：任务中台人群过滤与参与频次落地 ——
  只改列注释（`target_audience` / `limit_type` / `limit_count` / `period_key` 的真实语义），
  不改结构不迁数据。**已执行（2026-08-01）。**
  执行前查过存量：没有任何任务配了非 ALL 人群或 `limit_count>1`，所以行为变更影响为零。
- `mysql/sql-update-log/v3.50.0.sql`：`t_task_record_flow` 加 `discard_code`（丢弃原因分类）
  + 覆盖索引 `idx_t_tsk_flw_stat` + 历史回填。**已执行（2026-08-01）。**
  回填 8 条全部归类成功、零条未归类。⚠️ 回填是按文本特征匹配的**一次性补偿**，
  不要固化成常态逻辑 —— 新数据由 Java 侧直接写码。
- `mysql/彩票模块-联调造数.sql`：LOTTERY 活动 `LTQ7M3XKD8` + 四个奖级的奖品。**已执行。**
  彩票配置与奖级规则刻意留给工作台页面创建，顺带验证保存↔回显往返。
  尾部备了两句到 P4 联调才需要的语句（积分池预算重置、发号清场），用前先读注释。
- `mysql/sql-update-log/v3.52.0.sql`：首页营销大屏的权限功能点 —— t_menu 补 459 菜单
  （`visible_flag=0`，它就是首页，不该在侧边栏再出现一条）+ 460 功能点 `marketingStat:query`，
  七个统计接口共用一串。**已在开发库执行（2026-08-03）。**
  ⚠️ 给 ADMIN 角色授权那段更新 0 行 —— 开发库的 5 个角色没有一个 `role_code='ADMIN'`
  （多数为 NULL），与 v3.42.0.sql 当初情况一致，不是脚本写错。
- `mysql/sql-update-log/v3.39.0.sql`：清理号码池菜单与授权。**已执行。**
- `mysql/sql-update-log/v3.40.0.sql`：彩票 DDL（期号加 plan_draw_time；记录加 prize_code 快照、
  dispatch_status；prize_level 改 NOT NULL DEFAULT 99）。**已执行。**
- `mysql/sql-update-log/v3.41.0.sql`：彩票配置工作台菜单。**已执行。**
- `mysql/sql-update-log/v3.42.0.sql`：活动创建向导菜单 441 + 四个功能点 442~445
  （`activityConfig:query`/`:add`/`:update`/`:delete`），并给 ADMIN 角色补授权。**已执行。**
  ⚠️ `path` 取 `/activity/activity-wizard`，与既有的 `/activity/activity-config/list` 同口径 ——
  **`path` 是前端路由、`component` 才是 src/views 下的相对路径，两者别混**，写错不报错只白屏。
- `mysql/sql-update-log/v3.43.0.sql`：`t_activity_config.activity_type` 列注释补上 BASIC。**已执行。**
  只改 COMMENT；`MODIFY COLUMN` 时原定义 `varchar(32) NOT NULL` 已原样抄全 ——
  漏一个 `NOT NULL` 就等于顺手把约束放开了。

### 开发库现状（2026-07-29 核对，活动向导验收后已清场）
- `t_activity_config`：DRAW `12278CBYW7` / TASK `WLO9SMXDKD` / **LOTTERY `LTQ7M3XKD8`** 各 1 条，
  外加用户自建的 `VJR856K8DW`（LOTTERY 测试活动向导）与 `XHD9GCQGYW`（DRAW 测试活动1）。
  我造的 `P0验收-*` / `P0补验-*` / `P2验证-*` / `P3验收-*` / `聚合验收-*` 测试活动**均已清理**。
- **`WLO9SMXDKD`（新人成长营）完备度已变为「已配置」** —— P3 验收 13b 时用内嵌任务向导
  真提交了一个任务配置进去，`t_task_config` 下多了一条。若要恢复成「未配置」的干净态，
  删掉该活动下的任务配置即可。
- **彩票**：两个干净玩法 `RMAAUK45TG`（5位/10万）与 `NQ4W55L661`（7位/1000万），
  **均已下线**（P4 联调时上过线，验完还原），各配 2 条奖级规则；
  `t_lottery_issue` / `t_lottery_record` 全空，联调产生的记录、派奖流水、提案、钱包、履约单均已清理。
  ⚠️ 要跑领号必须先在工作台点「上线」—— 未上线时领号接口会拒绝。
- ~~`t_activity_config`：只有 1 条 DRAW 活动~~（下面几条是 07-26 的快照，任务模块部分仍有效）
- `t_task_template`：1 条 `DAILY_SIGN_TPL`，不符合新编码规范，需迁移。
- `t_task_config`：3 条 `task_name='ggg'` 的测试数据，`activity_code='NEWBIE_CAMP'` 来自已删除的前端 mock 常量，库里并无此活动 —— 孤儿数据，脚本里给了「删除」或「挂到新活动」两条注释语句，二选一。
- `t_prize_pool_item` / `t_prize_pool_config` / `t_pool_prize_mapping` / `t_draw_prize_log`：全空，等工作台联调时生成。

---

## 8. 用户偏好（协作方式）
- 工业级 B 端，追求"设计简单、使用简单、降低心智负担、提高运营效率"。
- 喜欢用最新技术做试验（抽奖模块用了 record/sealed/模式匹配/Text Block；JDK25 虚拟线程规划中）。
- 前端不熟、不画原型，由我产出原型 HTML + Vue 组件。
- 每次改动要真实验证（编译/build/单测/浏览器实跑），不臆断；诚实说明未验证的部分。
- 增量修改优先，不轻易重写；改前先读现状。
- 中文提交信息，结尾带 Co-Authored-By。

---

## 9. 升级与优化轮次（2026-08-02，分支 `upgrade/springboot4`）

> **这一轮不动业务逻辑**，全部是升级、瘦身与缺陷修复。
> 完整记录在两份白皮书里，这里只写「接手后必须知道」的部分。

### 9.1 分支与提交

`upgrade/springboot4` 从 `task` 的 HEAD 拉出，是 `task` 的**严格超集**
（`upgrade/springboot4..task` = 0 个提交），业务代码一行不缺。
合回主线时是干净的快进合并：`git checkout task && git merge --ff-only upgrade/springboot4`。

### 9.2 后端：Spring Boot 3.5.16 → 4.1.0

详见 `docs/后端升级白皮书-SpringBoot4.md`。**动代码前必看**的几条已并入 §0 的
「Spring Boot 4 升级连带修复」，此处不重复。补充三点状态：

- **API 文档换成了 `com.baizhukui:knife4j-openapi3-boot4-spring-boot-starter:5.2.1`**。
  官方 knife4j 与主流社区分支都只支持 Boot 3。这是**第三方 fork**，
  且会把 springdoc 拉低到 3.0.3、swagger 拉低到 2.2.47 —— 引入前请自行评估该发布者。
  `/doc.html` 可用，实测 200。
- 验证强度：全模块编译、单测 73/73、**真实启动 + 登录/验证码/Swagger 接口实测 200**。
- **上线前必须清一次 Redis**：`RedisConfig` 与 sa-token 两处序列化格式都变了，
  老 key 反序列化会失败；清了则所有在线用户被登出，安排低峰期。

### 9.3 🔴 堵住 `create_time` 被人为写入（有真实脏数据）

这轮挖出的最严重问题，链路已验证到底：

```
前端表单 createTime → XxxAddForm.createTime → SolvelaBeanUtil.copy（铁律 7 自动拷贝）
→ dao.insert → MyBatis-Plus 把调用方给的值写进 create_time，覆盖 DDL 默认值
```

**已做**：36 个 Add/Update Form 摘掉 73 个审计字段；4 个前端表单去掉输入项；
代码生成器实体模板删掉硬编码的 `@TableField(fill=...)`（**那才是复发源**）；
`MybatisPlusFillHandler` 里说反了的 javadoc 已改正（详见 §0 铁律 9 的源码实证）。

**⚠️ 未做完，接手后要处理**：`t_task_prize_mapping` 有 **2 行 `create_time` 与
`update_time` 都是 NULL**（id=4/task_config_id=6、id=55/task_config_id=50，
任务名分别是「P3验收-内嵌提交测试」和「ggg」，**看着都是测试数据**）。
订正 SQL 已写好但**默认注释掉**，两套口径任选：
`数据库SQL脚本/mysql/诊断-审计时间异常排查.sql`
（方案 A 从父表 `t_task_config` 回填；方案 B 确认是测试数据后直接删）。

全库四项检查只有这一处异常，业务字段完好，只有审计列受影响。

### 9.4 前端：产物 26MB → 8.3MB

详见 `docs/前端优化白皮书.md`。接手后最需要知道的：

- **monaco-editor 已整体换成 CodeMirror 6**。组件是
  `components/business/code-editor/SolvelaCodeEditor.vue` 与 `SolvelaCodeDiff.vue`，
  prop 形态与原 `vue-monaco-editor` 一致（`v-model:value` / `language` / `theme` / `height`）。
  语言**只支持 java / json**，且**只做着色不做语法检查**
  —— QL 的语法校验要走后端已有的 `check(script)` AST 预校验接口，那是独立的事。
- 路由 `/monaco-editor` → `/code-editor`。**这是数据库里的正式菜单**（`t_menu` id=301），
  已配 `sql-update-log/v3.51.0.sql`，升级已有库时别漏。
- 起止时间选择器现在会自动补当天 `00:00:00` / `23:59:59`（`constants/date-time-const.js`），
  覆盖 11 个文件 18 处。开奖时刻与记录类时间**刻意保持手动**。

### 9.5 首页与大屏（**2026-08-03：大屏已落地，首页就是大屏**）

> 🔴 **首页 `views/system/home/index.vue` 已整体让给营销作战大屏**，
> 原来的用户信息头 / 公告 / 通知 / 公众号卡片 / 更新日志 / 待办**都已从首页移除**
> （组件文件仍在 `views/system/home/` 下，已无引用方，要删随时可删）。
> 大屏组件在 `views/system/home/dashboard/`：
> `marketing-dashboard.vue`（外壳）/ `prize-trend-chart.vue` / `gameplay-panel.vue`，
> 参与统计图作为其中一块保留。

**后端 `solvela.stat` 现有七个只读接口**，权限串统一 `marketingStat:query`
（功能点见 `v3.52.0.sql`，**没执行的话非超管打开首页整片没权限**）：

| 接口 | 内容 | 轮询 |
|---|---|---|
| `GET /marketingStat/participation?days=` | 参与趋势 + 各玩法参与人数 | 组件自管 |
| `GET /marketingStat/overview` | 活动状态分布 + 活动卡片 + 逾期未开奖告警 | 热 30s |
| `GET /marketingStat/prizeHealth?activityCode=&days=` | 条数/价值双口径 + 待审积压 + 失败TOP + 价值趋势 | 热 30s |
| `GET /marketingStat/gameplay/{activityCode}` | 抽奖状态分布 / 彩票期号 / 任务列表 | 随下钻 |
| `GET /marketingStat/taskFunnel/{taskConfigId}` | 阶梯流失漏斗 | 随下钻 |
| `GET /marketingStat/eventHealth?days=` | 推进/丢弃，按 discard_code 聚类 | 冷 5min |
| `GET /marketingStat/topMembers?days=` | Top 获奖用户 | 冷 5min |

**🔴 动大屏前必须知道的四条**

1. **活动只有「启用 / 禁用」两态**，判据 `status != 2`（历史值 0 按启用），**不看起止时间**。
   全大屏与首页参与图同口径。数据统计方案 §2.3 的四态时间窗口径**已被这条取代**。
2. **玩法数走 `ActivityRefProvider.gameplayCount()` SPI**，不是三张表的子查询。
   新增第四种玩法只加一个实现类，统计代码一行不用改。
   与 `countRefs` 的区别：后者是删除守卫用的，把物资、期号这类附属引用也算进去。
3. **`prize_value` 是 varchar**，求和前用 `REGEXP '^[0-9]+(\.[0-9]+)?$'` 挡掉非数字 ——
   列注释是「积分数/券ID」，将来真存了 ID，`CAST` 会静默变 0。
4. **漏斗的「接取人数」与「到达人数」必须同为人数**。第一版这里错过：
   接取 12 人、第一档到达 18，**漏斗下层比上层还宽**。两个数各自都没算错，
   错在混了单位 —— 周期型任务是「每人每周期一条记录」，`COUNT(*)` 数的是记录。

⚠️ **自动化验证漏斗时会撞到一个假故障**：漏斗入场动画是 opacity 0 → 1，
而未合成帧的浏览器里 `requestAnimationFrame` 不跑，动画停在第一帧，
采样画布只看得到白色标签文字 —— **与「series 漏注册」现象一模一样**。
判别：读 zrender 显示列表，polygon 的 shape/fill 都对、只有 `opacity` 为 0 就是动画没跑。
（`FunnelChart` 已在 `src/lib/echarts.js` 注册。）

- **首页原来的假图表已清空**（Pie/Category/Gradient 数据是写死的常量），
  换成真实的 `participation-chart.vue`：上面三条参与趋势折线，下面横向柱状图。
- **新增接口 `GET /marketingStat/participation?days=`**（`solvela.stat` 包）。
  口径已与产品逐条确认，**改动前先读 `docs/首页图表-口径确认清单.md` 顶部的确认表**：
  参与=按会员去重的**人数**；抽奖含库存不足（有「谢谢参与」兜底）；
  任务只算 `flow_type=1`；活动按 `status != 2` 筛（**不看起止时间**）。
- ⚠️ 柱状图**刻意不用饼图**：跨玩法会重复计数（一人玩两种各算一次），
  各项之和大于总人数，画成饼图等于承诺「占比」而它加不到 100%。
- **`docs/活动大屏原型.html` 现为 v2.1**（v1→v2 修了 3 处违反方案的地方，
  v2→v2.1 改成活动两态、彩票与活动编码分开、修掉加不平的 KPI）。
  **✅ 落地方案已定并已实现：嵌首页**，不做独立全屏路由。
  原型仍保留作为交互与口径的对照物 —— 它的数字与真实数据实测一致
  （活动 `12278CBYW7`：315 = 151 + 100 + 64，券 51 条 1020 元）。

### 9.6 本轮新增/修改的文档

| 文件 | 用途 |
|---|---|
| `docs/后端升级白皮书-SpringBoot4.md` | Boot 4 升级全记录：改动点、难点与解决、回滚方案 |
| `docs/前端优化白皮书.md` | 前端瘦身全记录，含三个「静默失效」bug 的通则 |
| `docs/首页图表-口径确认清单.md` | 参与统计的口径定义，**改统计逻辑前必读** |
| `docs/活动大屏原型.html` | 已修正为 v2，文件头列了相对 v1 的 9 处修正及依据 |
| `数据库SQL脚本/mysql/诊断-审计时间异常排查.sql` | 审计时间异常排查 + 订正模板（默认注释） |
| `数据库SQL脚本/mysql/sql-update-log/v3.51.0.sql` | 菜单路由改名的增量脚本 |

### 9.7 未完成 / 待决策

1. **那 2 行 NULL 数据**要不要订正、走 A 还是 B（见 §9.3）。
2. **上线前清 Redis**（见 §9.2）。
3. **Excel 导入导出跑一次真实冒烟**（🔴 唯一一条从 08-02 拖到现在还没做的）。
   ⚠️ 这条的理由已经变了两轮：原先是「删了 `poi-ooxml-full` 改走 `poi-ooxml-lite`」；
   2026-08-08 之后 **POI 整个不存在了**，底层是自研的 SonicExcel（§11）。
   框架侧有 60 余条测试（含迁移语义快照、往返、错误策略），但**连着 DB + Redis 的真实业务链路**
   始终没被自动化覆盖 —— 要跑的是这三条：
   - 商品导出（`GoodsController`，走 3 个转换器：类目 / 枚举 / 字典）
   - 商品导入（`GoodsImportForm` 是 record，验证错误行能回显行号列名）
   - 企业导出（`EnterpriseController`，原水印接口，现走普通导出，前端应无感）
4. **大屏落地方案**未定（首页嵌入 / 独立全屏路由）。
5. 通知前端与测试：**接口文档地址没变**（`/doc.html` 仍可用，只是底层换成了 baizhukui fork，
   见 §0），但**前端路由 `/monaco-editor` 已改名为 `/code-editor`**（`t_menu` id=301，
   配套 `v3.51.0.sql`）—— 要改的是后者。

---

## 10. 依赖瘦身轮次（2026-08-06 ~ 08-08，✅ 已结案）

> 起初的目标：**删掉 guava 与 hutool，替代一律用 JDK 原生 API**（`List.of` / `Map.of` / `java.time` /
> `ThreadLocalRandom` / `Files` 等）—— 两个都已删完，共省约 5.6MB。
>
> 后续 §11 的 SonicExcel 又连带摘掉了 POI 全家桶（18.66MB）与
> commons-collections4 + commons-text（1.11MB）。
> **累计约 25.4MB**，当前 runtime classpath 为 **202 个 jar / 97.16MB**。

### 10.0 先看这张账：哪些能删、哪些是净亏

跑过 `dependency:tree -Dverbose` 反查每个工具库「除了我们自己还有谁需要它」：

| 库 | 我们的用量 | 还有谁需要 | 抄出来能删掉 jar 吗 |
|---|---|---|---|
| commons-lang3 | 40 处 | commons-text、knife4j、swagger-core、velocity×2、commons-compress | ❌ 删不掉 |
| commons-io | 7 处 | **commons-compress**（←fastexcel-reader）、**tika-core** | ❌ 删不掉 |
| commons-codec / commons-compress | **0 处** | compress 是 fastexcel-reader 的编译期依赖，codec 又挂在 compress 下 | ❌ 本来就不是我们的 |
| **commons-collections4** | 130 处 | ~~poi、poi-ooxml~~ → **无** | ✅ **已删（0.86MB）**，见 §10.5 |
| **commons-text** | 2 处（StringSubstitutor） | 无 | ✅ **已删（0.25MB）**，见 §10.5 |
| **guava** | 44 处 | **无** | ✅ **已删（2.92MB）** |
| **hutool-all** | 36 处 | 无 | ✅ **已删（2.7MB）** |

🔴 **结论仍然成立，但要连着前提一起读**：
**只有「除我们之外没人依赖」的库，换成自研才真的删得掉。**
lang3/io 至今删不掉，只是「谁拖着它们」换了人 —— 从 poi 换成了 compress + tika，
抄进自己的工具类照样是净亏（体积不变、CVE 面不变，还多一份要维护的实现）。

⚠️ **反过来也要留意：这条判据的输入是会变的。**
commons-collections4 原先「删不掉」是因为 poi 要它；2026-08-08 摘掉 POI 之后它变成了
「只有我们在用」，同一条判据就给出了相反的答案。**每摘掉一个大依赖，这张表都要重算一遍。**

### 10.1 ✅ guava 已移除（提交 `40438661`）

- `Lists.newArrayList()` / `Maps.newHashMap()` → `new ArrayList<>()` / `new HashMap<>()`，48 处机械替换
- 带参的 14 处**逐点核过调用方**：进 DAO 查询的用 `List.of(...)`；
  作返回值或交给 Service/框架的用 `new ArrayList<>(List.of(...))`；varargs 用 `Arrays.asList`
- `CaseFormat` → **`SolvelaCaseFormat`**（solvela-base/common/util）
- `ThreadFactoryBuilder` → `Thread.ofPlatform().name("前缀-", 0).factory()`
- `Interners` → `ConcurrentHashMap` + `computeIfAbsent` 规范化锁对象

🔴 **三条不要踩回去的**：

1. **不要把 `new ArrayList<>()` 改成 `List.of()` 图省事。**
   `Lists.newArrayList()` 返回**可变**列表，`List.of()` 不可变 ——
   换过去之后任何 `add/remove` 都在运行期抛 `UnsupportedOperationException`，编译期零信号。
2. **不要改 `SolvelaCaseFormat` 的语义。** 它不是照感觉写的：删依赖前用
   5×5×33 = **825 种组合**与 guava 逐条比对过，第一版有 48 处不一致（全在「下划线↔中划线」
   这对上 —— guava 那里走的是「只换分隔符、不动大小写」的快捷路径），补上特例后才 0 处不一致。
   `SolvelaCaseFormatTest` 把语义固化了， **代码生成器的产物命名依赖它**。
3. **发号器那把锁不能简化成 `synchronized(serialNumberId)`。**
   Integer 只缓存 -128~127，超出范围每次装箱都是新对象，两个线程锁的不是同一把锁 ——
   互斥静默失效、并发发重号。见 `SerialNumberInternService` 的注释。

### 10.2 ✅ 图形验证码已整体删除（提交 `dff502b6`）

需求方明确不要，**整体删除而不是留着不用**。顺带解开了「为了一个验证码组件留着 hutool」的死结。

**对外契约变化（前端/测试必知）**：

- `GET /login/getCaptcha`、`GET /login/getCaptchaFlag` **接口已不存在**
- `LoginForm` 不再有 `captchaCode` / `captchaUuid`，登录只需
  `loginName` + `password`(SM4密文) + `loginDevice`
- 后端 captcha 包、`RedisKeyConst.CAPTCHA`、`SwaggerTagConst.CAPTCHA`、
  `captcha-enabled` 配置全部清除；前端登录页对应的输入框/图片/开关/刷新定时器一并删除

⚠️ **安全影响（已与需求方确认后执行）**：登录页对撞库/暴力破解现在只剩
「连续登录失败 N 次锁定账号」这一道防线。

⚠️ **验证这类「接口是否真的没了」时不要只看 HTTP 状态码**：
本项目全局异常处理用 **HTTP 200 包错误体**，接口不存在时返回的是
`{"code":10001,...,"msg":"...NoResourceFoundException..."}`，状态码照样是 200。

### 10.3 ✅ hutool 移除（2026-08-08，三个提交）

`hutool-all` 2.7MB，全项目 36 处 import，按低/中/高风险分三档、每档一个提交做完， 最后从 `pom.xml` /
`solvela-base/pom.xml` / `common-api/pom.xml` 三处删除依赖。
`dependency:tree` 与 `build-classpath` 均已确认不再出现 hutool。

| 提交 | 内容 |
|---|---|
| `128efd35` | ① 低风险：24 处换 JDK 原生 |
| `7c5b965c` | ② 中风险：ServletUtil / NetUtil / BeanUtil / 脱敏 / 雪花自研 |
| `3a4e4da4` | ③ 高风险：SM4/AES 换 BouncyCastle，摘依赖 |

#### 🔴 验证方法论：这轮真正值钱的东西

沿用 §10.1 `SolvelaCaseFormat` 那次的做法， **每一处自研实现都先跑一次「一次性差异测试」**——
趁 hutool 还在 classpath 上，让新旧两个实现跑同一批输入逐条比对，
0 处不一致之后，才把期望值固化成永久单测、删掉差异测试。

**这不是形式主义，它逮到了三个「看起来对」的真错**：

1. **银行卡脱敏用的是 `cleanBlank`（去掉所有空白）而不是 `trim`（只去首尾）。**
   卡号常带分组空格 `6217 0000 1000 1234`，只 trim 会把空格算进长度，
   分组和保留位数全错 —— 但**不带空格的卡号完全正常**，随手测一个必然通过。
2. **手写的 4 条脱敏期望值有 3 条是错的**（银行卡 17 位的余数分组、
   邮箱无 `@` 时其实原样返回、`USER_ID` 的保留位数）。
   实现是对的，是「凭感觉写的断言」错了 —— 如果没有差异测试做基准，
   会反过来照着错误的断言去「修」正确的实现。
3. `SolvelaStringUtil.isBlank` 若直接用 JDK 的 `String#isBlank()`，
   不间断空格 U+00A0、BOM U+FEFF 这类字符会从「空白」变成「非空白」，
   而它们恰恰常出现在从 Excel / 前端粘来的文本里。判空口径会在全项目范围内静默漂移。

#### 各档要点

**① 低风险（`128efd35`）**

- 🔴 **`SolvelaStringUtil` 原本 `extends StrUtil`**，`isEmpty/isNotEmpty/isBlank/isNotBlank/
  trim/equals/join` 全是靠静态方法继承对外供着的，全项目 53 处在用。
  改成本类自实现，调用方一行没改。**`trim` 是 null 进 null 出，不是返回 `""`**。
- `DatePattern`/`DateUtil`/`LocalDateTimeUtil` → 项目本就有 `SolvelaDateFormatterEnum`
    + `SolvelaLocalDateUtil`，没有新造轮子
- `RandomUtil`/`IdUtil` → 新增 `SolvelaRandomUtil`。
  **顺带修了一处安全口径**：邮箱登录验证码原先用可预测的 `ThreadLocalRandom`，
  已改走 `SecureRandom`。该类注释里把「测试数据用 ThreadLocalRandom /
  凭证用 secureRandom()」的分界写死了。
- `FileUtil`/`ZipUtil` → `Files` + `java.util.zip`。
  ⚠️ 代码生成器的打包链路只在「运营点下载代码」时才跑，**编译期和原有单测都盖不到**，
  写坏了要等到有人下到一个打不开的 zip 才知道。为此把三个方法降为包级可见，
  补了 `CodeGeneratorTemplateZipTest` 钉住四件事：追加而非覆盖、
  条目名用正斜杠且不含最外层临时目录、**中央目录真的写出来了**（不 close 就是个空包）、
  非空临时目录能删干净。

**② 中风险（`7c5b965c`）**

- 🔴 **`SnowflakeIdGenerator`（common-api）：位布局与起始纪元 `1288834974657L` 必须与 hutool 逐位一致。**
  库里已有历史 ID，改任何一个参数都会让新旧 ID 落进同一段数值区间从而可能碰撞，
  且不再随时间单调递增（多处隐含依赖这一点排序）。
  验证方式是**拿 hutool 自己的解析器去拆自研生成的 ID**，位段全部对得上。
  `nextId()` 的 `synchronized` 不能拆成两个原子变量，会在「同一毫秒 + 序列回绕」
  的交叉点上发重号，已配 16 线程 × 5000 次的并发用例。
  ⚠️ **类名不叫 `Snowflake`**：门面类 `SnowFlake` 与它只差一个字母的大小写，
  Windows 文件系统大小写不敏感 —— 本次改造真的踩到了，新文件直接把门面覆盖掉了。
- 🔴 `SolvelaServletUtil.getClientIP`：`X-Forwarded-For` 是代理链
  `客户端, 代理1, 代理2`，取的必须是**第一个非 unknown 段**。
  取成最后一段的话，登录日志和风控里所有人看起来都来自自家网关。
  `getHeaderIgnoreCase` 整个删掉了 —— Servlet 规范本就规定 `getHeader` 大小写不敏感。
- 🔴 `SolvelaBeanUtil.beanToMap` **没有按原计划用 Jackson `convertValue`**：
  本项目 JSON 层挂了 `LongJsonSerializer`（Long 序列化成 String 防前端精度丢失）
  等自定义序列化器，走 Jackson 会让代码生成器模板里拿到的类型悄悄变掉 ——
  生成的代码照样能编译，只是内容不对。改用 `PropertyDescriptor` 反射，值保持原始对象。
- `DesensitizedType` 那个枚举字段**直接删了**：交接文档原先担心「11 处引用且在注解上」，
  实测这 11 处全在 `DataMaskingTypeEnum` 自己的构造参数里，**从来没有任何地方读过它**，
  打码规则一直是在 `DataMaskingUtil` 的 switch 里按枚举常量分发的。
- `hide()` 按**码点**而不是 char 遍历，否则 emoji 的代理对会被劈成乱码方块。
- `NetUtil` 三处改用项目本就有的 `SolvelaIpUtil`，没有新增实现。

**③ 高风险 · 加密（`3a4e4da4`）**

`ApiEncryptServiceSmImpl`（SM4）换成 **BouncyCastle**（`bcprov-jdk18on` 本就是现成依赖），
`ApiEncryptServiceAesImpl` 换成 JDK 自带 `Cipher`（AES 不需要 BC）。三点必须对齐：

1. **ECB + PKCS5(=PKCS7) 填充、无 IV** —— 原 `new SM4(key)` / `new AES(key)` 的默认值
2. 🔴 **SM4 那条链路是「双层编码」**：先加密得到十六进制小写串，
   再把这个 hex 串当文本做 Base64。除黑盒断言外，另有一个用例把
   「base64 解开之后必须是 32 位纯 hex」这个结构本身钉住。
   ⚠️ **AES 那条只有一层 Base64，没有 hex 层**，两边别搞混。
3. 密钥字节仍由 `stringToHex`/`hexToBytes` 推导，未动

**三层验证全部实跑**：① 字节级差异测试，14 个明文样本新旧密文串完全相同、
互相能解开对方的密文；② 黄金向量 `123456` ←→
`NzFlNWI2MjE5ODUyODAxOWM0OTI3ZDhkM2ZkNWY1ODY=` 双向对齐
（这串是**前端 sm-crypto 实测能登录成功的密文**，不是从当前实现反推的，
已固化进 `ApiEncryptServiceTest`）；③ **真起后端打了一次 `/login`**，
返回 `code=0` 并拿到 token。

同一次实跑顺带验掉了第②档的三处（都不是靠推断）：请求头传
`203.0.113.9, 10.0.0.1` 时 `lastLoginIp` 记成 **203.0.113.9**（第一段，不是最后一段）、
`lastLoginUserAgent` 取值正常、启动横幅的地址由自研 `normalizeUrl` +
`SolvelaIpUtil.getLocalFirstIp` 拼出且 `/doc.html` 实测 200。

⚠️ **ECB 模式本身有弱点**（相同明文块产生相同密文块、无完整性保护），
但它是**前后端约定好的既有契约**，前端 sm-crypto 那侧也是 ECB。
本轮只做「换实现、不换行为」；改 CBC/GCM 必须前后端同时改并处理存量。
已在类注释和测试里写明，免得下一个人误以为是疏忽。

#### 🔴 两个与本轮无关、但接手时会撞上的环境问题

1. **`solvela-admin` 的 `TaskRuntimeP0AcceptanceTest`（25 个）在本机跑不起来**，
   与 hutool 无关（已在改动前的提交上复现）。根因是连不上 MySQL：
   `Public Key Retrieval is not allowed` —— MySQL 8 的 `caching_sha2_password`
   走非 SSL 连接时需要 JDBC 串加 `allowPublicKeyRetrieval=true`。
   本轮起后端验证时是**用启动参数覆盖的，没有改仓库里的配置** ——
   该参数允许客户端向服务端索要公钥，有中间人风险，是否长期写进 dev 配置请需求方决定。
2. **`mvn clean compile` 之后可能出现「solvela-base 说 Nothing to compile， 但 target/classes 里根本没有 class」**，表现为
   solvela-admin 报一堆
   `NoSuchFileException: ...SolvelaXxx.class`。这是 maven-compiler-plugin
   的增量状态（`target/maven-status/.../createdFiles.lst`）与实际产物不一致导致的，
   **不是代码问题**。`rm -rf */target` 后重新 `mvn compile` 即可。
   （与铁律 20 同源：验证之前先确认跑的是不是刚构建出来的那个包。）

#### 本轮新增的测试（40 个，都在 `mvn test` 默认范围内）

| 测试                                | 钉住什么                                 |
|-------------------------------------|------------------------------------------|
| `SolvelaStringUtilTest`（9）        | 判空/trim/join/hide/cleanBlank 的口径    |
| `CodeGeneratorTemplateZipTest`（4） | 代码生成器打包链路                       |
| `SolvelaServletUtilTest`（6）       | 代理链取第一段、六个候选头的优先级       |
| `DataMaskingUtilTest`（10）         | 六类脱敏规则                             |
| `SnowflakeIdGeneratorTest`（6）     | 位布局、纪元、单调性、并发不重号         |
| `ApiEncryptServiceTest`（8）        | 黄金向量、双层编码结构、AES/SM4 不可互换 |

### 10.4 本轮其它已完成项（首页大屏的收尾）

| 提交 | 内容 |
|---|---|
| `31e25495` | 首页改营销大屏：六个统计接口 + 前端落地（详见 §9.5） |
| `8fb7d693` | 清死代码：首页 5 个旧组件、2 套备用登录皮肤（login2/login3）、18 张孤儿图片；顺带修了大屏中间栏被撑开 647px 空白的布局 bug |
| `10fcccfe` | 大屏三栏底部对齐（`.fill-card` + `min-height:0`） |
| `42ad3abf` | 大屏配色收口：新增 `dashboard-theme.js`，分类色/状态色/有序梯/框架色四种职责分开，槽位顺序跑过配色校验 |
| `914487a5` | 新增 `docs/任务引擎核心运转与排雷白皮书.md` |


### 10.5 ✅ commons-collections4 + commons-text 已移除（2026-08-08，提交见 git log）

摘掉 POI 之后重算 §10.0 那张账，这两个库变成了「除我们之外没人依赖」，合计 **1.11MB**，
runtime classpath 从 204 个 jar 降到 202 个。

**commons-collections4（0.86MB，130 处）→ `SolvelaCollectionUtil`**

130 处里 **125 处是 `isEmpty` / `isNotEmpty`**，本质就是一个 null 检查，不属于"要长期维护的实现" ——
这正是它和 lang3/io 的区别所在。剩下 5 处单独处理，其中**一处是真地雷**：

- 🔴 `ErrorCodeRangeContainer` 用 `CollectionUtils.subtract(codeList, distinctCodeList)` 找重复错误码，
  靠的是 commons 的**多重集语义**（按重数扣减）。换成 `removeAll` 会**永远返回空**，
  这条启动期校验静默失效。已改写成显式的「找出出现多于一次的 code」。
- `NoticeService` 的两处 `subtract` 入参都先 `.distinct()` 过，`removeAll` 与原语义等价，安全。
- `RedisService.arrayToList` → `Arrays.asList`（顺带去掉一个 `@SuppressWarnings("unchecked")`）。
- `MapUtils.isNotEmpty` 1 处，并入 `SolvelaCollectionUtil`。

⚠️ 替换必须**按文件门控**，只改 import 了 commons collections 的文件：项目里有 4 个文件用的是
`org.springframework.util.CollectionUtils`，同名不同物，按方法名盲替换会误伤。

顺带修掉一个坏味道：`EnterpriseController` 在用 `org.apache.commons.collections`（**collections 3**，
另一个 artifact），来源是 `velocity-tools → commons-beanutils` 的传递依赖，**没有任何 pom 声明过它** ——
等于拿传递依赖在编译。jar 摘不掉（那是 velocity 的），但我们不该依赖它。

**commons-text（0.25MB，2 处）→ `SolvelaTemplateUtil`**

只有邮件和站内信模板用 `StringSubstitutor` 做 `${key}` 替换。摘依赖**之前**先写了个临时用例，
拿真的 `StringSubstitutor` 跑 13 条语料逐条比对（转义 `$${}`、未闭合、空 key、`$100`、解析不到……）， 全绿之后才把期望值固化进
`SolvelaTemplateUtilTest` 并删掉临时用例 —— 和 SonicExcel 第③档摘 POI 同一个套路。

🔀 **一处有意的口径收紧**：`StringSubstitutor` 默认会把替换进去的值**再扫一遍**（递归解析）， 值里带 `${...}` 会被继续替换。
`SolvelaTemplateUtil` 不递归 —— 模板参数常来自用户数据，
递归解析等于给了一条模板注入的路。已固化成测试。


---

## 11. SonicExcel：自研 Excel 框架（2026-08-08，✅ 四档全部完成）

> **完整设计与决策记录见 `docs/SonicExcel-架构设计文档.md`**，本节只写接手人必须先知道的几件事。

彻底摘掉 `cn.idev.excel:fastexcel`（阿里系 EasyExcel fork）与 `org.apache.poi:*`，
换成基于 `org.dhatim:fastexcel`（法国人写的，同名不同物）自研的 SonicExcel。
**实测 −18.66MB，214 → 204 个 jar。**

| 档 | 内容 | 提交 |
|---|---|---|
| ① | 写引擎 + 元数据层（`@SonicTitle`），水印整体删除 | `0ab8cb57` |
| ② | 读引擎 + 转换器 Spring 集成，字典/枚举翻译从 service 收进转换器 | `3eaeb446` |
| ③ | 摘掉 POI 全家桶（连带 xmlbeans / ehcache / JAXB / commons-math3 等 15 个 jar） | `eb2146de` |
| ④ | 列宽自适应、CSV 通道、错误报告导出、导入模板下拉 | `1290abb7` |
| D10 | 字典标签反查（导入方向） | `23d7a5ea` |

### 11.1 🔴 三条不能碰的红线

1. **所有文本必须走 `Worksheet#inlineString`，禁止 `value(r, c, String)`。**
   fastexcel 的 `Workbook` 没有关闭 shared strings 的开关，`StringCache` 是个永不清理的 HashMap ——
   漏用一次，千万级高基数文本就会把堆吃穿。`SonicExcelWriteTest` 里有哨兵测试（断言产出包中
   `sharedStrings.xml` 的 `uniqueCount="0"`）。
2. **读侧只接受 `Path`，不接受 `InputStream`。** fastexcel-reader 拿到流会用
   `SeekableInMemoryByteChannel` 把整个 xlsx 读进堆 —— 100MB 上传文件在读第一行前先吃 100MB。
   API 签名就是防线，别加 InputStream 重载。
3. **`SonicStaxIsolation` 不能删。** fastexcel-reader 带进来的 `aalto-xml` 会通过
   `META-INF/services` 抢占 JVM 全局 StAX 实现，实测影响 spring-core / spring-web /
   hibernate-validator / mysql-connector-j / tika-core / **aws-query-protocol（S3 走它）**。
   注意 `XMLEventFactory` 的 JDK 实现类在 **`.events.` 子包**下，写错不会启动报错，
   要等首次使用才抛 `FactoryConfigurationError`。

### 11.2 明确不做的事

- **不兼容非 Excel 工具产出的 xlsx（WPS / Numbers）** —— 产品决策。只影响导入；我们导出的是标准 xlsx。
  这类文件是合法 zip，字节头挡不住，所以读引擎把 `ExcelReaderException` 统一翻译成
  「请用 Excel 另存为 .xlsx 后重试」。
- 水印、模板填充（`fill`）、`.xls`、多级表头、`WriteHandler` 式扩展点 —— 都是有意放弃的，理由见设计文档 §1.2。
- **模板下载还没有对外接口**：框架能力已就绪，加 `/xxx/importTemplate` 要配权限和前端入口，属产品决策。

### 11.3 🔴 顺带修掉一个全系统级 bug：BusinessException 的消息恒为 null

做 D10 时字典歧义的报错信息丢了才暴露出来 ——

`BusinessException` 上有 `@Data`，同时又声明了 `private String message` 字段。
Lombok 为它生成的 `getMessage()` **覆盖掉了 `Throwable.getMessage()`**
（Lombok 只看本类声明的方法，看不到继承来的），返回那个从未被赋值的字段 —— **恒为 null**。

而 `GlobalExceptionHandler` 是直接把 `e.getMessage()` 返给前端的，
**等于全系统每一个业务异常的提示语都是 null**，静默、不报错、不抛异常。

修复是删掉那个字段，让 `getMessage()` 回落到 `Throwable`。全项目没有任何地方调用过 `setMessage()`。
`BusinessExceptionTest` 钉住了四个构造器 + 子类。

⚠️ **接手人注意**：修好之后前端会开始收到**真实的业务错误信息**。
如果某些页面一直靠「提示为空就显示默认文案」兜着，那里的展现会变 —— 这是好事，但属于用户可见的行为变化，上线前值得扫一眼。

⚠️ 另外 `@Data` 打在异常类上还会生成 `equals` / `hashCode` / `toString`，
`toString` 覆盖 `Throwable` 的会让日志里的异常摘要变样。**这次没动**（不阻塞、改了会影响日志格式），留待判断。

### 11.4 测试

全量 **236 条**（solvela-base 128 / common-api 6 / solvela-marketing 73 / solvela-admin 29），`mvn clean test` 全绿。
SonicExcel 自己占 60 余条，覆盖：迁移语义快照、inline strings 红线、StAX 隔离、表头寻址、
错误策略三态与熔断、XXE 回归、zip 炸弹、临时文件生命周期。

⚠️ 上线前唯一没被自动化覆盖的面：**真实业务的导入导出冒烟**（需要 DB + Redis）。

---

## 12. 文件模块重写：素材库（2026-08-09 ~ 08-10，✅ 六档全部完成）

> **完整设计与决策记录见 `docs/文件模块-架构设计文档.md`**，本节只写接手人必须先知道的事。

把 Solvela 自带的文件上传整体重写成 **运营素材库**。核心不是加功能，是把
「字节 / 引用 / 呈现」三个身份拆开 —— 原实现里存储层认识 HTTP 类型（`MultipartFile`、
`ResponseDTO`）、业务权限编码在文件路径里（`private/` 前缀）、文件名有三个互相矛盾的真相源。
设计文档 §2 列的 11 个既有缺陷，有 8 个是拆开分层后自动消失的。

顺带 **AWS SDK 可选化：solvela-admin 运行时依赖 202 → 171 个 jar，97.15 → 88.78 MB**。

### 12.1 提交清单（分支 `upgrade/springboot4`）

| 档 | 内容                                                              | 提交       |
|----|-------------------------------------------------------------------|------------|
| ①  | 存储层地基 `solvela.base.storage`                                 | `8e1c8eec` |
| —  | 依赖治理：AWS SDK 标 optional + 摘 jsr305                         | `37e3ea9d` |
| —  | 架构设计文档                                                      | `d5a8e38a` |
| ②  | 数据模型 v3.53.0 + 修列改名断裂                                   | `ce71704f` |
| ②  | `FileAssetService`：校验三道关 + TEMP/CONFIRMED                   | `83c4fca8` |
| ③  | HTTP 层：RFC 6266 / 流式 / Range / 批量URL去N+1                   | `89e095e7` |
| ④  | 图片尺寸约束 / 标签检索 / 分类后台                                | `8a6d8f45` |
| ⑤  | 删 `IFileStorageService` 与两个存储实现                           | `d15b76c6` |
| ⑤  | 契约改名 `fileKey/fileName → storageKey/originalName`（跨前后端） | `c986da42` |
| ⑥  | `t_activity_display` + 富文本图片引用解析                         | `0af43911` |
| —  | 清退双写字段 v3.54.0（含一个上传故障修复）                        | `0d052fc3` |
| —  | 素材库后端接口（分类计数/详情含引用/改名打标签/安全删除）         | `57fff4ca` |
| —  | `ActivityDisplayController`                                       | `43476362` |
| —  | 素材库前端（分类卡片 → 网格 → 抽屉）                              | `f9cefc71` |
| —  | 活动展示配置组件 + 接入向导                                       | `98a0f134` |
| —  | 分类管理入口 + `categoryName` 校验                                | `648cf961` |
| —  | 展示配置的回访路径                                                | `1b929983` |

**SQL 已在开发库执行**：`v3.53.0`（建表 + 平移）、`v3.54.0`（drop 五个双写列）。

### 12.2 🔴 四条不能碰的红线

1. **`storageKey` 不可变，永不覆盖。** 换图 = 传新文件 + 改引用。
   覆盖同 key 意味着每次换图都要刷 CDN —— 刷新有延迟有配额有时刷不干净，
   最终表现为「有的用户看到新图有的看到旧图」。它同时是将来「活动配置推 CDN」的前置条件。
2. **key 里不含任何用户输入，扩展名从嗅探 MIME 反推。**
   用户传 `evil.html` 而内容是 PNG 时存成 `.png` —— 这一条直接掐死存储型 XSS。
3. **端到端流式，禁止 `byte[]`。** 旧实现名为流式实为全量进堆。
4. **`t_file_relation` 的引用登记不是可选步骤。** 不登记的图会被孤儿清理任务当垃圾删掉，
   表现是活动上线三个月后配图突然全变叉，且查不出是谁删的。

### 12.3 🔴 一个栽了两次的坑：列约束与代码写入面不一致

同一模块两次栽在同一类问题上，**接手时请把它当成检查清单**：

- **第一次**（`0d052fc3` 修复）：删掉旧 `FileService` 后，`t_file.folder_type` / `file_type`
  这两个 `NOT NULL` 且无默认值的列没人写了。MyBatis-Plus 跳过 null 字段不拼进 INSERT，
  MySQL 8 严格模式下**每一次上传都失败**。单测全绿是因为 DAO 全是 mock 的。
- **第二次**（`648cf961` 修复）：`t_file_category.category_name` 同样 `NOT NULL` 无默认值而代码没校验，
  用户收到一整段 `DataIntegrityViolationException` 的 SQL 原文 —— 既看不懂又泄露表结构。

> **规则：DB 上每一个 NOT NULL 且无默认值的列，代码里都必须有一处对应的写入或校验。**
> 这类问题单测结构上发现不了，只有集成测试或真跑一次才行。

### 12.4 两个环境/工具链的坑（都撞了两次）

1. **`mvn -pl <module> test` 会从本地仓库拿旧的上游模块**，报一堆莫名其妙的「找不到符号」。
   **验证必须走完整 reactor**（`mvn -o test`），`-pl` 只适合确定上游没变时用。
2. **Git Bash 发中文会按 GBK 编码**，用 curl 测带中文的 JSON 会收到「参数JSON格式错误」——
   那是测试端的问题不是接口的问题，浏览器发的是 UTF-8。

### 12.5 尚未完成（按优先级）

| # | 事项 | 状态 |
|---|---|---|
| 1 | ~~素材库选图器~~ | ✅ **2026-08-10 完成，见 §12.7**（真实后端点通：选图 → 保存 → 引用登记 → 刷新回显） |
| 2 | **孤儿清理定时任务** | 挂起，等定时任务模块重构后做。`TEMP` 状态、7 天窗口、`findUnreferenced` 都已就位，**只差调度**——所以现在 TEMP 记录只增不减 |
| 3 | **业务线接 `confirm`** | 意见反馈/公告等业务保存时未调 `confirm`，其附件停留在 TEMP。清理任务上线后这些图会被回收 |
| 4 | 内容去重 | `content_hash` 只存不用 |
| 5 | 客户端直传 | 10MB 以内代理上传够用；要传视频才需要 |

### 12.6 可视化验证的边界

后端 **358 条**测试 + 前端 `npm run build:test` 全绿。但要清楚它们证明不了什么：

- 前端 build 只抓语法与引用错误。本轮**两个真实前端 bug 都不是构建发现的**：
  渲染函数里写 `h('a-button')`（字符串标签浏览器不认识）、向导步骤常量改 computed 后漏 `.value`。
- **拿到 token 后用真实后端验过三条链路**（都通）：文件详情+引用反查、
  Range 下载（`206` / `Content-Range: bytes 0-99/77613`）、
  RFC 6266 双写法在真实文件名上的表现（`filename="1000066741 _1_.jpg"; filename*=UTF-8''...%20%281%29.jpg`）。
- **仍未做的**：素材库与活动向导的完整交互没有真跑起来点过。
  → **2026-08-10 补跑了，见 §12.7。一跑就跑出两个构建期和单测都发现不了的真 bug。**

---

## 12.7 素材库选图器（2026-08-10 ✅，附两个「没点过就发现不了」的 bug）

### 做了什么

| 文件 | 作用 |
|---|---|
| `components/support/material-picker/index.vue` | 选图器弹窗：左分类 / 右网格 + 文件名标签检索 + 分页，单选多选可切，**弹窗内可直接上传并自动选中** |
| `components/support/file-thumb/index.vue` | 素材缩略图。存在的唯一理由见下面第 2 个 bug |
| `lib/file-preview.js` | 按 fileId 取字节转 object URL，带 Promise 缓存 |
| `components/support/image-field/index.vue` | 图片字段（选/传/移除），从 `activity-display-form` 的内联渲染函数抽出来 |
| `FileQueryForm.imageOnly` + `FileMapper.xml` | 只列图片。按 `content_type LIKE 'image/%'` 而不是扩展名白名单——扩展名是从嗅探 MIME 反推的，content_type 才是原始结论 |

选图器与素材库页面 (`views/support/file/file-list.vue`) **刻意不共用模板**：那边是「管理」
（多选打标签、看引用、删除、管分类），这边是「挑一张」。硬凑一个组件只会两边都长满
`v-if="pickerMode"`。共用的是接口和取数口径，不是模板。

### 🔴 bug 1：`fileUrl` 少了 `/support` 前缀，下发的每一个文件链接都是死链

`FileAssetService.DOWNLOAD_PATH` 原本是 `/file/download/`，而 `FileController` 继承
`SupportBaseController`，类级 `@RequestMapping` 是 `/support` —— 真实路径是
`/support/file/download/{id}`。少一段的后果是**所有 `fileUrl` 指向不存在的路径**，
而 `<img>` 加载失败是静默的，页面上只是「图没出来」。

- 改法：生成用长形态 `/support/file/download/`，**反查匹配单独留一个短形态常量**
  `DOWNLOAD_PATH_MATCH = "/file/download/"`。两者必须分开：`fileIdFromDownloadPath` 是
  `indexOf` 子串匹配，短形态同时命中新老两种 URL；**若拿长前缀去匹配，历史富文本正文里
  那些 `/file/download/12` 会静默失配** —— 保存活动规则时登记不上引用，图随后被孤儿清理
  任务删掉（红线 4）。`FileAssetServiceTest` 里已加了同时断言三种形态的用例。

### 🔴 bug 2：`<img>` 发不出 Authorization 头，所以后端下发什么 URL 都没用

下载接口要登录态，而 token 存在 localStorage 不是 cookie，`<img src>` 带不上。
浏览器拿回来的是一段 JSON 错误体，表现同样是「图打不开且不报错」。
开发环境还多一层：前端 8081、后端 1024，**相对路径会打到 dev server 上**。

- 改法：预览一律走 `lib/file-preview.js`「带 token 取字节 → `URL.createObjectURL`」，
  收在 `FileThumb` 一个组件里。缓存的是 **Promise 不是结果**，否则同一张图并发挂载三次
  就发三个请求；失败的不留缓存，否则一次网络抖动会让这张图在整个会话里都显示不出来。
- **非图片不去取字节** —— 一个 20MB 的 zip 拿回来只为渲染一个「ZIP」占位。
- 这两个 bug 合起来意味着：**在此之前素材库的缩略图、详情抽屉的预览、展示配置的回显图
  从来没有显示出来过**，而 358 个后端单测 + 前端 build 全绿。§12.6 说的「没真跑过」
  遮住的就是这个。

### 真实后端验过的链路（活动 VJR856K8DW）

选图 → 确定 → 字段显示所选图 → 保存 → 刷新页面回显仍在（`mainImageId=108` 已落库）→
`t_file_relation` 出现 `ACTIVITY_DISPLAY/20` 且文件 `status` 由 TEMP(1) 转 CONFIRMED(2) →
详情抽屉显示「引用情况 1 处 · 被引用中，不能删除」。
另外验了 `imageOnly`：传一个 PDF 进库，选图器里不出现、素材库里正常出现（占位显示 PDF）。

### 12.7.1 素材库交互返工（同日，运营视角提的三条）

| 问题 | 改法 |
|---|---|
| 进了分类出不来 | 「层级」原先只活在组件状态里，**浏览器后退键没有东西可退**、刷新也掉回分类页。改成写进 URL（`?categoryId=1`，「全部素材」是 `categoryId=all`），并加了显式的「← 返回分类」按钮。⚠️ 面包屑那条链接**本来就是能点的**，实测响应正常 —— 真正的问题是它看起来不像按钮，加上后退键无效 |
| 分类卡片页的「上传素材」不知道传去哪 | 删掉全局按钮，上传入口挂到**每张分类卡片**上。文件必须落进某个分类，而在分类页按下全局按钮时「哪个分类」没有答案（原先默认丢进「通用」，运营看到的是"传完了但不在我以为的地方"） |
| 上传多一次点击 | 原先：点按钮 → 开一个什么都不问的弹窗 → 再点「点击上传」→ 才弹文件框。改成**点按钮直接弹系统文件框**，选完才出待传面板（逐个文件 上传中/已上传/失败）。**全成功自动关闭**，有失败才留着显示后端给的原话 |
| 卡片信息太少 | 卡片补上 **标签 / 上传日期 / 引用情况**。引用数是新加的 `FileVO.referenceCount`，`queryPage` 里一条相关子查询算完（走 `t_file_relation` 的 `idx_file`，不是 N+1）。它同时是删除守卫的依据，先在卡片上说清楚，别等点了删除才报错 |

「分类内网格页」的上传按钮文案写成「上传到「意见反馈」」：在「全部素材」里没有分类可言、
会退回通用分类，那就把落点在按下之前写明白。

### 一条排查经验：页面不合成帧时，CSS 离场动画不会结束

在浏览器里观察上传面板时，一度以为它「打开了但内容不渲染」。查到最后是
**Browser 面板没显示 → 页面不合成帧 → `animationend` 不触发 → antd 弹窗停在
`ant-zoom-leave-active` 上没被移除**，于是屏幕上留着一个空壳弹窗。
组件状态实测是对的（`uploadPanelFlag=true` + 队列里有记录）。
**这是无头观察方式的产物，不是代码问题** —— 下次再看到「DOM 在但状态说已关闭」，先想这条。

## 12.8 活动公开图（2026-08-10 ✅）：免登录读取口

> **C 端用户是匿名的，永远带不上 token。** 所以活动展示图必须有一条无鉴权也能取到的路 ——
> 这不是优化，是 C 端能不能显示图的前提。本节把这条路铺好了，**C 端本身仍未开工**。
>
> ⚠️ **先读 §12.8.5**：本节 §12.8.1 建立的「可见性由分类决定」当天就被整个撤掉了，
> 现在**没有私有文件**。留着这几节是因为其中的排查过程和修法仍然成立，
> 但凡是写「私有」的地方，结论以 §12.8.5 为准。

### 12.8.1 🔴 修掉一个还活着的越权：`/upload/**` 免登录端出整个上传目录

`FileConfig` 把本地上传目录整个挂成了静态资源，**不看 `visibility`**。实测不带任何 token
请求 `/upload/feedback/202608/10/xxx.jpg` 返回 200 + 完整字节，而同一个文件走
`/support/file/download/{id}` 是要登录态的。

**设计文档声称档⑤ 已经修掉的「本地模式下私有文件根本不私有」，其实换了个地方原样活着。**
它一直没暴露，是因为 `FileAssetService` 把每一次上传的 `visibility` 都写死成公开 ——
全库没有一个私有文件，这个矛盾就没有机会显形。**两个错误互相掩护。**

修法（v3.55.0 + 代码）：

| 改动 | 说明 |
|---|---|
| `t_file_category.default_visibility` | 可见性**由分类决定**，上传处不再写死。DB 默认值取「私有」——加分类的人没想过这件事时，结果是"图看不见"而不是"附件被公开了"，前者会被立刻发现 |
| 新分类 `ACTIVITY`（活动素材） | 唯一默认公开的分类。它的 code 会成为 storageKey 第一段（`activity/...`），将来在 CDN 上按前缀配规则靠它 |
| 存量文件一律转私有 | 原先全是公开，只是因为上传处写死了 1，不代表有人判断过 |
| 删掉 `/upload/**` 静态映射 | 换成 `FileController#publicAccess`（`/support/file/public/**`）：同样免登录、同样能被 CDN 回源，但**会查 visibility**，私有文件一律 404（不是 403 —— 不告诉外面"这个 key 存在但你没权限"） |
| `Cache-Control: immutable` | 敢这么写是因为 storageKey 不可变、永不覆盖（红线 1）。换图 = 换 key = 换 URL |

**为什么不做「公私分目录、静态映射只挂公开那棵树」**：决策 D2 写着**权限进 DB 不进路径**，
把可见性编回目录名就是在重犯 `private/` 前缀那个错。代价是每次公开请求多一次主键查询，
用 immutable 缓存头挡在浏览器与 CDN 之外。

### 12.8.2 URL 是后端拼的，不是前端拼的

常见做法是「后端给前缀、前端拼域名」。**本项目刻意多走半步**：库里只存 `storage_key`（相对），
但**完整 URL 由后端 `urlOf()` 产出**。这样「这张图该走 CDN 还是走鉴权接口」只在一处决定；
前端拼的话，每个页面都要知道一遍规则，将来加 CDN 就是满地改。

配置：`file.storage.public-url-prefix`，dev 指向 `http://127.0.0.1:1024/support/file/public/`，
上了对象存储/CDN 换成 CDN 域名 + 同一个 storageKey 即可，**业务代码与前端都不动**。
test/pre/prod **留空并写了理由**：占位符会拼出打不开的 URL，留空则稳稳退回登录态接口 ——
管理端照常可用、C 端图不显示，后者一眼能发现。

前端 `FileThumb` 因此有两条路且并存：公开图直接进 `<img>`（浏览器/CDN 缓存生效），
私有图仍走「带 token 取字节 → object URL」。**同一个网格里两种图各走各的**，
实测活动素材直接用公开 URL、意见反馈的图走 blob，都正常渲染。

### 12.8.3 🔴 「移除主视觉」曾经是完全无效的——两个 bug 互相掩护

实测点「移除」再保存：界面提示保存成功、图也消失了，刷新回来图还在，引用也没解除。
查下去是**两处**，而且**只修一处的话表面现象一点都不会变**：

1. **`updateById` 默认跳过 null 字段** → `mainImageId=null` 压根没进 UPDATE 语句。
   修法：`ActivityDisplay` 上九个可清空字段标 `@TableField(updateStrategy = ALWAYS)`
   （本表的保存语义是整表单覆盖，null 表示"运营清空了它"）。
   ⚠️ **不要顺手加到 `createTime`/`updateTime` 上** —— 那两列由 DDL 产生，标了会被写成 NULL（铁律 9）。
2. **`FileAssetService.confirm` 空集合直接 return** → 「清空最后一张图」时那句先清后建从没执行。
   3 张变 2 张是对的、1 张变 0 张是错的，**差一个元素，行为完全相反**。
   后果：那张图从此删不掉（守卫说"正被 1 处业务引用"），而实际上没有人在用它。

两条都补了测试，但性质不同，**这个区别值得记住**：
- `confirm` 那条是**行为测试**（断言 DAO 上发生了 deleteByBiz），mock 测得出来；
- `updateStrategy` 那条只能做**注解固化测试** —— null 跳过是 MyBatis-Plus 生成 SQL 时干的事，
  DAO 一 mock 前后都是绿的（§12.3 同一类陷阱）。所以固化的是「这九个字段必须能写回 null」
  这条约定，外加「时间列不许标 ALWAYS」的反向断言。

### 12.8.4 验收（真实后端，匿名与登录态两侧都验）

| 场景 | 结果 |
|---|---|
| 旧 `/upload/**` | 已消失（返回 NoResourceFound，不是文件字节） |
| 匿名取当时判定为私有的文件 | 404（可见性撤销后此场景不再存在，见 §12.8.5） |
| 目录穿越 `../../etc/passwd` | 被拦，「非法请求」 |
| 按 `categoryCode=ACTIVITY` 上传 | 落到活动素材分类 |
| 匿名取活动图 | 200 / 真实字节 / `immutable` / `inline` |
| 选图 → 保存 → 引用登记 | `status=CONFIRMED`、`ACTIVITY_DISPLAY/20` |
| 移除 → 保存 → 引用解除 | `refs=[]`（两个 bug 都修完才成立） |

⚠️ **ACTIVITY 分类在 dev 拿到的 id 是 6**，不是脚本注释里假设的数字 ——
正因如此上传接口补了 `categoryCode` 入参，前端传 `'ACTIVITY'` 而不是数字 ID。
**新增分类一律按 code 引用**，四个内置分类能用数字是迁移脚本对齐过的历史特例。

### 12.8.5 🔴 收尾：公开/私有这个维度被整个撤掉了（v3.56.0，同日）

**上面 §12.8.1 建立的「可见性由分类决定」，当天就被推翻了。** 读这一节之前先知道结论：
**现在没有私有文件，`t_file.visibility` 与 `t_file_category.default_visibility` 两列都已 DROP。**

产品判断：这个模块服务的是活动/任务配置，不是网盘。运营传进来的每一张图，
最终目的都是给匿名的 C 端用户看 —— 「公开」是唯一有意义的状态。为一个只有一种取值的维度
维护一列、一个枚举、一处上传判断和一处读取分支，是在为不存在的需求付利息。

**「不该公开的东西」怎么办**：不该进这个模块。将来若开放 C 端用户上传（头像、晒单图），
那是独立链路，要防的是**上传本身**（配额/审核/鉴黄），不是给文件挂一个标记 ——
两件事塞进同一张表只会两边都做不干净。

**但 §12.8.1 那个洞的修法保留了**：静态资源映射没有恢复，读取仍走
`/support/file/public/**`。理由与权限无关 —— 那段映射**只在本地存储模式下成立**，
切到对象存储时它什么都服务不了；走控制器则本地与云端是同一条代码路径，
Range / Content-Type / 日志也都由一处产出。

顺带把富文本的落脚点定了：新增 `CONTENT`（正文配图）分类，编辑器统一传到它里面。
不是为了权限（现在都公开），纯粹是**素材库别乱** —— 运营挑主视觉时不想在一堆正文截图里翻。

> **给接手人的提醒**：`visibility` 这条线在 git 历史里活了不到一天（v3.55.0 → v3.56.0）。
> 如果你在某处代码或文档里看到"私有文件"的字样，那是漏网的，按本节的结论改。

### 还没验的

- **多选模式**（`multiple` / `limit`）目前没有调用方，只有单选路径是真点过的。
- **C 端一行代码都没有**。公开图这条路铺好了，但「活动页长什么样」是另一整块。
- 富文本正文里的图仍走 `fileApi.uploadFile` 默认分类（私有），**编辑器里看不到、C 端也取不到**。
  要让它对 C 端可用，得把富文本的上传也指到 ACTIVITY 分类去 —— 一行 prop 的事，但要连着
  「历史正文里那些私有图怎么办」一起想，没顺手做。
- 富文本里插图仍走 `fileApi.uploadFile` 直接插 URL，**编辑器里那张图同样受 bug 2 影响**
  （wangeditor 的 `<img>` 也带不上 token）。要修得先决定公开图走不走 CDN / `publicUrlPrefix`，
  是独立的一块。
- `SchemaFormRenderer` 的 `image_upload` 控件**仍然是 mock**（写死 `https://cdn.example.com/task/...`）。
  改成真上传要一并在 `TaskConfigService` 里接 `confirm` 登记引用，否则正好撞红线 4。

---

## 13. 会员域地基与积分商城（2026-08-22）

> **一句话**：给系统补上了它一直缺的会员主表，并把全链路关联键从 `member_name` 换成了
> `member_id`。**库已经迁完并全量对账通过，Java 侧一行都还没动。**
> 商城只到 DDL + 原型，等会员域收口后再开工。

### 13.1 起因：`member_name` 是个没人定义过的全局主键

动手前的现状：**系统没有会员主表**。而 `member_name varchar(64)` 事实上是全局主键 ——
钱包、流水、券、履约、提案、任务、彩票、抽奖，十张表全靠它关联，却从来没有一张表来定义它。

它叫 "name"，所以任何人接手都会以为「用户改昵称就是改它」。
一旦真放开改名，十张表的历史数据当场指向一个不存在的账号 —— **不报错，只是查不到了**：
钱包余额还在，但这个人查不出自己的流水。

于是标识拆成三个，各司其职：

| 字段 | 给谁用 | 例 | 可变性 |
|---|---|---|---|
| `member_id bigint` | **机器** —— join / 对账 / 迁库合并 | `5579345309` | 永不可变 |
| `member_name varchar(32)` | **人** —— 加好友 / 报客服 / 分享 | `zhangsan_01` | 可改（限频） |
| `nickname varchar(64)` | **眼睛** —— 界面上显示的字 | `张三` | 随便改 |

**为什么关联键必须是 `member_id`**：合并库时 A 库有 `zhangsan`、B 库也有 `zhangsan`，
两个不是同一个人。关联键是 `member_name` 就只能让某一方用户改自己的号（产品灾难）；
关联键是 `member_id` 就给冲突方重新发号 + 留张映射表，用户完全无感。

### 13.2 会员号：10 位数字，Feistel 可逆置换

产品要求是「B 站 mid 那种数字，不要一堆乱码，10 位够了」。

**不能直接 `AUTO_INCREMENT` 从 1000000000 起** —— 那会泄露业务量：
竞对今天注册一个号、下月再注册一个，相减就是你一个月的真实新增。
B 站早期的 mid 就是顺序的，正因为被人拿去统计注册量才改成跳号。

做法：内部老实自增，对外用**可逆置换**打散。置换是双射，所以「绝不重复」是数学保证而非
概率保证 —— 不需要查库判重、不需要加锁。

- 域取 `2^34`，拆左右各 17 位，4 轮 Feistel，轮函数 `HMAC-SHA256(KEY, round‖right)` 取低 17 位
- cycle-walking 收窄到 90 亿，加偏移 `1000000000`
- 实测（200 万个号）：200 万序号 → 200 万个互不相同的会员号；20 万次 `decode(encode(x))==x`；
  平均 1.909 次迭代/号（理论 `2^34/9e9=1.909`）；
  「连续同向最长游程」5 —— 同一测量下 `java.util.Random` **也是 5**，顺序自增是 19999，
  即在该指标上与真随机源无法区分

**发号走号段模式**（`t_member_id_seq` 单行表，`step` 默认 1000）：
一次批发 1000 个到 JVM 内零售，热点行问题消失。
实测 32 线程 × 500 = 16000 个号：零重复、只打库 16 次、模拟重启后新实例与旧实例零重叠。

🔴 **两条红线**：
- `KEY` 上线后**永不能改** —— 改了等于换置换，新号可能撞已发出的老号。像对待数据库密码一样对待它。
- 主键/唯一键冲突时**让它抛异常，不要 catch 后重试** —— 那会把「置换实现写错了」这种
  严重问题，悄悄退化成一个随机重试的隐患。
- 重启丢弃当前段剩余的号是**无害**的：对外号本来就是跳着走的，90 亿容量里连零头都算不上。

参考实现（含自验 main）目前还在 scratchpad，**尚未落进 `common-api`**，见 §13.7。

### 13.3 几个值得记住的 DDL 取舍

**hash 列用 `binary(32)`，不是 `char(64)`。** 实测本机 MySQL 8.4.8 的索引键长：

| 列类型 | 索引键长 |
|---|---|
| `char(32)` utf8mb4（MD5 hex） | 128 字节 |
| `char(64)` utf8mb4（SHA-256 hex） | **256 字节** |
| `char(64)` ascii | 64 字节 |
| `binary(32)`（SHA-256 原始） | **32 字节** |

hex 编码 ×2、utf8mb4 的 CHAR 按每字符最大 4 字节算索引键长再 ×4 —— 合起来 **×8**。
摘要本来就是二进制，转成字符再存是纯粹多此一举。
顺带：`char(32)` 存 MD5 看着"更省"，实际索引键长是正确答案的 **4 倍**，还赔上算法。

**算法是 `HMAC-SHA256(KEY, 手机号)`，不是 `SHA-256(手机号+盐)`。**
手机号只有约 19 亿种可能取值，GPU 每秒能算几十亿次 —— **盐泄露就等于全部明文**。
HMAC 的 KEY 必须放配置中心/KMS，**不能和数据库备份躺在同一个地方**。

**没有 salt 列，也不要加。** Argon2 的 PHC 串已经把每次随机生成的盐带在里面
（实测同一密码连编两次，第 4 段不同、两串都能 `matches()` 通过）。
`password`+`salt` 两列是 MD5/SHA1 时代的遗产。
顺带把 `password` 从抄来的 `varchar(100)` 放宽到 255 —— 那串实测 **97 字符**，
只剩 3 字符余量，调高 Argon2 参数就会溢出，而非严格模式下是**静默截断**，
之后所有人登录失败且完全查不出原因。

**注销时两个标识的释放策略相反**（容易想当然套用）：
- 手机号**释放**（`phone_hash` 置 NULL）—— 手机号本来就会被运营商回收给别人
- `member_name` **永久占用，不释放** —— 它是社交身份。张三注销后李四拿到 `zhangsan`，
  老朋友加过来会以为还是张三，这是社工诈骗的现成入口。微信就是这么做的
- `member_id` 同样永不回收，否则新用户一查订单是别人的

**登录日志单独建表并按月分区。** `last_login_time` 不能挂主表：主表是热读表，
而且它的 `update_time` 挂着 `ON UPDATE`，每登录一次就刷新一次 ——
「资料最后修改于何时」这个信息会**永久丢失**，运营看到「刚更新过」其实只是这人登录了一下。
- 🔴 **分区表的每个唯一索引（主键也算）都必须包含分区键**，所以主键是 `(id, create_time)`。
  `AUTO_INCREMENT` 仍合法（只要求自增列是某索引的第一列）
- 实测 60 万行：`DROP PARTITION` 33ms vs `DELETE` 同范围 700ms，**快 21 倍**，
  而且不写 undo、不产生大事务、binlog 只有一条 DDL
- 代价也实测了：分区表索引是**本地索引**，`WHERE member_id=?` 不带分区键无法裁剪，
  要扫所有分区（695us vs 非分区 582us，约 20% 开销）。
  这就是「按月 + 只留 6 个月」的原因 —— 分区数控制在个位数。按天分区留两年会是 730 次索引查找
- 🔴 `pmax` 是兜底不是免维护许可证：没它超范围 INSERT 直接报错；有它数据会静默堆进去且
  DROP 不掉。**必须监控 pmax 行数，> 0 就告警**

**不复用既有的 `t_login_log`**：它的 `user_id` 是 `int`（上限 21 亿），
而会员号是 `1000000000~9999999999` —— **只有约 1/9 装得进去**，硬阻塞。
那张表是后台员工登录用的（`UserTypeEnum` 只有 `ADMIN_EMPLOYEE`）。

### 13.4 租户维度：先转正、再启用拦截器、最后<b>整个删掉</b>（v3.70.0 → v3.73.0）

这一节记的是一次<b>走了三步、最后原路退回</b>的改造。留着它不是为了记流水账，
是因为「退回」本身才是结论，而只看最终代码是看不出来的。

| 版本 | 做了什么 | 结果 |
|---|---|---|
| v3.70.0 | 默认租户 `'0'` → `'taozi'`，25 张表转正 | 已执行 |
| v3.71.0 起 | 启用 `TenantLineInnerInterceptor`，每条 SQL 自动追加 `tenant_id = ?` | 已执行 |
| **v3.73.0** | **27 张表的 `tenant_id` 全部 DROP，拦截器与白名单一并删除** | **已执行** |

**为什么最后删掉。** 盘点是实测的：27 张表 12,095 行，
**`tenant_id <> 'taozi'` 的行数 = 0** —— 这一列自始至终只有一个取值。
于是拦截器每条 SQL 追加的条件<b>恒为真</b>，而它的成本一点都不虚：

- 每个索引项白搭 6 字节，还诱导后来的人往索引里加它；
- 拦截器与分页拦截器的先后顺序是个坑（写反的表现是**总数按全租户算、数据按单租户查**，
  翻页全是空白且不报错）；JOIN 时条件该落 ON 还是 WHERE 又是一个；
- 每张新表都要记得进白名单，漏了就是静默的跨租户泄露口子 —— 为此还专门写了个启动自检去兜。

一句话：**为一件从未发生的事，长期维护一整套机制**。

🔴 **删了将来还能加回来，但不是免费的**：加回来 = 27 张表 ADD COLUMN（INSTANT，毫秒）
+ 需要隔离的唯一索引重建（INPLACE，实测 50 万行约 1.2 秒/条）+ 重新引入拦截器与白名单。
真正贵的是**那时表里已经有数据了**，存量行归属哪个租户要人来拍板。
所以这是一次**有意识的取舍**，不是「反正能加回来」。

**删列时索引会自动收窄，这正好是单租户下正确的语义**（7 条索引带着它）：

```
t_member.uk_mbr_name        (tenant_id, member_name)  ->  (member_name)
t_member.uk_mbr_phone_hash  (tenant_id, phone_hash)   ->  (phone_hash)
t_member.uk_mbr_email_hash  (tenant_id, email_hash)   ->  (email_hash)
t_member.idx_mbr_status_time / idx_mbr_source_time    ->  去掉最左的 tenant_id
t_member_login_log.idx_mbr_log_time                   ->  (create_time)
t_member_verify.uk_mbr_vrf_idcard                     ->  (id_card_hash)
```

三条唯一索引从「租户内唯一」变成「全平台唯一」——
一个手机号一个账号、一张身份证一个账号、账号名全局唯一，本来就该如此。

**执行顺序只有一条要求：先发代码、后跑 v3.73.0.sql**（或同一维护窗口内一起做）。
反过来会让旧代码的 INSERT 带上一个不存在的列 → `Unknown column 'tenant_id'`，整条链路当场挂。
⚠️ 这与 `member_name` 换键那次不同：那次要分三步是因为**换键**（新旧键必须并存一段），
这次是**纯删一个恒定值列**，没有任何查询依赖它的取值。

**删掉的东西**（都在 git 历史里）：`solvela-base/common/tenant/` 三个类 （`TenantContext` / `SolvelaTenantLineHandler` /
`TenantTableSelfCheck`）、
`TenantConst`、`MybatisPlusConfig` 里的拦截器注册，以及全部 Entity / Form / VO /
Mapper XML / 前端查询表单里的 `tenantId`。

### 13.4.3 SQL 脚本治理：基线取代增量（2026-08-22）

**起因**：「某天要搭一套新系统，我不知道要执行哪些 SQL」。查下来这个问题当时<b>没有答案</b> ——
77 张表的建表语句散落在 6 个 schema 文件 + 49 个版本迁移文件里，其中 3 张只在迁移日志、
**3 张（`t_task_event` / `t_task_record_flow` / `t_lottery_number_pool`）整个仓库都没有建表语句**。
照当时 README 说的「第一次部署只执行 `smart_admin_v3.sql`」，会缺掉整个营销域和会员域。

**现在**：新环境 = 两个文件。

| 文件 | 内容 | 生成方式 |
|---|---|---|
| `mysql/schema-baseline.sql` | 75 张表结构 | 从真库 `SHOW CREATE TABLE` 逐表导出 |
| `mysql/data-baseline.sql` | 18 张配置表、505 行种子数据 | 同上，**只有配置、无任何业务数据** |

验收标准是「空库能不能一把建出可用系统」：150 + 39 条语句零失败，
菜单 270 / 角色菜单 135 / 文件分类含四个内置 code / 定时任务 9 全部到位，
业务表全部 0 行。列与索引和源库<b>逐一比对完全一致</b>。

⚠️ 比对必须走 `information_schema`，**不能比 `SHOW CREATE TABLE` 文本** ——
MySQL 会省略可推断的 `CHARACTER SET`，语义相同的两张表文本不同，第一版比对误报了 6 张。

**删除的东西**（都在 git 历史里）：
- `sql-update-log/` 的 v3.15.0 ~ v3.69.0 共 **46 个**。删除前提是种子数据已收敛 ——
  那 46 个里有 17 个含 `INSERT`，光 `t_menu` 的增量插入就横跨 8 个文件，
  靠人按版本序拼才能得到完整菜单，漏一个就少一块功能入口且不报错。
- `mysql/smart_admin_v3.sql`（上游基线）与 `deploy/mysql-initdb/99-apply-update-log.sh`。
- 四个上游遗留 README（根目录、数据库脚本目录、web 下两个空文件）。

🔴 **删迁移文件连带打坏了 docker-compose**，差点留下一个坏掉的部署环境：
它把 `smart_admin_v3.sql` 挂成 initdb 脚本，还有个 shell 会**回放整个 sql-update-log**。
删掉 46 个之后那个 shell 只剩 3 个文件可放，其中 **v3.72.0 当时根本不该被执行**（它会提前删列）。
（这个隐患随 2026-08-22 删除整个 `sql-update-log/` 一并消失 —— initdb 现在只挂两个基线。）
已把 initdb 从「5 个 schema + 回放 45 个增量」改成只挂两个基线。
—— 教训：**删数据脚本前要 grep 部署配置，不只是 grep 代码和文档。**

**分域 DDL 文件（activity/lottery/task/member/mall）保留，但不是权威。**
它们的价值是设计注释；计划等模块完工后把注释搬进本文档，然后删掉、只留基线。
中间态的风险是两份定义静默漂移，所以有 `tools/CheckModuleDrift.java`。

> 首次跑这个检查：**11 张表在漂**。除了本轮 `member_id` 换键没回写之外，
> `t_task_record` 缺 `version`（v3.44.0 加的）、`t_task_template` 缺 `status`、
> `t_lottery_record` 缺 `dispatch_status`/`prize_code` —— 这些**很久以前**就漂了，一直没人发现。
> 靠纪律维护两份定义是不成立的，这正是「最终要合并」的理由。现已全部同步，检查通过。

### 13.5 关联键迁移（v3.71.0 / v3.71.1 / v3.72.0，均已执行）

**影响面 10 张表**（不是 9 张，见铁律 22）：

| 分类 | 表 | member_name 处理 |
|---|---|---|
| 状态（反复 UPDATE） | `t_member_wallet` `t_task_record` | **DROP COLUMN** |
| 单据（写完不改） | `t_member_asset_transaction` `t_member_coupon` `t_proposal_record` `t_physical_delivery` `t_prize_log` `t_draw_prize_log` `t_lottery_record` `t_task_record_flow` | 保留为**展示快照** |

**为什么单据类要留快照**：后台不用 join 会员表就能认出是谁；而且单据上本就该记
「当时那个账号」，不是这人现在叫什么 —— 审计要回答的是「当时是谁」。
这和 `t_mall_order` 里 `commodity_name`/`sku_attrs` 快照是同一个模式。
🔴 **快照列一定不建索引** —— 没索引，谁写了 `WHERE member_name=?` 会立刻表现为慢查询被发现；
建了索引，关联键就会悄悄退回 `member_name`，改名断链的问题原样复活。

**执行结果**（v3.71.0，2026-08-22 已在开发库执行）：
```
第 1 步  ADD COLUMN member_id  10 张表    成功 10 条
第 2 步  回填 UPDATE ... JOIN              成功 10 条，回填 9216 行
第 3 步  🔴 闸门：未回填行数 = 0            通过
第 4 步  换索引（先建→再删→改名）           成功 33 条
第 5 步  member_id 收紧 NOT NULL            成功 10 条
```
**全量对账**（不是抽样，逐行拿 `member_id` 回查 `t_member` 比对名字）：
**9216 行全部一致，零错位、零孤儿**；三条唯一约束换键后重复组均为 0。

播种 `t_member` 用的是 Feistel 发号器，2417 个不同 `member_name` → 2417 个不重复会员号。
其中 1 个不符合微信号格式：`zz_verify_manual_issue`（22 字符，超 20 上限），
是开发期造的测试账号，**正式上线前应清掉**。

**三个踩过的坑**：

1. 🔴 **排序规则不一致会让整个迁移跑不起来**。`t_member.member_name` 我一开始写死
   `utf8mb4_general_ci`，而十张存量表全是库默认的 `utf8mb4_0900_ai_ci` ——
   回填的 JOIN 直接抛 `Illegal mix of collations`。
   而 `0900_ai_ci` 本来就是大小写不敏感的，那个指定纯属多余还制造了不一致。
   **这类错只在跨表比较时暴露，建表当下完全看不出来。** 迁移前必查：
   ```sql
   SELECT TABLE_NAME, COLLATION_NAME FROM information_schema.COLUMNS
    WHERE TABLE_SCHEMA=DATABASE() AND COLUMN_NAME='member_name';
   -- 结果必须只有一种 COLLATION_NAME
   ```
2. 🔴 **换索引要「先建新的 → 再删旧的 → 最后改名」**。新旧同名不能共存，
   所以"先 DROP 再 ADD"中间必然有一段**索引不存在**的窗口。普通索引只是慢，
   **唯一索引是致命的** —— 那段窗口里重复数据能直接写进去，等你把唯一约束加回来才发现
   加不上，而脏数据已经落库。用临时名过渡，唯一约束全程一刻都没失效。
3. **迁移文件里不能混入「必须等代码切完才能跑」的步骤**。最初把删列写在 v3.71.0 的第 6 步，
   结果这个文件**没有任何一个时刻是可以整体执行的**。已拆出 **v3.72.0.sql**。

**「双键并存」的中间态曾经存在，现已收口**（v3.72.0 于 2026-08-22 执行）。
那个中间态是刻意设计的 —— `member_id` 先就位、`member_name` 先留着不带索引，
让「改库」和「改代码」可以分两次上线、各自回滚。收口之后的终态：

| | 状态 |
|---|---|
| 关联键 | `member_id`，十张表 NOT NULL，索引全部指向它 |
| 状态类两张（钱包 / 任务记录） | `member_name` **列已删除** |
| 单据类八张 | `member_name varchar(32) NULL`，**展示快照，身上没有任何索引** |

🔴 收口时两道闸门都跑过：单据类没有一行 `member_name` 超过 32 字符（收窄不会截断）、
十张表没有一行 `member_id` 指不回 `t_member`（删了快照就只剩这一条路）。

### 13.5.1 Java 侧换键（2026-08-22，本次）

十张表的 Entity / AddForm / QueryForm / VO / Mapper XML 全部切到 `memberId`，
连带整条运行态调用链（抽奖 / 彩票 / 任务 / 派发 / 账务）。编译与单测编译全绿。

**一条贯穿全部改动的规则**：**关联键只认 `member_id`，`member_name` 只做展示。**
落到具体代码上是三件事：

| 位置 | 做法 |
|---|---|
| 查询 / join / 唯一键 / Redis key / 分布式锁 key | 一律 `member_id` |
| 单据类表（8 张）的 `member_name` | 保留，**只写不读**，落库时记「当时那个账号」 |
| 状态类两张（钱包 / 任务记录） | Entity 里**没有** `memberName` 字段，join `t_member` 取当前值显示 |

🔴 **补了一个迁移编排上的缺口：新增 `v3.71.1.sql`，必须在新代码上线前执行。**
原计划 v3.71.0（改库）→ 改代码 → v3.72.0（删列）中间少了一步：
状态类两张表的 `member_name` 当时仍是 `NOT NULL 且无默认值`，
而新代码不再写这一列 —— MyBatis-Plus 省略 null 字段 + MySQL 严格模式，
钱包自愈初始化和任务记录创建会全部被拒（`Field 'member_name' doesn't have a default value`）。
v3.71.1 只做一件事：把那两列放开成可空。它对旧代码无害，可以随时提前跑。
**这已经是这个模式在本项目的第 7 次复发**（前 6 次见 `TaskEventContext` 类注释里的清单），
这次是在迁移编排上犯的。

**执行顺序是四步，少一步都不行**（开发库上已按此顺序全部走完）：

```
v3.71.0   改库                     随时
v3.71.1   放开 NOT NULL            ← 新代码上线之前
（部署新代码）
v3.72.0   删列 / 收窄快照           ← 代码确认不再读 member_name 之后
```

⚠️ 开发库上第四步是在 426 个测试（含 25 个真连库验收）全绿、确认没有任何地方
再读 `member_name` 之后直接跑的，**没有等「观察一个版本」** —— 开发库上没有版本可观察。
真上预发/生产时这一步仍应留出观察期。

**新增了 `MemberDao` / `MemberService`（`common-api` 的 `solvela.member`）**，
只做会员号 ⇄ 账号的翻译，是这次换键唯一的新依赖。刻意没建 `Member` 实体、没加缓存：
会员域的注册与资料维护还没开工，现在摆一个 20 多个字段的实体只会给人「会员模块已经有了」的错觉。

- `getXxx` 查不到返回 null（展示用），`requireXxx` 查不到直接抛（**写入路径一律用 require**）
- **查会员表只在请求边界查一次**，结果沿调用链传下去。抽奖 / 领号 / 事件上报各一次，
  不在每个要用名字的地方各查一次 —— 那是把一次往返变成三四次
- 顺带堵上一个老口子：以前随便传个字符串就能给一个**根本不存在的会员**记账、发货、发券，
  当场不报错，要等有人 join 会员表才发现

**六处刻意的取舍**（都属于「不写下来下一个人会当成 bug 改掉」那一类）：

1. **彩票签名要素仍是 `member_name`，没跟着换。**
   `security_sign = HMAC(lotteryCode|issueNo|sequenceNo|ticketNumber|memberName)`。
   口径一改，**所有存量号码验真集体失败**，客服那边表现为「记录被篡改」。
   而那一列是写完就不再变的快照，拿它签名本身自洽 —— 换成会员号并不会更安全。
2. **Redis 计数 key 从账号换成会员号**（抽奖限领 / 抽奖限流 / 彩票限流 / 风控频次）。
   代价是**换上去那一刻，当前周期的限领与频次计数从零开始**：
   key 变了，旧计数还在 Redis 里但没人再读。选在上线时点接受这一次性重置，
   比继续用一个「改名即归零」的 key 划算 —— 后者是每次改名都送一份额度。
3. **钱包 / 任务记录的列表页 join `t_member` 取名字，且必须 `LEFT JOIN`。**
   `INNER JOIN` 会让「会员被清理/注销」的那些行**整行从列表里消失** ——
   余额还在库里、页面上看不见，而且不报错。
   join 之后两表同名列（`status` / `create_time` / `create_by`…）必须全部带别名前缀，
   否则 MySQL 直接报 ambiguous。
4. **单据类的展示快照不 join 会员表取当前值**，就用落库那一份。
   审计要回答的是「当时是谁」。首页 TOP 榜同理：分组键是 `member_id`，
   名字取 `MIN(member_name)` 的快照 —— 按名字分组的话，改过名的人会裂成两行且看不出来。
5. **QueryForm 一律只收 `memberId`，删掉 `memberName`。**
   快照列没有索引，拿它当查询条件必是全表扫；而且它可改，按旧名字查等于查不到。
   ⚠️ **这是个不兼容变更，前端必须跟着改**（见 §13.7 第 2 条）。
6. **实物履约 Excel 导入仍收「会员账号」列**（运营记不住 10 位会员号），
   在服务端**批量**换成 `member_id`，换不到的账号**逐行报错退回**。
   以前一个拼错的账号会静默生成一张永远查不到主人的履约单。

**已知没改、且刻意没改的一处**：抽奖白名单
（`t_prize_pool_item.white_list`）仍按**账号**匹配 —— 白名单用户改名后名单静默失效。
它是运营配置，要连配置页和存量数据一起迁，属于会员域收口后的独立一步。

### 13.6 积分商城（表已建，Java 代码零行）

`数据库SQL脚本/mall.sql`，7 张表，**2026-08-22 已建进开发库**（16 条语句零失败），
`t_file_category` 也已播种 `MALL_COMMODITY`（`category_tag='商城'`）。
`CheckModuleDrift` 与 `VerifyFreshInstall` 都 PASS，基线 82 张表。

定位是**积分兑换，不是电商** ——
刻意不做购物车/多商品合单、优惠券叠加、满减、运费模板、分销、评价、售后工单、会员等级差异化定价。

🔴 **建表之后、写代码之前，四个坑先记在这里**（代码生成器一定会踩）：

1. **`t_member.member_id` 不是 `AUTO_INCREMENT`**。生成器模板对「主键+非自增」产出的是裸
   `@TableId`，而 MP 全局默认 `IdType.ASSIGN_ID` —— 会悄悄塞一个 **19 位雪花 ID** 当会员号，
   绕过 `MemberIdAllocator`，10 位数字的契约当场作废，而且唯一键拦不住（雪花本来就唯一）。
   必须手改成 `@TableId(type = IdType.INPUT)`。
2. **`t_mall_sku.available_stock` 是 VIRTUAL 生成列**（已实测确认）。
   `MybatisPlusConfig` 注册的 `InsertBatchSomeColumn("insertBatch", t -> true)` 是全字段插入，
   必然把它带进 INSERT → MySQL 3105。**实体里干脆别映射这一列**，
   可用库存只在查询 SQL 里 select 给 VO。
3. **`t_member_login_log` 是复合主键 `(id, create_time)` 的分区表**，且 append-only。
   生成器只会认 `id`，产出的 update/delete/表单页要直接删掉。
4. **不能进 VO / Form 的列**：`t_member.password`（Argon2 PHC 串）、
   `phone`/`email`/`real_name`/`id_card`（密文）、三个 `binary(32)` 的 `*_hash`
   （生成器给 `byte[]`，JSON 出去是 base64、Excel 导出直接炸）、
   `t_mall_commodity.detail_content`（mediumtext 富文本，会被塞进**列表** VO）。

另外 `t_member_id_seq` **不要生成 CRUD** —— 已有手写的 `MemberIdSeqDao`，
给「发号水位」开一个可编辑的后台页面，等于给「两个人共用一个会员号」开门。

初版 DDL（照通用电商模板削的）改动最大的几处：

- 🔴 **整个交易域是空的** —— 只有商品怎么陈列，没有一行记录「谁兑了什么」。
  补了 `t_mall_order`（一单一 SKU，不拆订单-订单项两级）
- **库存模型翻过来**：`total_stock` 恒为运营投放量，`available = total - locked - sold`（虚拟列）。
  旧模型卖一件减一件，运营再也看不出原始投放量。同时**删掉 `version` 乐观锁** ——
  行内条件 UPDATE 本身就防超卖，两套并存说明模型没想清楚
- **限兑并发正确性靠 `t_mall_exchange_limit` 的唯一索引**，不是 count 订单表
  （两个请求同时读到 count=0 会双双通过）
- `commodity_type` 对齐 `PrizeTypeEnum`（PHYSICAL/COUPON/BALANCE），
  **白捡 `sa/ledger/handler` 现成的履约链路**；并补了 `asset_ref` —— 初版写着「虚拟商品发卡密」
  却没有任何字段能存"发哪张券"
- **砍掉 `vip_rules`**：全库 grep 无 `member_level`，系统根本没有等级体系，该字段落地即空转
- **图册表整张删掉**，复用 `t_file_relation`（那张表的 `sort` 列注释原文就是「轮播图必需」）
- 图片存 `file_id` 不存 URL，但**商品图与素材库做分类隔离**（新增文件分类 `MALL_COMMODITY`，
  素材库列表固定排除）—— 商品图复用率接近零，混进去会把运营的策展空间冲垮，
  但绕过 `t_file_relation` 会让删除守卫失明（`FileAssetService:479`），运营删图后商城全站变死链
- **订单表不存收件信息**，只存 `address_id`。初版抄了一份 `receiver_*` 进订单表，
  与 `t_physical_delivery` 完全重叠 —— 同一份 PII 存两份、加密脱敏都要做两遍，还会不一致
- 地址簿三列密文（姓名/电话/门牌），**省市区刻意不加密**（识别不到个人，但后台要按省统计发货分布）。
  ⚠️ 列宽要按**密文**长度定，按明文定会静默截断
- 秒杀**不往商品表加字段**（一个商品会有多场，字段只能存一场；且 `seckill_price` 会变成
  第二价格源）。只在订单表预留了 `source_type`/`source_biz_id`。另：`SECKILL` 改叫 `FLASH_SALE`

原型 `docs/mall.html` 重写过，修了两类问题：
- 🔴 **铁律 6**：原版 `:dataSource` 与 `#bodyCell` 写在 in-DOM 模板里，
  浏览器强制小写 + Vue 不 camelize 插槽名 → SKU 表格整片空白且不报错。已挪进 `x-template`
- 🔴 删光规格时原版 `skuList = []` 直接返回，**无规格商品根本填不了价格库存**，
  提交出去 `skuList` 是空数组。DDL 注释一直写着「无规格商品填 `{}`」，前端从没生成过这一行

### 13.8 履约单泛化与收件信息加密（2026-08-22）

`t_physical_delivery` 一次改两件事，它们必须一起做。

**① `proposal_id` → `source_biz_id`**

原来这一列是 `proposal_id bigint NOT NULL`，唯一键 `(proposal_id, source_type)` ——
写死了「履约单必然来自发奖提案」。商城兑换实物根本没有提案 ID，
那条路径下这张表**插都插不进去**，实物商品也就落不了发货单。

泛化之后语义反而更正：这张表本来就有 `source_type`，
与 `t_proposal_record` / `t_member_coupon` / `t_mall_order` 那几张
「(source_type, source_biz_id) 定位上游」的口径完全一致 —— **只认单号，不认上游是什么业务**。

```
member_id / member_name / source_biz_id(varchar 64) / source_type
UNIQUE KEY uk_t_biz_phy_dlv_src (source_biz_id, source_type)
```

中奖链路写 `source_type='PROPOSAL'` + 提案 ID，商城将来写 `'MALL'` + 订单号，
运营的发货台与物流批量导入两条路径完全复用，一行都不用改。
换索引照旧「先建新的 → 再删旧的 → 改名」，唯一约束全程一刻都没失效。

**② 收件三列加密落库**

`receiver_name` / `receiver_phone` / `receiver_address` 现在是密文。 实现在 `solvela-base/common/crypto/`：`PiiCipher`
（AES-256-GCM）+ `PiiTypeHandler`。

|      |                                                                                   |
|------|-----------------------------------------------------------------------------------|
| 算法 | AES-256-GCM，每次加密新的随机 12 字节 IV，128 位认证标签                          |
| 格式 | `P1:` + base64(iv‖密文+tag)，前缀是**格式版本**，密钥轮换时靠它分辨新旧           |
| 密钥 | 配置 `solvela.crypto.pii.secret-key`，SHA-256 派生成 32 字节。四个 profile 各一把 |
| 列宽 | 姓名/电话 `varchar(255)`、地址 `varchar(512)`                                     |

**五处刻意的设计**：

1. **不复用 `ApiEncryptServiceAesImpl`。** 那个是<b>接口传输</b>加密，三条都不能用于落库：
   ECB 模式（相同明文恒产出相同密文 —— 不用解密就能按密文分组数出「哪些人住同一个地址」）、
   密钥 `"1024lab__1024lab"` 硬编码在源码里、出错时 `return ""`（落库场景下就是
   **把用户地址悄悄变成空串**，而且那一行再也救不回来）。
2. 🔴 **做成 TypeHandler，不是在 Service 里手动调。** 这张表的写入路径有四条
   （后台新增、后台编辑、Excel 新增导入、中奖自动建单），商城下单还要再加一条。
   手动调一定会漏，而漏了的表现是「那一行明文进库，当场完全正常」。
   钉在 JDBC 边界上就没有「忘记」这个选项。
3. 🔴 **两处必须同时挂**：实体的 `@TableField(typeHandler=...)` **加**
   `@TableName(autoResultMap = true)`（少了 autoResultMap，MP 只在写时用 typeHandler、
   读时不用 → 存进去是密文、查出来还是密文，且不报错）；VO 的 resultMap 上也要挂
   （列表页走自己的 resultMap，不吃实体注解 → 漏了就是页面上一片 `P1:...`）。
4. 🔴 **`PiiTypeHandler` 刻意不是 Spring Bean、刻意不带 `@MappedTypes`。**
   两者任一都会让 `MybatisPlusAutoConfiguration` 把它注册成**全局 String 处理器** ——
   全库每一个 varchar 列都会被当成 PII 加解密，菜单名、活动编码、物流单号统统变成 `P1:`。
   cipher 通过 `PiiCipherHolder` 这个只做一件事的 Bean 递进去。
5. 🔴 **空串与 null 一律不加密。** 不是省 CPU：`selectStatusStat` 用
   `receiver_address IS NULL OR receiver_address = ''` 数「收件信息没补全、想发也发不了」的单子。
   把空值也加密成密文，那个数字会**静默归零**，而运营正是靠它去催用户的。

**明文长度上限是从密文列宽反推的**，不是拍脑袋：密文长度 = `3 + base64(12 + 明文字节 + 16)`，
中文按 3 字节算 → 姓名 40 字(203) / 电话 30 位(83) / 地址 100 字(443)。
表单 `@Size`、Excel 导入的逐行校验、`PiiCipher.cipherTextLength` 是同一组数字，改一处要改三处。
放开上限而不改列宽的后果是 MySQL 非严格模式**静默截断密文** —— 存进去了，读出来解密失败。

**它防什么，不防什么**：防静态泄露（库被脱、备份被拷、DBA 直接 select）。
**不防**有应用权限的人 —— 后台运营打开发货单看到的仍是完整明文，不然没法发货、没法导给物流商。
「谁能看到完整收件信息」是权限点的问题，见 §13.7 第 7 条。

**验证**：`PiiCipherTest` 11 个单测（往返 / 同明文不同密文 / 改一个字节解密必须抛 /
换密钥必须抛 / 空密钥拒绝启动 / 长度公式与实测一致）；
`PhysicalDeliveryPiiTest` 3 个真连库用例 —— 写一行之后**用 jdbcTemplate 直接看库里存的是什么**
（必须 `P1:` 开头、搜不到明文），再确认实体读与列表 VO 读回来都是明文。
🔴 后者不能省：这次真正会出错的地方是**接线**，而接线错了不抛异常、编译与单测照样全绿。

### 13.7 下一步（按依赖顺序，不要跳）

1. ~~`MemberIdCodec` / `MemberIdAllocator` 落进 `common-api`~~ —— **已完成**
2. ~~十张表的 Java 侧切到 `memberId`~~ —— **已完成，见 §13.5.1**。
   前端 13 个文件已跟着改（查询字段 `memberName` → `memberId`，列表补出会员号列），
   `v3.71.1.sql` **已在开发库执行**，`mvn test` 426 个全绿
   （其中 `TaskRuntimeP0AcceptanceTest` 25 个是真连库跑的）。
   - ⚠️ **还差浏览器里真点一遍**：单测与 build 全绿**证明不了它能用**
     （本项目已多次「全绿但功能从未生效」）。重点看三处：
     列表页**分页总数与数据是否一致**（新 join 的写法没在真页面上验过）、
     按会员号搜索是否真过滤、钱包/任务记录列表的账号列是否显示得出来（LEFT JOIN 取的当前值）
   - ⚠️ 这几个迁移文件本身已随 `sql-update-log/` 目录一并删除（开发库全部执行完毕）。
     真要往预发/生产推时，从 `git log` 里把它们取回来，**严格按 §13.5.1 的四步顺序**跑
3. ~~上线观察一个版本，再执行 v3.72.0.sql 收口~~ —— **开发库已收口**（见 §13.5）。
   预发/生产推送时才需要重新走一遍那四步
4. ~~商城 7 张表建库~~ —— **已完成**（2026-08-22，见 §13.6）。
   `MALL_COMMODITY` 文件分类已播种，`FileCategoryService.SYSTEM_CODES` 也已补上它
   （不补的话运营删掉这个分类，历史商品图全成孤儿）。
   - ⚠️ 还欠一件配套的：**素材库列表要固定排除 `category_tag = '商城'`**，
     否则商品图会把运营的策展空间冲垮。现在没做也不出问题（商品图上传入口还不存在），
     但它必须和商城前端同一批上线，别拖到有几千张商品图之后
5. ~~`t_physical_delivery` 的 `proposal_id` 泛化成 `source_biz_id` + 收件三列加密~~
   —— **已完成，见 §13.8**
6. 商城后端骨架（下单事务 + 超时释放 job）。**动代码生成器之前先读 §13.6 那四个坑。**
   ⚠️ 超时释放三步（取消订单/释放锁定库存/回滚限兑计数）要在一个事务里，
   且**必须拆到独立 Bean**（铁律 11）
   - 🔴 `t_mall_address` 的收件三列<b>必须复用 §13.8 那套 PiiTypeHandler</b>，
     不要另写一份加密。两边各一套的话，「同一个地址在两张表里对不上」会非常难查
7. **收件信息的可见性权限点**。现在后台谁能打开发货单，谁就能看到完整的姓名/电话/地址
   （不脱敏是刻意的：运营要靠它发货）。加密防的是静态泄露，不防有应用权限的人 ——
   真要收口，得加权限点 + 脱敏展示 + 查看留痕，那是独立一件事

